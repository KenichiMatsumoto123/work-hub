# テスト実行用の環境変数（結合テスト・DB込みテスト用）
# このファイルを .env.test にコピーし、導入先アプリの値で埋める

NODE_ENV="test"

# テスト用DBの接続文字列（開発用・本番用DBとは必ず分離すること）
DATABASE_URL="mysql://root:password@localhost:33060/<アプリ名>-test"

# 以降、アプリの起動・テストに必要な環境変数を
# 導入先の .env.example を参考にテスト用の値で定義する
# （認証クッキー名・トークン有効期限・外部APIキーのダミー値 等）
