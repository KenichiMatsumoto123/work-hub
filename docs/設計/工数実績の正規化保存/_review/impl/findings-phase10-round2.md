# 実装レビュー結果（Phase 10 Round 2・差分レビュー）＋ Phase 10 完了記録

- **対象差分**：`cfed3f1..ce0a7e0`（`apps/web/src/server/functions/reports.ts` 1 ファイル・+135/-76）
- **レビュー日時**：2026-08-16
- **総合判定**：**PASS**（Critical 0・Major 0・新規 Minor 0）

## **Phase 10 の収束を宣言する**

`flow-review-policy` 4.2 の収束条件（Critical 0・Major 0）を Round 2 で満たした。

| Round | Critical | Major | 備考 |
|---|---|---|---|
| 1 | 0 | 2 → **実質 1** | FIND-001 は前提が誤読と判明し取り下げ。FIND-002（NUL バイト）のみ実在 |
| 2 | **0** | **0** | **収束** |

## Round 1 findings の解消状況

| finding | 判定 | 裏付け |
|---|---|---|
| FIND-001（マスタ解決の形） | **取り下げ**（指摘の前提が誤読）。ただし修正は人間判断により維持 | Round 1 の【重要な訂正】節を参照 |
| FIND-002（NUL バイト混入） | **解消** | `projectKeyString` / `taskKeyString` とも `JSON.stringify([...])` に統一。書き込み側と読み出し側が同一関数を呼ぶため形式の不一致なし。Python のバイト単位走査で 0 件 |

## Round 2 の重点判定

### (A) 解決の順序の維持 — **PASS（陽性対照つきの実測）**

**これが本差分の最重要リスクだった。**複数行 INSERT では **VALUES の並び順がそのまま行ロックの取得順序**になるため、順序が崩れれば設計の前提（デッドロック回避）が壊れる。

コード追跡：`filter()` は入力配列の相対順序を保存するため、`sortedKeys` → 1 周目の `pending` → 2 周目の `pending` を通じて順序が一貫して維持される。INSERT の `VALUES` は `keys.map(...)` でその順序を踏襲。

**実測（レビュアーが独立に実施）：**

| 実装 | 試行 | 40P01 |
|---|---|---|
| 現行実装（無改変） | 40 | **0** |
| 現行実装（無改変・再実行） | 40 | **0** |
| **陽性対照**：`clientNames` の `.sort()` を 1 行だけ除去 | 40 | **8** |
| 同上（別試行） | 40 | **3** |

**陽性対照でデッドロックが実際に発生することを確認したうえでの、現行実装 80 試行・0 件である。**これは「順序の固定こそがデッドロックを防いでいる」という設計書の主張を、実装に対して直接裏付ける測定でもある（AC-62 は自動テストで無検証と決定した項目であり、この実測が Phase 10 での担保にあたる）。

一時的なミューテーションは `git checkout` で復元し、`git diff HEAD` が空であることを確認済み。

### (B) AC-64・M-3・M-4 の維持 — **PASS**

- **AC-64**：「初回 SELECT →（INSERT ＋ 再 SELECT）を最大 2 回 → throw」を維持。**一部キーのみ未解決で残った場合も `pending` に残ったキーだけを次の周の対象にする**（部分解決を正しく扱う）
- **M-3**：`IS NOT DISTINCT FROM` をキーごとに `sql` テンプレートで組み立て `sql.join(conditions, sql\` OR \`)` で連結。ユーザー入力はすべて補間としてバインドされ、生 SQL 文字列連結は無い
- **M-4**：Set / Map による重複排除を解決前に行い、各マスタ種別につき 1 回のバッチ呼び出しに集約。問い合わせ回数はむしろ減っている

### (C) 複数行 INSERT 化に特有の失敗モード — **PASS**

- **部分的な ON CONFLICT 衝突**：毎周 `pending`（未解決キーのみ）に対して再 SELECT するため正しく拾える。80 試行で予期しないエラー 0 件
- **空配列**：`if (sortedKeys.length === 0) return resolved` により `select` / `insert` は一度も呼ばれない。`or(...)` や `sql.join` が空配列で呼ばれる経路は存在しない
- **パラメータ数上限**：`resolveTaskIds` の INSERT は 4 パラメータ/行のため、同一日報内で約 16,383 件のユニーク新規タスク名から PostgreSQL の 65535 上限に触れうる。設計書が「実運用でこの規模になることは想定しない」と明記する非機能要件の付随記述であり対応する AC は無い。**到達しても「エラーになる（黙って切り捨てられない）」という設計意図は保たれる**ため報告閾値未満と判定
- エラーメッセージが単一ラベルからカンマ結合の複数ラベルに変わったが、この文言を固定する AC は無い

### (D) FIND-002 の修正の完全性 — **PASS**（上表のとおり）

### (E) 既知の Minor — **変化なし**

MINOR-L2-1（DB エラー時の SQL 文露出・設計書が許容）・MINOR-L3-1（E2E-5 の flaky リスク・本差分の範囲外）・MINOR-L2-3（反復回数の字面）はいずれも本差分で悪化していない。MINOR-L2-3 については新しい JSDoc が終端条件を明記したため**曖昧さがむしろ減っている**。

---

# Phase 10 完了記録

## 実行検証（司令塔が独立に実測）

`_flow-config.md` の「実装承認スキップに伴う代償措置」により、**実行検証は 1 項目も省略しない**。

| 項目 | 結果 |
|---|---|
| `npm run check-types`（型） | **PASS**（エラー 0 件） |
| `npm run test`（単体・結合内部） | **213 PASS / 0 FAIL** |
| `npm run test:integration`（結合 DB込み） | **75 PASS / 0 FAIL** |
| `npm run test:e2e`（E2E） | **22 PASS / 0 FAIL**（既存 15 ＋ 新規 7） |
| `npm run build` | **PASS**（`✓ built in 1.97s`。成果物は gitignore 済みで作業ツリーは clean） |
| `npm run lint` | **該当なし**（本リポジトリに linter は無い。`AGENTS.md` の規定により `check-types` を代替の静的チェックとする） |
| NUL バイト | **0 件**（Python のバイト単位走査。`grep -P '\x00'` は NUL を検出できないため使用しない） |
| DB 残留 | `daily_reports` / `time_entries` / レビュー用マスタとも **0 件**、advisory lock **0 件** |
| `git status` | **clean** |

## 代償措置の充足確認

`_flow-config.md`「実装承認スキップに伴う代償措置（必須）」の 3 項目：

| 代償措置 | 充足状況 |
|---|---|
| Phase 10 の収束ループで Major の「文書化のみで通過」は使えない（Critical/Major の完全ゼロ化が必須） | **充足。**Round 2 で Critical 0・Major 0 を達成。文書化のみで通過させた Major は 1 件も無い |
| 実行検証（全テスト・E2E・lint・型・build）は 1 項目も省略しない | **充足。**上表のとおり全項目を司令塔が実測（lint は本リポジトリに存在せず `AGENTS.md` の規定により該当なし） |
| 戻り先が Phase 1 / 5 になる findings が出た場合は人間へエスカレーション | **該当なし。**Phase 10 の findings の戻り先はいずれも Phase 8（実装）であり、Phase 1 / 5 に戻るものは無かった |

## 残存する既知の限界（引き継ぎ事項）

本機能は完成しているが、次は自動テストで担保されていない。将来の変更時に留意すること。

| 項目 | 状態 |
|---|---|
| **AC-62**（マスタ解決の順序） | 自動テストでは無検証（人間判断・2026-08-15）。**Phase 10 Round 2 の陽性対照つき実測（順序を除去すると 40 試行で 8 件／3 件デッドロック、現行実装は 80 試行で 0 件）が担保にあたる。**順序を変更する際はこの測定を再実施すること |
| **AC-64**（再 SELECT 0 件の終端条件） | 無検証。READ COMMITTED では到達経路を決定的に構成できない。分離レベルを変更する場合は再判定が必要 |
| **`db:push` の非収束** | drizzle-kit 0.31.10 の introspection の制約。人間判断により受容。`AGENTS.md`「`db:push` の既知の挙動」に運用上の注意を記載済み |
| **未決事項 No.1** | 利用者の実 DB に対する重複・非正規化名の確認は**人間が `db:push` の前に実施する**。検証環境の DB では 6 クエリとも 0 件を確認済み |
| **MINOR-L2-1** | DB エラー時に生の SQL 文・列名が画面へ伝播する。設計書「エラー処理」節が明示的に許容。Phase 8 で書き込み経路が 5 経路に拡大し露出面が広がった |
| **MINOR-L3-1** | E2E-5 の `window.setTimeout` グローバルラップは flaky FAIL のリスクがある（false PASS 側には振れない） |
| **FIND-R5-M01** | `decodeURIComponent` 除去の退行を、ガードBの単体テスト 10 件が検出できない。実害ゼロの検出力空洞。Phase 7 で文書化のみで通過と決定 |
| **パラメータ数上限** | 同一日報内で約 16,383 件のユニーク新規タスク名から PostgreSQL の 65535 パラメータ上限に触れうる。設計書が想定しない規模であり、到達してもエラーになる（黙って切り捨てない） |

## フロー全体の記録

| Phase | 結果 |
|---|---|
| 1 設計書作成 | 完了 |
| 2 設計書 敵対的レビュー | **7 周**（上限 5 ＋延長 2）で終了 |
| 3 人間承認（設計書） | **承認**（2026-08-15） |
| 4 実装計画書 | lean 運用により省略 |
| 5 テスト作成（Red Phase） | 完了（観点表レビュー 4 周を経て凍結） |
| 6 テスト 敵対的レビュー | **5 周で収束**（Critical 0・Major 0） |
| 7 人間承認（テスト） | **承認**（2026-08-15） |
| 8 実装（Green Phase） | 完了（Red Phase の 132 FAIL / 56 FAIL がすべてゼロに） |
| 9 リファクタ | 人間判断により省略 |
| 10 実装検証 | **2 周で収束**（Critical 0・Major 0） |
| 実装承認ゲート | **スキップ**（`_flow-config.md` の設定。代償措置は上記のとおり充足） |

**本フローは完了。**
