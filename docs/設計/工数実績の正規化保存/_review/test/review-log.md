# レビュー収束ログ（Phase 5・テスト観点表）

対象：`docs/設計/工数実績の正規化保存/_review/test/観点表.md`
方式：`adversarial-reviewer` 役割のサブエージェントを観点レーンごとに fresh context で並列起動

---

## Round 1

- レビュー日：2026-08-15
- レーン：A（網羅性）／B（期待結果の厳密さ・設計書との整合）／C（実現可能性）
- 総合判定：**FAIL**（Critical 2・Major 17・Minor 25）

各レーンには、この観点表が**対話方式で確定した**（人間がファイルを直接編集できない環境のため、司令塔が案と論点を提示し人間が採否を決定した）ことを伝え、**司令塔の起案に人間の判断が引きずられた可能性を能動的に疑う**よう指示した。

### Critical

| ID | 見出し | 検出レーン |
|---|---|---|
| R1-C1 | 1-7・4-1（AC-39 原子性）を対象外とした根拠が**2 つとも事実誤認**であり、不可逆 DELETE のデータ損失経路が全面無検証になる | A |
| R1-C2 | 1 章に後始末・名前空間の規定が一切なく、絶対件数を主張する期待結果が**同一スイート内で決定的に破綻する** | C（A・B も Major として検出） |

**R1-C1 の詳細（司令塔の説明が誤っていた）**

対象外の根拠として司令塔が示した 2 点は、いずれも成立しない。

1. 「障害注入には実装構造の規定が必要（申し送り制約 #4）」→ **制約 #4 は AC-64（再 SELECT 0 件）についての記述**であり AC-39 のものではない。原子性の障害注入は、テストの `beforeAll` で一時的な CHECK 制約を張る（例 `CHECK (hours <> 13.13)`）だけで成立し、**実装側に何も要求しない**
2. 「壊しうる経路は T-3 と AC-72 がカバーする」→ **カバーしていない。**AC-72 が規定するのは**アドバイザリロックの取得文だけ**の所属である

結果、次の実装が観点表の全行を PASS する：

```
db.transaction(async (tx) => {
  await tx.execute(sql`SELECT pg_advisory_xact_lock(...)`)   // 4-2/4-3/4-5 PASS
  await tx.insert(dailyReports)...
  await db.delete(timeEntries).where(...)      // ← db を直接使用
  await db.insert(timeEntries).values(rows)    // ← db を直接使用
})
```

正常系では結果が同じため全観点を通過するが、E-5（接続断・デッドロック・一意制約競合）でトランザクションが失敗した瞬間、**`time_entries` の DELETE だけが確定し `daily_reports` はロールバックする**。本機能で唯一の不可逆操作が巻き戻らない。

**R1-C2 の詳細（実測）**

1 章の期待結果は絶対件数で書かれている（1-2「1 件のまま」、3-4「2 件のみ」、3-5「合計 3 件」等）が、1 章には後始末規定も名前空間規定も無い。`vitest.integration.config.ts` は `fileParallelism: false`（直列）であり、**直列だからこそ前のケースが作ったマスタが必ず残る**。名前も衝突する（2-1 と 3-4 が `"A社"`、3-3 と 3-6 が `"基幹刷新"`、2-3 と 3-6 が `"調査"`）。

レーンC がクリーンな `workhub_test` 上で 2-1 → 3-5 → E2E-1 → 3-4 の順に投入して実測：

```
3-4 が 2 件を期待する時点の clients 総数 = 6
3-5 が 3 件を期待する時点の clients 総数 = 6
```

CI は毎回まっさらだが**同一 run 内の累積だけで落ちる**。ローカルは開発用 DB に接続する運用のためさらに落ちる。設計書の申し送り制約 #7 が「Phase 5 で方針を決めること」と名指しした事項が未決のまま凍結されようとしていた（MC-4）。

### Major（重複統合後）

| ID | 見出し | 検出レーン |
|---|---|---|
| R1-M1 | AC-03・AC-08・AC-09 に対応する行が 1 つも存在せず、`DELETE` の範囲を絞る実装・空行以降を捨てる実装が全行を PASS する | A・B |
| R1-M2 | 4-7u（単体降格）が謳う検出力を持たず（呼び出し側で使われることを検証しない）、かつ**承認済み設計書に無い実装構造規定**を新たに要求している | A・B・C |
| R1-M3 | 「トランザクション → 並行実行時の競合」のうち**異なる日付の同時保存＝マスタ作成競合**が全面未起案。ON CONFLICT 欠落・再 SELECT 欠落・分離レベル変更のいずれも検出されない | A |
| R1-M4 | 設計書が「置かない選択は認めない」と明記した `deleteReportFn` の `requireSession` 静的検証が、凍結成果物のどこにも記録されていない | A・C |
| R1-M5 | 1-6① の Given が「削除済みの日付」だけのため、**`time_entries` の件数で判定する実装**が PASS する。その実装は実績 0 件の日報を削除できなくする | B |
| R1-M6 | AC-89 の「アドバイザリロック取得**後**に存在確認する」を落とせる行がなく、4-3 の Given が対象日のデータ有無を規定していない | B・C |
| R1-M7 | 4-2 の Given（保存 1 本）と期待結果（「後にコミットされた側のみ」「合算にならない」）が噛み合わず、AC-45 の中核が無検証 | B・C |
| R1-M8 | 3-7 の Given に `projects` が無く、AC-57 の「`is_archived` が true の projects も再利用」が無検証。違反実装ではその取引先の日報が二度と保存できなくなる | B |
| R1-M9 | E2E-6 の期待結果「両方とも 401」は、規定どおりの本文を送ると**正しい実装でも 500 になる**（seroval の `fromJSON` が `requireSession` より先に throw する。実測） | B |
| R1-M10 | 関連 AC 欄が、期待結果が検証しない AC を掲げている（2-5→AC-56/66/67、2-6→AC-31/71、3-1→AC-50）。Phase 6 の AC×テスト対応マトリクスに偽の ✅ が立つ | B |
| R1-M11 | 4-5 は「0 行＝合格」の判定形で陽性対照がなく、`pg_locks` のキー分解も未規定のため**照会を誤っても必ず PASS** する | B |
| R1-M12 | 4-4・4-3 の「await せずに起動 → 後から待つ」手順は、正しい実装で Vitest の unhandled rejection を起こし run 全体を落とす（実測。テストは PASS 表示のまま exit code 1） | C |
| R1-M13 | 4-5 の「トランザクション外で 1 文だけ実行する実装は 4-2 で落ちる」は**事実に反する**（実測：待たされて見えるうえ完了直後の `pg_locks` にも残らない）。AC-72 の当該分岐は無検証 | C |
| R1-M14 | 「待たされていることの判定待ち時間」の規定が**向きを取り違えている**（「解放操作より短く」は上限。必要なのは非ブロック完走時間に対する下限）。短すぎると違反実装が false PASS する | C |
| R1-M15 | E2E-5 の「2 秒以内に」が強制も観測もされておらず、窓を外すと**違反実装でも PASS** する（`retries: 2` と相まって恒常的に無検出になりうる） | C |
| R1-M16 | E2E-7・E2E-4 の「保存操作から 3 秒後」は自動消去タイマーの起点（保存完了後）とずれており、E2E-7 は false FAIL、E2E-4 は false PASS になりうる | C |
| R1-M17 | E2E-2 は 1 回目と 2 回目の成功文言が完全に同一のため、**2 回目のクリックが空振りしても PASS** する | C |

### レーンが PASS と判定した項目（実測の裏付け）

| 項目 | 裏付け |
|---|---|
| 並行性テストの土台（専用クライアント `{ max: 1 }`） | レーンC 実測：専用クライアントがロック保持中、`testDb.transaction()` 内の `pg_advisory_xact_lock` は 300ms 経っても未解決、解放後 307ms で完了 |
| 申し送り制約 #1（プール共有による再入）の実在 | レーンC 実測：共有プールでセッションロックを取ると後続の `begin()` が**同一 pid に割り当たって 3ms で獲得**してしまう。専用クライアントを立てる判断は正しい |
| 申し送り制約 #3（直接呼び出しの戻り値が `undefined`） | レーンC 実測：`RETURN VALUE = undefined`、`daily_reports` は `ROWS = 1`。1-8・5-9・5-10 の依存先（`Error.message`・DB 直読み）と整合 |
| 3-4 の期待件数「`"A社"` と `"A 社"` の 2 件のみ」 | レーンB 実測：`s.replace(/\s+/gu,' ').trim()` を 6 入力に適用し distinct 2 件を確認。R-1・AC-16 と一致 |
| 2-6・2-7 の丸め期待値 | レーンB・C 実測（PostgreSQL 16.13）：`'1.005'`→`1.01`、`'0.005'`→`0.01`、`'24'`/`'24.004'`→`24.00`。JS の `Math.round(n*100)/100` は `1.00` になり結果が分かれる |
| 5-7 の `parseFloat` 解釈 | レーンB 実測：`parseFloat('0x10')` = 0、`parseFloat('7.5h')` = 7.5、20 桁 = 1.23e19 |
| 5-10 の閏年 4 分岐・2 桁年 | レーンB 実測：2028/2000/0004 = true、2026/1900/0050 = false。AC-84・AC-85 と完全一致 |
| 5-10 の TZ 3 通り実行 | レーンC 実測：`process.env.TZ` の実行時変更が `Date` にも `Intl` にも反映される（Node v22） |
| 4-5 の `pg_locks` 観測手段そのもの | レーンC 実測：T-1 の単一 bigint 形式は `classid=0, objid=9, objsubid=1` として別接続から見え、コミット後 0 件 |
| E2E-6 の URL 導出手順 | レーンB 実測：dev の ID 生成規則・`export` = `<変数名>_createServerFn_handler`・`url = TSS_SERVER_FN_BASE + functionId`・POST は URL 無改変、をすべて実装で確認 |
| 2.1 節のセレクタとソース参照 | レーンB がすべて実ファイルで確認（1 件のみ行番号ずれ＝Minor） |
| 2 章のクリティカルパス業務チェックリスト網羅 | レーンA：正常系完走＝E2E-1、入力不正＝E2E-4、権限なし＝E2E-6、対象なし＝E2E-3、中断／再開＝E2E-5、前提データなし＝E2E-3 |
| 2-4・3-9・4-8 の対象外判断 | レーンA が各理由の事実性を確認。2-4 は非準拠実装も 5-5 が捕まえる、4-8 は決定的に構成できない、3-9 は土台では代替できない |

### 司令塔による裁定（Round 1）

**Critical 2 件のうち R1-C1 と、Major の R1-M2 は、司令塔が人間に提示した説明が事実として誤っていたことに起因する。**人間の判断そのものではなく判断材料に瑕疵があったため、正しい事実を提示して再判断を仰ぐ（`flow-review-policy` 4.3-2）。

残りは司令塔が観点表を修正し、差分再レビュー（Round 2）にかける。

---

## Round 2（差分レビュー）

- レビュー日：2026-08-15
- 対象：Round 1 findings を反映したコミット `8b19d0c` の差分
- 総合判定：**FAIL**（Critical 1・Major 11・Minor 19）

### Round 1 findings の解消状況

| ID | 判定 | 備考 |
|---|---|---|
| R1-C1（1-7 の対象外根拠） | **解消** | 実施へ変更。障害注入が機能することを 3 レーンが実測 |
| R1-C2（後始末・名前空間） | **部分的** | 規定 1〜3・5 は解消。**規定 4（日付割当）が新たな Critical を生成**（R2-C1） |
| R1-M1（AC-03/08/09） | **部分的** | AC-08・AC-09 は追加。**AC-03 は未追加**（司令塔のコミットメッセージは追加したと述べていたが誤り） |
| R1-M2（4-7u） | **部分的** | 6-1 へ委譲したが、委譲先も実行不能（R2-M4） |
| R1-M3（マスタ作成競合） | **部分的** | 4-9 新設。入力仕様が未規定で再 SELECT 欠落実装が silent PASS しうる |
| R1-M4（requireSession 静的検証） | **部分的** | 6-2 に記録。判定方法が未規定で素直に書くと退行を落とせない |
| R1-M5（1-6① 件数判定） | **解消** | 1-6③ の追加で決定的に落とせることを実測 |
| R1-M6（AC-89 のロック順序） | **部分的** | 4-3 の Given は明記。「ロック取得後」は依然無検証 |
| R1-M7（4-2 の Given） | **解消** | 2 本起動に修正。実測で識別力を確認 |
| R1-M8（3-7 の projects） | **解消** | — |
| R1-M9（E2E-6 の 500） | **解消・別の違反を導入** | 技術的には正しいが**承認済み設計書の明文に反する**（R2-M6） |
| R1-M10（偽の関連 AC） | **部分的** | 2-5・2-6・3-1 は解消。**4-5（AC-72）・4-9（AC-64）で同型が再発** |
| R1-M11（4-5 の陽性対照） | **解消** | 照会式が正のキーで機能することを実測 |
| R1-M12（unhandled rejection） | **解消** | 実測：後付け＝EXIT=1、起動時装着＝EXIT=0 |
| R1-M13（AC-72 の TX 外 1 文） | **未解消** | 誤記述を削除しただけ。4-2・4-3・4-4・4-5 すべてを通過することを実測 |
| R1-M14（待ち時間の向き） | **解消・新規欠陥** | 下限規定に修正。ただし T 計測手順が 4-9/4-3/4-4 の Given を破壊する（R2-M2） |
| R1-M15（E2E-5 の窓） | **部分的** | 強制は追加されたが測定量が AC-65 の要求より緩く、false PASS が残る |
| R1-M16（タイマー起点） | **解消** | — |
| R1-M17（E2E-2 の空振り） | **解消・別の違反を導入** | POST 2 回確認で解消。**追加した DB 直検証が設計書の E2E スコープ規定に反する**（R2-M7） |

### Critical

| ID | 見出し | レーン |
|---|---|---|
| R2-C1 | 1.0 節の日付フォールバック式 `2000-02-<観点番号の下2桁>` が**存在しない日付**（`2000-02-32` 等）を生成し、3-2〜3-8・5-1〜5-9 の 15 観点が正しい実装に対して決定的に FAIL する | A・B・C |

**R2-C1 は R1-C2 を修正するために新設した規則そのものが同型の欠陥を再生産したものである。**しかも FAIL の原因が「日付検証を正しく実装したこと」であるため、Phase 8 で「テストが落ちる → 日付検証を緩める」方向の圧力を生む。

### Major（重複統合後）

| ID | 見出し | レーン |
|---|---|---|
| R2-M1 | **4-4（AC-55）が違反実装を一切検出しない。**ロックを取らない実装で 60 試行して孤児 0 回。READ COMMITTED では delete の存在確認が save の未コミット行を見られず、**AC-89 により delete は必ず reject・save は必ず成功**するため、ロックの有無で決着が変わらない | C |
| R2-M2 | 規定 4 の「同一テスト内で T を計測する」手順が 4-9・4-3・4-4 の Given を破壊する（T 計測の副作用の後始末が未規定） | C |
| R2-M3 | 接頭辞方式が 5-4・5-6 に適用できない（`(名称未設定)` / `[object Object]` は固定値で接頭辞を付けられず、後始末経路が無い） | C |
| R2-M4 | 6-1（AC-62 発行順）は `db` への介入点が無く実行不能。`vi.spyOn(db,'transaction')` は実測で失敗（Proxy のため）。`vi.mock` には実装の DB アクセス呼び出し形の前提が要り、**設計書に無い実装構造の想定なしには書けない**（4-7u を撤回した理由と同型） | A・C |
| R2-M5 | AC-03 が依然どの行にも対応せず、最初の無効行で走査を打ち切る実装が全行を PASS する | A・B |
| R2-M6 | E2E-6 の「本文を送らない」が**承認済み設計書の明文（:1010・:1027「本文の date にセンチネル日付を入れる」）に反する**。技術的結論は正しいが、逸脱の記録もエスカレーションもない | B |
| R2-M7 | E2E-2 に追加した DB 直検証が設計書:992・1013（E2E は画面上で観測できる事実に限定）に反する。かつ R1-M17 の解消には不要だった | B |
| R2-M8 | AC-48（無効行の 300 文字名でも保存成功）がどの行にも対応せず、対象外理由も無い。長さ検証を全行に適用する実装が全行を PASS し、目的節のデグレード制限に違反する | A・B |
| R2-M9 | AC-72 の「トランザクション外で取得して即解放される実装」が 4-2・4-3・4-4・4-5 のすべてを通過する（実測）。R1-M13 は誤記述の削除のみで穴が残る | A・B・C |
| R2-M10 | 1-7 の一時 CHECK 制約に冪等な張り直し規定が無く、異常終了時に当該テストファイルが恒久的に起動不能になり開発 DB に制約が残留する（実測：2 回目の ADD が 42710） | C |
| R2-M11 | 日付割当の自己違反（1-11 が 1-1 と、1-12 が 1-2 と、1-10 が 1-5 と同じ日付）＋接頭辞書式 `IT<観点番号>-` の導出規則が一意でない（1-2 → `IT12-`? `IT1-2-`?） | A・B・C |

### 人間へのエスカレーションが必要な項目

`flow-review-policy` 4.3-2（承認済み成果物の変更が必要）に該当する。

1. **R2-M6**：E2E-6 の本文（設計書 :1010・:1027 の撤回可否）
2. **R2-M7**：E2E-2 の DB 直検証（設計書 :992・:1013 の拡張可否）
3. **R2-M4**：AC-62 の検証手段（設計書に実装構造の注入点を追加するか、機能的固定に切り替えるか、無検証と記録するか）
4. **R2-M1**：AC-55 の検証手段（AC-89 により孤児経路が塞がれた結果、判定不能になった）

---

## Round 3（差分レビュー）

- レビュー日：2026-08-15
- 対象：Round 2 findings を反映したコミット `984c60d` の差分
- 総合判定：**FAIL**（Critical 2・Major 9・Minor 17）

### Round 2 findings の解消状況

| ID | 判定 | 根拠 |
|---|---|---|
| R2-C1（日付フォールバック） | **解消** | 3 レーンが機械検査。**66 日付・重複 0・全件 `::date` キャスト成功**。E2E との衝突なし |
| R2-M1（4-4 の検出力ゼロ） | **解消** | レーンC が 4 実装を実走。判定①（解放前に両方未解決）は正実装 PASS×3／ロック無・行ロック代用・存在確認をロック前の 3 違反はいずれも FAIL×3。**決定的に落とせる** |
| R2-M2（T 計測が Given を破壊） | **部分的** | 捨て日付へ分離は解消。計測ペイロード未規定・`beforeAll` が規定 1〜3 の射程外（R3-M3） |
| R2-M3（5-4・5-6 の後始末） | **未解消** | 規定 4（id 採取）を実走すると **23503 で失敗**するか、テストが作っていない共有行を削除する（R3-M4） |
| R2-M4（6-1 が実行不能） | **解消・代替が無効** | 6-1 撤回は妥当。代替の 4-10 が検出力を持たない（R3-C2） |
| R2-M5（AC-03） | **解消・新規欠陥** | 5-11 の期待値は AC-03 と完全一致（実測）。ただし `task_id` の対応付けを見ない（R3-M1） |
| R2-M6（E2E-6 の本文） | **解消** | 設計書 2 箇所を人間承認のうえ改訂済み。観点表と語句レベルで一致 |
| R2-M7（E2E-2 の DB 検証） | **解消** | 削除され設計書の E2E スコープ規定と整合 |
| R2-M8（AC-48） | **解消** | 5-12 が R-6「対象行に限る」を落とした実装を throw で捕捉 |
| R2-M9（AC-72 の TX 外 1 文） | **部分的** | 4-5③ は `saveReportFn` について**実測で識別成立**（正 1 行／TX 外 1 文 0 行／セッション 1 行）。**`deleteReportFn` 側は依然無検証**（R3-M2） |
| R2-M10（1-7 の DDL 冪等化） | **解消** | 2 周連続実行で両周成功 |
| R2-M11（日付・接頭辞） | **解消** | 書式 `IT<節2桁><観点2桁>-` は 8 文字固定で `LIKE` の相互衝突なし |

### Critical

| ID | 見出し | レーン |
|---|---|---|
| R3-C1 | **5-7 が正しい実装で決定的に FAIL する。**3 値（`"7.5h"` / `"0x10"` / 20 桁）を単一の保存に入れる構成のまま「`"7.5h"`→`7.50` 格納」と「20 桁→throw」を両立させており、R-2 判定 3 により同時に成立しない。Round 2 で 2-7 について同じ欠陥を直したのに 5-7 へ適用し忘れた。**AC-35 を関連 AC に持つ唯一の行**であるため、矛盾を避けると AC-35 が全観点で無検証になる | A |
| R3-C2 | **4-10（AC-62 の唯一の担保）の検出力が実測 40%。**設計書がシーケンス図で規定する「まとめて解決」（1 文の複数行 INSERT）の違反実装では、**40P01 の発生率が 2.5%／回＝20 回で約 40%**。1 文にまとめると最初の行で直列化されて ABBA が組み上がらないため。名前ごとに 1 文ずつ INSERT する形なら 85〜95%／回だが、**20 回で 16.3〜19.3 秒**かかり `testTimeout: 20_000` に張り付く | C |

### Major（重複統合後）

| ID | 見出し | レーン |
|---|---|---|
| R3-M1 | 5-11 が `task_id` の対応付けを見ないため、**有効行のフィルタでインデックスがずれて工数を別タスクに書く実装**が期待結果（件数 2・集合 `{7.50, 2.68}`・合計 `10.18`）を満たして PASS する。2-8 は全行有効のためこの状況を再現せず、捕捉する行が存在しない | A・B |
| R3-M2 | **AC-72 が `deleteReportFn` について全行で無検証。**4-5 の①②③はすべて `saveReportFn` 対象。delete だけセッションレベル `pg_advisory_lock` を使う実装は 4-3・4-4 を通過し、**その日付の保存・削除が接続再利用まで恒久ブロックする**。レーンB が delete 側も 4-5③ と同一手法で落とせることを実測 | A・B |
| R3-M3 | 規定 7 の T 計測ペイロードが未規定（空の日報だと 1ms でマスタ解決を含むパスの代表値にならない）。`beforeAll` の書き込みは規定 1（接頭辞）・規定 3（後始末）の射程外で、**マスタが各 1 件残留する**ことを実測 | C |
| R3-M4 | 規定 4（id 採取による後始末）が R2-M3 を解消していない。`(名称未設定)` / `[object Object]` は `(NULL, NULL)` の**グローバル共有行**であり、採取されるのは既存行。削除すると **23503 で `afterEach` が throw** するか、`time_entries` も消して**開発者の実データを破壊**する | C |
| R3-M5 | **SQLSTATE の取得経路が未規定。**Drizzle 経由の例外は `DrizzleQueryError` で `e.code` は `undefined`、実値は `e.cause.code`（実測）。4-10 を `e.code !== '40P01'` と書くと**あらゆるエラーで真＝トートロジー**、2-1〜2-3 を `e.code === '23505'` と書くと**正しい実装で必ず FAIL** | C |
| R3-M6 | 4-10 の Given に**実績h の指定が無い**。実績h が無効だと AC-27 によりマスタが 1 件も作られず競合が発生しないため、違反実装でも 20/20 PASS する | B |
| R3-M7 | 4-10 は取引先層しか張らない。**取引先を昇順にし、タスクだけ走査順で解決する実装**は、取引先が未作成だと 0/20（PASS）だが、**取引先が既存の通常運用では 19/20 でデッドロックする**（実測） | B |
| R3-M8 | 5-10 が同一日付で `saveReportFn`（行を作る）と `deleteReportFn`（AC-89 のメッセージ判定＝行が無いことが前提）を併用し、**呼び出し順の規定が無い**。save を先に呼ぶと正しい実装が FAIL する。TZ 3 周回でも 2 周目以降が壊れる | A・B |
| R3-M9 | E2E-5 で「主判定」と指定した①（2 回目クリック直前に成功文言が残っている）に**識別力が無い**（違反実装でも正実装でも同じ）。実際に落とすのは②。かつ①と③が矛盾し、窓を外すと①が正しい実装を FAIL させる | B |
| R3-M10 | E2E-7 が設計書の「保存操作から 3 秒後に消えている」を**「観測から 5 秒以内」に緩めている**。逸脱の記録も承認も無く、自動消去を 4000ms に書いた実装（AC-88 違反）が通る。E2E-6・E2E-2 の逸脱は承認・記録で処理したのに扱いが非対称 | B |
| R3-M11 | 司令塔が Round 2 で設計書 T-2 に追記した「AC-89 が孤児経路を塞ぐ」が、設計書内の 4 箇所（:678・:709・:711・:623）および申し送り制約 #8 と**正面から矛盾**する。同一 AC について両立しない 2 つの因果モデルが同居している | B |

### レーンが PASS と判定した項目（実測の裏付け）

| 項目 | 裏付け |
|---|---|
| 4-4 の新判定 | レーンC が 4 実装を実走。正実装 PASS×3／3 種の違反すべて FAIL×3。**決定的に識別する** |
| 4-5③（実行中の `pg_locks` 観測） | レーンB・C とも実測。正実装＝`granted=true` 1 行／TX 外 1 文＝0 行／セッションレベル＝完了後も 1 行。**3 実装を識別**。照会式は追加キャストなしで動作 |
| 日付割当の健全性 | 66 日付・重複 0・全件キャスト成功。`0004-02-29` / `0050-03-01` は `date` 列への INSERT と読み戻しも成功 |
| 5-11 の期待値と AC-03 の一致 | `'7.5'`→`7.50`、`'2.675'`→`2.68`、合計 `10.18`（実測）。AC-03 と完全一致 |
| 6-2 の新判定 | 実ファイルに適用。4 定義に正しく分割され、`deleteReportFn` から `requireSession` を外す／別ミドルウェアに差し替える／`saveReportFn` から外す、の 3 変異をいずれも該当関数だけ FAIL として捕捉。**ファイル全体の `toContain` は同じ退行を素通しする**ことも確認 |
| 4-10 が正しい実装で false FAIL しないこと | `fixed` 実装で 40P01 = **0 / 340 試行**、所要 0.27〜1.2s |
| X = max(10T, 300ms) の下限としての妥当性 | 4-2・4-4・4-9 で正実装は 300ms 後も未解決、非ブロック違反実装は 14〜17ms で完走 |
| 規定 8（1-7 の冪等 DDL） | 2 周連続実行で両周成功 |
| 1-2 の接頭辞スコープ判定 | `JOIN tasks ON ... WHERE t.title LIKE 'IT0102-%'` で期待どおり 1/1/1/2 |

### 収束状況

| Round | Critical | Major |
|---|---|---|
| 1 | 2 | 17 |
| 2 | 1 | 11 |
| 3 | **2** | **9** |

Major は減り続けているが、**Critical は 3 周とも司令塔が直前の修正で作ったもの**である。R3-C2（AC-62 の検証手段）は R1-M2 → R2-M4 → R3-C2 と 3 周にわたり手段を変えて失敗しており、人間の判断が必要。

---

## Round 4（差分レビュー）

- レビュー日：2026-08-15
- 対象：Round 3 findings を反映したコミット `11f15b2` の差分
- 総合判定：**FAIL**（Critical 1・Major 6・Minor 19）

### Round 3 findings の解消状況

解消 9・部分的 4。R3-C1（5-7）・R3-M1（5-11 の対応付け）・R3-M3（T 計測）・R3-M5（SQLSTATE）・R3-M8（5-10 の順序）・R3-M9（E2E-5）・R3-M10（E2E-7）はいずれも**実測で解消を確認**。R3-C2（AC-62）は人間の判断で無検証として決着したが観点表内に取り残しあり。R3-M2（AC-72 の delete 側）・R3-M4（5-4/5-6 の後始末）・R3-M11（設計書の因果モデル）は部分的。

### Critical

| ID | 見出し | レーン |
|---|---|---|
| R4-C1 | **観点 5-6 の後始末が正しい実装でも決定的に 23503 で FAIL する。**`Task.label` / `Task.name` にオブジェクトを入れると `projects.name='[object Object]'`（親は接頭辞付き取引先）と `tasks.title='[object Object]'`（`project_id`・`client_id` とも非 NULL）が作られる。この task は規定 4（`(NULL,NULL)` 限定）にも規定 3（接頭辞一致）にも掛からず、残った task が `projects` を、残った project が `clients` を参照するため削除が 2 段連続で 23503 になる。**後始末が中断するのでマスタが残留し 2 周目以降も毎回 FAIL する（自己回復しない）** | C |

### Major（重複統合後）

| ID | 見出し | レーン |
|---|---|---|
| R4-M1 | **司令塔が Round 3 で設計書に書いた因果モデルが事実として誤り。**「孤児の防止を担うのは手順 2」は **AC-55 の射程（対象日に行が存在しない状態）でのみ成立**する。行が既に存在する場合、手順 2 があってもアドバイザリロックを外すと孤児が発生することをレーンB が実測（`useLock=false` → `daily_reports=0` / `time_entries=1`）。Round 2 の「60 試行で孤児 0 回」を射程外へ不当に一般化した | B |
| R4-M2 | AC-55 の**定義本体（設計書:169）を直していない**ため、設計書自身が「手順 2 により孤児は構造的に発生しない」と述べた結果、AC-55 の文言が全実装に対して真になるトートロジーと化した。4-4 が実際に検証する性質（直列化）は AC-55 の文言のどこにも書かれていない | A |
| R4-M3 | 設計書 :623 の書き換えが「`time_entries` だけが残るデッドロック（40P01）」という**成立しない障害モード**を作り、同節から AC-55 の唯一の引用が消えた | B |
| R4-M4 | 4-10 を対象外にしたのに、観点表 :184（1.6 節）と :153（4-7 行・優先度「高」）が**「AC-62 は 4-10 で担保する」のまま**。上から読むと担保済みに見え、AC×テスト対応マトリクスに偽の ✅ が立つ | A・B・C |
| R4-M5 | 規定 4 を書き換えたのに **5-4 の行が旧方式（`tasks.id` を採取して削除）を名指しで指示したまま**。行の指示のほうが具体的なため、R3-M4 が実測で断じた 23503／実データ破壊が再現する | A・B |
| R4-M6 | **4-5④ に陽性対照が無い。**①の陽性対照は `2000-02-22`（キー 52）に対するもので、④ の対象日（`2000-02-25`・キー 55）を守らない。誤キーで照会するとセッションレベル実装でも 0 行＝**vacuous PASS**（実測）。⑤ は Given に「対象日に日報が存在すること」が無く、正しい実装が false FAIL する | A・B・C |
| R4-M7 | E2E-5 の①を「判定ではない」に降格した結果、**窓を外した実行で②が false PASS** する。`T2 > t0 + 2000` では先行タイマーが既に満了しており、取り消さない違反実装でもエラー表示が 3 秒残る。窓外れを検出できる唯一の手段（①）を判定から外したため、①②③のいずれも FAIL にできない。さらに「無効として再実行」に Playwright 上の実装手段が無い | A |
| R4-M8 | **AC-79・AC-80・AC-81・AC-82・AC-86・AC-87 の 6 件**が観点表のどこにも現れず、委譲先の記録も無い（:186 は 4 件しか名指ししていない）。とくに **AC-81（テンプレ保存の失敗はエラー扱いにしない）と AC-82（自動消去の発火が副作用を出さない）は観測経路がゼロ**で、2 秒周期で自己再スケジュールする実装が全テストを PASS する | A |

### レーンが PASS と判定した項目（実測の裏付け）

| 項目 | 裏付け |
|---|---|
| **実行時間とタイムアウト余裕** | 1.4 節を観点表どおりに組んで実走：合計 **2.24 秒**、最長の単一 `it()` = **415ms**（4-2 326ms／4-3 312ms／4-4 312ms／4-5③ 410ms／4-5⑤ 411ms／4-9 415ms）。非並行観点は 0.43 秒。**1 章全体で約 2.7 秒**。4-10（Round 3 実測で違反実装時 16.3〜19.3 秒）が外れたことで `testTimeout: 20_000` の崖が消え、余裕は 48 倍になった |
| 4-5④⑤ の識別力 | 3 実装で実走。④ `xact`=0 行／`session`=**1 行 granted**／`outside`=0 行。⑤ `xact`=**1 行 granted**／`session`=1 行／`outside`=**0 行**。④＝セッションレベル、⑤＝TX 外 1 文をそれぞれ決定的に落とす |
| 規定 9 の SQLSTATE 取得 | drizzle-orm 0.45.2 の 5 経路すべてで `e.code=undefined`・`e.cause.code=<正しい SQLSTATE>`。postgres-js 直は `e.code`。`(e.cause?.code ?? e.code)` が全経路で正値 |
| 規定 7 の T 計測 | `T=25.6ms`（コールド）／暖機後 3.2〜3.8ms。後始末後の `IT0000-` 残存 0。X は 300ms 下限が支配し実保存（5.6〜13.1ms）に対し 20 倍以上の余裕 |
| 規定 4 の新方式（`(NULL,NULL)` タスク行） | 開発 DB 相当（既存 `(名称未設定)` 行＋それを参照する実績）で 5-4 を実走 → 既存行を再利用・例外なし・開発者のデータ保持。**旧規定は同条件で 23503 で FAIL することを再現**（対比が成立） |
| 5-11 の期待集合 | R-2 の判定順を実装して検算：①=(R1,7.5)(R2,2.675)／②=(**R2**,7.5)(**R4**,2.675)。観点表の 2 組は**両方とも正しい**。インデックスずれ実装は②で不一致、打ち切り実装は②で 0 件 |
| 5-7 の 3 分割 | ①`7.5h`→`7.50`・throw なし／②`0x10`→0 件・throw なし／③20 桁→throw。3 値が同時成立する |
| 5-1② | 両ペイロードとも実行時は throw なし・0 件。`projects: []` は `data.projects[0].tasks[0]` 実装を TypeError で落とす |
| 日付割当 | 69 日付・重複 0・`::date` キャスト失敗 0。4-10 削除で空いた 2 日を 4-5④⑤ が、新規 3 日が衝突なく収まる |
| 5-10 の TZ 3 周回の実効性 | `TZ=America/New_York` で `new Date('2028-02-29').getDate()===28` → `Date` 往復比較実装を落とせる |

### 収束状況

| Round | Critical | Major |
|---|---|---|
| 1 | 2 | 17 |
| 2 | 1 | 11 |
| 3 | 2 | 9 |
| 4 | **1** | **6** |

**4 周連続で「直前の周の修正が新しい欠陥を生む」構図が続いている。**とくに AC-72 の delete 側（R1-M13 → R2-M9 → R3-M2 → R4-M6）と AC-65 / E2E-5（R1-M15 → R3-M9 → R4-M7）は 3 周以上の持ち越しであり、`flow-review-policy` 4.3-4（振動の検知）に該当する。

**R4-M1・R4-M2・R4-M3 は承認済み設計書の変更を要するため人間へエスカレーションする**（4.3-2）。
