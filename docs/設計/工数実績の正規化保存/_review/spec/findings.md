# 設計書 敵対的レビュー結果

- 対象: `docs/設計/工数実績の正規化保存/工数実績の正規化保存_設計書.md`
- 方式: 敵対的較正・3レーン並列（A: 仕様の確からしさ / B: 網羅とスコープ / C: 異常系）

---

# Round 1

- レビュー日: 2026-08-13
- 総合判定: **FAIL**

## サマリー

| Severity | 件数 |
| -------- | ---- |
| Critical | 3 |
| Major | 15 |
| Minor | 20 |

レーン別の生件数は A: Major 10 / Minor 12、B: Critical 1 / Major 6 / Minor 7、C: Critical 2 / Major 5 / Minor 6。重複を統合した結果が上表（件数の水増しをしないためのマージ後の値）。

## Findings

### Critical

#### F-001: 0 < 実績h < 0.005 の行が丸めで 0.00 になり、`chk_hours_positive` 違反で保存全体が失敗する
- **観点 / レーン**: 境界値・DB制約との衝突（レーンC）
- **該当箇所**: 「R-2 対象行の抽出とスキップ条件」判定2／「R-3 実績h の丸め」／「例外系 E-4」／AC-01・AC-02。制約実体は `apps/web/src/server/schema/tasks.ts:109`
- **問題**: R-2 判定2 は「変換結果が 0 以下」のみをスキップ対象とするため、`0.001` / `0.004` / `0.0049` は「対象行」として通過する。R-3 でこれを小数第2位に丸めると `0.00` になり、`time_entries.hours` へ `0.00` を INSERT することになる。`chk_hours_positive`（`hours > 0`）はこれを拒否する。仮にアプリ側で丸めなくても、`hours` は `numeric(4,2)` であり PostgreSQL は列型へキャストしてから CHECK を評価するため、`0.001` を渡しても同じく `0.00 > 0` で違反する（アプリ実装では回避不能）。設計書 E-4 の「本設計では 0 以下の行を事前にスキップするため到達しない想定」は**事実として誤り**である。
- **影響**: 実績h に `0.001` のような微小値を 1 行でも入力すると、T-1 のトランザクション全体がロールバックし、**日報そのものが保存できなくなる**。利用者には `保存エラー: <PostgreSQL の制約違反メッセージ>` という原因の分からない文言だけが 2 秒表示される。従来は `raw_data` に何でも保存できていたため、これは本変更が持ち込む新規の保存不能パスである。さらに AC-01 と DB 制約が直接矛盾しており、Phase 5 のテストがこの矛盾をそのまま実装不能な期待値として固定するおそれがある。
- **失敗シナリオ**: 実績h に `0.004` を入力 → R-2 判定1(数値)・判定2(>0)・判定3(≤24) をすべて通過 → R-3 で `0.00` → INSERT → `chk_hours_positive` 違反 → トランザクション全体ロールバック → 日報が 1 行も保存されない。
- **推奨対応**: R-2 のスキップ条件を「**丸め後の値が 0 以下ならスキップ**」に改める（判定順を「変換 → 丸め → 0以下判定 → 24超判定」に再定義する）。あわせて E-4 の記述を修正し、AC-19 に「実績h が 0.004 のように丸めると 0.00 になる行は `time_entries` が作成されない」を追加して境界を受け入れ条件として固定する。
- **仕様決定の要否**: 不要

#### F-002: 対象行が 0 件のときの `time_entries` 一括 INSERT の挙動が未定義（空配列 INSERT で保存が失敗する）
- **観点 / レーン**: 空状態（レーンC）＋ 非機能要件と T-1 の食い違い（レーンA・F-A-015 を統合）
- **該当箇所**: 「非機能要件 > 性能」の「`time_entries` の INSERT は 1 回の複数行 INSERT にまとめる。」／「T-1 保存」手順3／E2E候補カタログ C-2
- **問題**: 設計は `time_entries` の INSERT を 1 回の複数行 INSERT にまとめることを実装制約として指示しているが、**対象行が 0 件になった場合に INSERT 自体を発行しない**という規定がない。Drizzle の `insert().values([])` は例外を投げる（`node_modules/drizzle-orm/pg-core/query-builders/insert.js:31-33` — `throw new Error("values() must be called with at least one value")`）。この例外はトランザクション内で発生するため、`daily_reports` の upsert ごとロールバックされる。加えて、非機能要件の「1 回の複数行 INSERT にまとめる」と T-1 手順3 の「対象行ごとに…`time_entries` を INSERT」は文面として食い違っており、どちらが正かが決まっていない（レーンA F-A-015）。
- **影響**: 対象行 0 件は例外的な入力ではなく**通常運用で頻発する状態**である。(a) `defaultDailyReport()` はタスク行 1 件・実績h 空で始まるため、朝に予定だけ入れて保存する運用が該当する。(b) 設計書自身の E2E 候補 C-2「実績h を空欄のままタスク行を残して保存する → **エラーにならず保存され**、その行は工数として記録されない」と正面から矛盾する。(c) タスク行をすべて削除して保存し直す運用（AC-07 の極端形）も該当する。つまり、この規定不足のまま実装されると**日報の初期保存が壊れる**。
- **失敗シナリオ**: 実績h を 1 つも入力せずに「日報保存」を押す → 対象行 0 件 → 空配列で複数行 INSERT → 例外 → トランザクションロールバック → `保存エラー: values() must be called with at least one value` が表示され、日報が保存されない。
- **推奨対応**: T-1 の手順3に「**対象行が 0 件の場合は `time_entries` の INSERT を発行しない**（削除のみ行い、日報の保存は成功させる）」を明記する。非機能要件と T-1 の文面を「対象行が 1 件以上ある場合、1 回の複数行 INSERT にまとめる」に統一する。あわせて「実績h が有効な行が 1 件も無い日報を保存しても成功し、当該日付の `time_entries` は 0 件になる」を AC として追加する（現状の AC 群にこのケースが 1 件も無い）。
- **仕様決定の要否**: 不要（C-2 の期待結果として既に「エラーにならず保存される」と要件側の答えが出ている）

#### F-003: 「削除範囲が当該日付に限定されること」を検証する受け入れ条件が存在しない
- **観点 / レーン**: 要件の網羅性（レーンB）
- **該当箇所**: 受け入れ条件 AC-01〜AC-27 全体／「トランザクション設計 T-1」（`DELETE FROM time_entries WHERE date = <対象日>`）／「テストレベル判定 > 影響度トリガー > 不可逆操作」の理由欄
- **問題**: 設計書は不可逆操作トリガー該当の理由として「**削除範囲の指定を誤ると他日付の実績を消す**」と自ら明記している。にもかかわらず、27 件の AC のいずれも「**他日付の `time_entries` が影響を受けないこと**」を規定していない。AC-03/06/07/08 はすべて「当該日付の」件数・合計のみを対象にしており、AC-09 も「当該日付の `time_entries` も削除される」までしか言っていない。`deleteReportFn` 側についても同様に他日付の残存を規定した AC がない。
- **影響**: 設計書は「実装承認ゲートをスキップする設定のため、Phase 5 のテスト観点はこの条件を**唯一の根拠**として起案される」と宣言している。したがって AC に無い性質はテスト化されない。結果として、`WHERE date = ...` を書き落とした実装（`DELETE FROM time_entries` 全件削除）や、範囲を月単位で誤指定した実装が、**AC-01〜AC-27 の全件を PASS したまま通過する**。AC-03 も AC-06 も全件削除実装で成立してしまう。過去の全工数実績が失われる不可逆なデータ消失であり、`time_entries` は「実績の唯一のマスター」であるため復旧手段がない（バックフィルは非ゴール）。
- **失敗シナリオ**: 4/1〜4/28 の日報を保存済み → 4/28 の日報を再保存 → 実装の DELETE に日付条件が欠落 → 4/1〜4/27 の `time_entries` が全消失 → 4/28 分だけ再挿入されるため AC-01〜AC-09 はすべて PASS → 誰も気づかない。読み取り側は非ゴールで `time_entries` を表示する画面が存在しないため、消失は SQL を直接叩くまで発覚しない。
- **推奨対応**: 以下の AC を追加する。①別日付の `time_entries` が存在する状態で対象日の日報を保存しても、別日付の件数・合計は変化しない。②別日付の `time_entries` が存在する状態で `deleteReportFn` を実行しても、別日付の `time_entries` および `daily_reports` は残る。あわせてテストレベル判定の「不可逆操作」欄が挙げた理由と AC の対応関係を明示し、結合(DB込み)テストの必須観点に組み込む。
- **仕様決定の要否**: 不要

---

### Major

#### F-004: R-1 の空白圧縮ルールが本文内で矛盾し、全角スペース単独の扱いが一意に決まらない
- **観点 / レーン**: 仕様の曖昧性（レーンA）
- **該当箇所**: 「R-1: 名前の正規化」手順2と直後の補足
- **問題**: 手順2は「**連続する**空白を半角スペース 1 つに圧縮する」と書き、補足は「全角スペースも「空白」として扱い**半角スペース 1 つに畳む**」と書いている。実装者は2通りに読める。(a) 長さ2以上の連続だけを畳む＝`"A　社"`（単独の全角スペース）はそのまま残る、(b) 長さ1以上のすべての空白列を半角スペース1つに置換＝`"A　社"` → `"A 社"`。前者では `"A　社"` と `"A 社"` が別の `clients` レコードになり、後者では同一になる。
- **影響**: 名前正規化はマスタ同一性の唯一の判定基準であり、実装が (a) を採れば IME で全角スペースを打った日と半角で打った日で取引先マスタが二重化する。ユニーク制約（S-1）は正規化後の文字列が異なる以上これを防げず、`time_entries` の集計が取引先ごとに分裂する。本機能のゴールが達成できない。
- **失敗シナリオ**: 1日目に取引先「A　社」（全角スペース）、2日目に「A 社」（半角）と入力 → 実装 (a) では `clients` が2件になり、AC-13 のテスト（半角のみを例示）は PASS するため検出されない。
- **推奨対応**: 手順2を「1文字以上の連続する空白文字列を半角スペース1つに置換する（結果として単独の全角スペース・タブも半角スペースになる）」と明記し、AC-13 の例に全角スペースとタブのケースを追加する。
- **仕様決定の要否**: 不要

#### F-005: R-1 の「空白文字」の集合が未定義で、`.trim()` 実装と列挙正規表現実装で結果が割れる
- **観点 / レーン**: 仕様の曖昧性（レーンA）
- **該当箇所**: 「R-1: 名前の正規化」（「半角スペース・全角スペース・タブ・改行」の列挙）
- **問題**: 列挙は4種だが、JS の `String.prototype.trim()` / `\s` はこれより広い集合（NBSP `U+00A0`、`\r`、垂直タブ `\v`、フォームフィード `\f`、`U+2000`〜`U+200A`、`U+FEFF` 等）を空白として扱う。実装者Aが `name.trim().replace(/\s+/g, ' ')` と書き、実装者Bが列挙どおり `/[ 　\t\n]+/` と書くと、`"A社 "`（Excel/Web からのコピペで頻出）に対して A は `"A社"`、B は `"A社 "` を返す。「改行」が LF のみか CRLF を含むかも未定義。
- **影響**: F-004 と同じくマスタの二重化を招く。さらに、この差は Phase 5 のテストでも露見しない — テスト作成者も同じ曖昧な記述から出発するため、実装と同じ空白集合を仮定したテストになりトートロジー化する。
- **失敗シナリオ**: 取引先名を業務システムからコピペして末尾に NBSP が混入 → 実装Bでは「A社」と別レコードになり、以後の集計が分裂する。ユニーク制約は文字列が異なるため発火しない。
- **推奨対応**: 「JS の `String.prototype.trim()` が空白とみなす文字集合＋`U+3000`」のように正規表現・API レベルで一意に規定する（例：`s.replace(/[\s　]+/gu, ' ').trim()` を仕様として明記）。
- **仕様決定の要否**: 要

#### F-006: 実績h の数値解釈が `Number()` の実挙動と乖離し、かつ既存 UI の集計（`parseFloat`）とも食い違う
- **観点 / レーン**: 仕様の曖昧性（レーンA F-A-003）＋ 異常系・型変換（レーンC F-C-004）＋ 分類の食い違い（レーンA F-A-011）を統合
- **該当箇所**: 「R-2 対象行の抽出とスキップ条件」判定1・判定2および直後の補足／「データ項目マッピング表」No.4／AC-03／AC-19
- **問題（レーンA）**: 判定1は「`Number()` で変換できない（**数値以外の文字列を含む**）」という2つの基準を等価であるかのように並記しているが、両者は等価でない。`Number('1e2')` = **100** → 判定3に落ち保存全体がエラー。`Number('0x10')` = **16** → 判定4に落ち16時間の実績として記録される。`Number('Infinity')` = Infinity → 保存全体エラー。`Number('１.５')`（全角数字）= NaN → 無言でスキップ。`Number('1,5')` = NaN → スキップ。どの基準を実装の正とするかで結果が3通りに分かれる。さらに判定1は空文字・空白のみを「変換できない」に分類する一方、直後の補足は「`Number('')` は `0` を返すため判定2でスキップ」と書いており分類が食い違う（F-A-011）。
- **問題（レーンC）**: 本設計は `Number()` を指定しているが、**既存アプリの実績h 集計はすべて `parseFloat` である**（`ProjectBlock.tsx:30`、`routes/index.tsx:142`、`lib/report-generator.ts:11-12,38,40`）。両者は末尾に非数字を含む入力で結果が食い違う（`parseFloat('7.5h') === 7.5` / `Number('7.5h') === NaN`、`parseFloat('3時間') === 3` / `Number('3時間') === NaN`）。設計書にはこの差分への言及が一切ない。
- **影響**: 「16時間」の誤記録は集計を直接汚染する。全角数字の無言スキップは、実績h 入力欄が `type` 指定のないテキスト入力で IME が有効なため現実に起こりうるが、利用者へのフィードバックが一切ない。`'1e2'` の保存全体エラーは打ち間違いで日報そのものが保存できなくなるクリティカルパス障害である。`parseFloat` との乖離では、利用者は画面ヘッダの「合計: … → 7.5h」や日報テキスト出力でその工数が計上されているのを見ているのに `time_entries` には 1 件も作られない — **無警告のサイレントな工数欠落**であり、正規化データが「集計基盤」として信頼できなくなるという本機能の目的そのものを損なう。また AC-03 は「日報の実績h の合計」の定義が `Number()` 基準か `parseFloat` 基準（＝利用者が画面で見ている値）かで結論が変わるため、受け入れ条件として一意に検証できない。
- **失敗シナリオ**: 実績h に `7.5h` と入力 → 画面合計は 7.5h、日報テキストにも 7.5h と出力 → 保存 → `time_entries` は 0 件 → 月次集計で 7.5 時間が消える。エラーも警告も出ない。／実績h に全角で「１．５」と入力 → 「保存しました ✓」が出るがその行の 1.5h は残らない。
- **推奨対応**: 判定1を機械的基準1本に統一する（例：`/^\d+(\.\d+)?$/` にマッチしない文字列はスキップ、と規定して `1e2`/`0x10`/`Infinity` を明示的にスキップ側へ寄せる）。`Number()` と `parseFloat` のどちらを正とするかを明記し、既存 UI 側との差分を「許容する／揃える」いずれかで決める。AC-03 の「日報の実績h の合計」を「R-2 で対象行と判定された行の丸め後の合計」と定義し直す。「数値として解釈できないのに空でない実績h」を黙って捨てるか検証エラーとして返すかを決める。
- **仕様決定の要否**: 要

#### F-007: 丸め仕様が実装依存で、丸め境界（x.xx5）と 24 判定のタイミングが未定義
- **観点 / レーン**: 仕様の曖昧性（レーンA F-A-004）＋ 境界値（レーンC F-C-005・F-A-014）を統合
- **該当箇所**: 「R-3 実績h の丸め」／「R-2」判定3／AC-02／AC-03／AC-21
- **問題**: (1) 「小数第 3 位以下を四捨五入」としか書かれておらず、丸めの実行系が未定義。実績h は文字列として入力され `Number()` を経由すると2進浮動小数の誤差が入るため、手段ごとに結果が食い違う：`Math.round(2.675 * 100) / 100` = **2.67**（`2.675*100` が `267.49999999999994` になるため）、`(2.675).toFixed(2)` = **"2.67"**、文字列 `'2.675'` をそのまま `numeric(4,2)` に渡す = **2.68**（PostgreSQL の numeric は10進で half-away-from-zero）、人間が「四捨五入」と聞いて期待する値 = **2.68**。つまり設計書の日本語の規定（2.68）を、最も素直な JS 実装（2.67）が満たさない。同様に `Math.round(1.005 * 100) / 100 === 1`、`(1.005).toFixed(2) === '1.00'`。(2) R-2 判定3 の「24 を超える」が、`Number()` の結果（丸め前）に対する判定なのか、R-3 で丸めた後の値に対する判定なのかが明記されていない。`24.004` は丸め前判定ならエラー、丸め後判定なら `24.00` として正常保存となり、結論が正反対になる。
- **影響**: AC-02・AC-03・AC-21 の期待値が一意に決まらない。Phase 5 のテスト作成者は自然な解釈で `2.675 → 2.68` を期待値に書き、Phase 8 の実装者は `Math.round` を書く可能性が高く、Green にならずに Phase 8↔5 の往復が発生する。本フローは Phase 7 でテストが人間承認・凍結されるため、凍結後にこの衝突が出ると承認済み成果物の変更＝人間エスカレーションになる。
- **失敗シナリオ**: テスト側が「`1.005` → `1.01`」を期待し、実装が `Math.round(x*100)/100` を使って `1.00` を返す → Phase 8 でテストを通すために実装ではなくテストを直す圧力がかかる（Phase 8 はテスト修正禁止のため収束ループが空転する）。
- **推奨対応**: R-3 に丸めのアルゴリズムを一意に規定する。R-2 判定3 には丸め前・丸め後のどちらで判定するかを明記し、`24.004` の期待挙動を AC に追加する。
- **仕様決定の要否**: 不要（レーンC 判定）／要（レーンA 判定）→ **司令塔の裁定: 不要**。丸めの実行系は業務判断ではなく設計者が決めてよい技術的選択であり、決めて書けば解消する（レーンC の判定を採用）。ただし決めた内容は Phase 3 の人間承認の対象に含める。

#### F-008: M-3 の「NULL も値として一致判定する」の実現方法が未定義で、素直な実装では adhoc タスクが永久に解決できない
- **観点 / レーン**: 仕様の曖昧性（レーンA）
- **該当箇所**: 「M-3: タスク（`tasks`）」／「解決の共通方針」
- **問題**: M-3 は検索キーを「`project_id`・`client_id`・`title` の 3 つ組（NULL も値として一致判定する）」とだけ書き、SQL レベルでどう実現するかを規定していない。SQL の `=` は NULL に対して常に UNKNOWN を返すため、Drizzle で素直に `and(eq(tasks.projectId, null), eq(tasks.clientId, null), eq(tasks.title, t))` と書くと、既存の adhoc タスクは**決してヒットしない**。設計書の共通方針は「見つからなければ `ON CONFLICT DO NOTHING` で挿入 → 再 SELECT」なので、挿入は S-3（`NULLS NOT DISTINCT`）に弾かれて 0 行、再 SELECT も同じ `= null` で 0 行となり、**`task_id` が確定できないまま `time_entries` の INSERT に到達して NOT NULL 違反 → 保存全体がロールバック**する。
- **影響**: AC-16（取引先・プロジェクトともに空）と AC-17（プロジェクトのみ空）は要件メモ決定5で明示された主要ケースであり、そのユーザーが日報を**一切保存できなくなる**。設計書だけを根拠に実装する Phase 8 の `impl-builder` は、この落とし穴を回避する根拠を設計書から得られない。
- **失敗シナリオ**: 取引先欄を空にしたまま実績h を入力して保存（E2E 候補 C-3 とまったく同じ操作）→ 2回目の保存以降 NOT NULL 違反で保存失敗。
- **推奨対応**: M-3 に「検索条件は `IS NOT DISTINCT FROM` を用いる（Drizzle では `sql` テンプレートで明示する）」と実現手段まで書く。あわせて `ON CONFLICT` のアービタが S-3 のインデックスに一致することも明記する。
- **仕様決定の要否**: 不要

#### F-009: 取引先が空の行では、異なるプロジェクトの同名タスクが S-3 により 1 タスクへ統合される
- **観点 / レーン**: 重複・衝突（レーンC F-C-007）＋ 仕様の曖昧性（レーンA F-A-006）を統合
- **該当箇所**: 「R-4 `tasks.type` の判定」（特に「取引先名が空の場合、プロジェクト名が入力されていてもプロジェクトマスタは作成しない」）／「S-3」／「R-5 タスク名の既定値」／AC-16
- **問題**: 取引先名が空の行は `client_id = NULL` / `project_id = NULL` の adhoc タスクになる。S-3 は `UNIQUE NULLS NOT DISTINCT (project_id, client_id, title)` であるため、**`title` が同じなら全期間・全プロジェクトにまたがって同一タスク 1 件に統合される**。日報の入力欄では取引先が空でもプロジェクト欄（`Task.label`）とタスク名は入力でき、「実装」「調査」「MTG」「定例」といった汎用的なタスク名は複数のプロジェクトで重複するのが常態である。R-4 は帰結として「プロジェクト名は正規化テーブルに残らない」とだけ書いているが、真の帰結は「取引先が空の行どうしは、プロジェクト名が異なっていてもタスク名さえ同じなら同一の集計単位に融合する」ことであり、これが設計書のどこにも書かれていない。S-3 の副作用の記述は「同一プロジェクト内に同名タスクを 2 件作れない」ことにしか触れていない。R-5 の既定値 `(名称未設定)` はこれをさらに悪化させ、タスク名も取引先も空の全行が**単一のタスクレコード**に集約される。
- **影響**: 本機能が掲げる「タスク単位の累計実績」が、無関係な作業の混合値になる。`time_entries` の総計は正しいまま**タスク単位の内訳だけが静かに誤る**ため、後続の集計機能を作った時点まで誤りに気づけない。融合したタスクの `time_entries` は後から分離不能で、既存データのバックフィルも非ゴールのため復旧手段がない。AC-12 はこの統合を「正しい挙動」として固定してしまうため、テストでも検出できない。
- **失敗シナリオ**: 取引先欄を空にしたまま、プロジェクト「請求書改修」タスク「調査」に 2h、プロジェクト「認証改修」タスク「調査」に 3h を入力して保存 → `tasks` は「調査」1件、`time_entries` は 2 件で合計 5h。SQL でプロジェクト別に分けることは永久にできない。
- **推奨対応**: 少なくとも設計書に本挙動を明示する。そのうえで (a) 取引先が空でプロジェクト名がある行の扱いを再検討する（「取引先未設定」相当の既定 client を作る／保存時に検証エラーとする／プロジェクト名をタスク名に連結して `title` を一意化する 等）、または (b) 非ゴールとして「取引先が空の行はタスク単位集計の対象外である」ことを受け入れ条件と非ゴールの双方に明記し、AC-16 に既知の帰結として書き足す。
- **仕様決定の要否**: 要

#### F-010: AC-22 のエラーメッセージ仕様が「例」しかなく、機械判定できない／メッセージが呼び出し元へ届く前提が未検証
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA）
- **該当箇所**: AC-22／「エラー処理」／E2E 候補 C-1
- **問題**: 3点ある。(1) AC-22 は「**原因が分かる**エラーメッセージ（対象のタスク名と入力値を含む）」という主観的表現で、規範的な文言仕様がない。「エラー処理」節にある例は「例：」と明示されており規範ではない。テスト作成者は期待文字列を自分で決める＝新たな仕様決定を強いられる。(2) 未定義のサブケースがある。24 超の行の**タスク名が空**のときに何を表示するか（`(名称未設定)` か、空文字か、行位置か）。R-5 の既定値は `tasks.title` へ適用される規定であってエラーメッセージへの適用は書かれていない。24 超の行が**複数**あるとき、最初の1件だけを報告するか全件列挙するかも未定義。(3) 「**呼び出し元**に返る」の指す先が不明。サーバー関数の直呼び（結合テスト）か、ブラウザ上の表示（E2E C-1）かで検証方法が変わる。さらに後者は「サーバーで `throw` した `Error.message` が TanStack Start の RPC 境界を越えてクライアントの `err.message` に届く」ことを前提にしているが、設計書が根拠として挙げる `lib/storage.ts` はクライアント側が `err.message` を読む実装を示すだけで、**サーバーのメッセージがそこに到達する保証は何も示していない**。本リポジトリには利用者向けメッセージを `throw` するサーバー関数の前例がなく（`apps/web/src/server` 配下の `throw new Error` は `env.ts` と `auth/config.ts` の起動時設定エラー2件のみ）、実挙動は未確認である。
- **影響**: Phase 5 で書かれる AC-22 のテストと C-1 由来の E2E が、実装と別の文言を仮定して固定される。Phase 7 で人間が承認・凍結した後に食い違いが判明すると、承認済み成果物の変更となり人間エスカレーションが必要になる。(3) の前提が崩れていた場合、AC-22 は設計どおりに実装しても達成不能（利用者には汎用エラーしか出ない）で、Phase 1 への差し戻しになる。
- **推奨対応**: エラーメッセージのフォーマットを規範として固定し、タスク名が空の場合・複数件該当の場合の挙動を明記する。AC-22 の「呼び出し元」をサーバー関数の戻り／画面表示のどちらかに特定する。RPC 境界でのメッセージ保持は Phase 3 までに実機で確認するか、確認できない場合は「メッセージ本文の到達」を AC から外し、結合テスト側で検証する形に落とす。
- **仕様決定の要否**: 要

#### F-011: 受け入れ条件の適用前提が明示されておらず、AC 同士が直接矛盾する
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA）
- **該当箇所**: AC-01 × AC-21、AC-16〜AC-18 × AC-19
- **問題**: 2組の矛盾がある。(1) AC-01 は「実績h が**数値として解釈でき 0 より大きい**タスク行 1 件につき `time_entries` が 1 件作成される」と条件を書いている。実績h = `25` の行はこの条件を満たすため、AC-01 のテストは `time_entries` が 1 件作られることを要求する。一方 AC-21 は同じ行に対して「保存全体が失敗し、`daily_reports` も `time_entries` も更新されない」を要求する。同一入力に対して2つの AC が正反対の結果を要求している。(2) AC-16／AC-17／AC-18 は「〜のタスク行は、…のタスクとして記録され、`time_entries` も作成される」と書くだけで、**実績h が有効であることを前提に含めていない**。実績h が空文字の行に対して AC-16 のテストを素直に書くと「タスクと `time_entries` が作成される」を期待することになり、AC-19 および R-2 の「スキップした行はマスタも作らない」と衝突する。
- **影響**: Phase 5 のテスト観点は受け入れ条件を唯一の根拠として起案されると設計書自身が宣言している。矛盾した AC からは矛盾したテストが生成され、Phase 6 のレビューで検出できても戻り先が Phase 1（設計書）となり、フロー設定の規定により人間エスカレーションが発生する。
- **失敗シナリオ**: `test-builder` が AC-01 から「実績h=25 の行 → `time_entries` 1件」、AC-21 から「実績h=25 → 保存失敗」の2本を起案し、どちらも Green にできない。
- **推奨対応**: AC-01 に「かつ 24 以下」の条件を追加する。AC-16〜18 の冒頭に「実績h が有効な（0 超 24 以下の数値として解釈できる）行のうち」という共通前提を明記する（AC-20 の書式に揃える）。
- **仕様決定の要否**: 不要

#### F-012: E2E の前提が事実誤認であり、E2E-1/E2E-2 の期待結果は現状の記述では検証できない
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA F-A-009）＋ テストレベル判定の妥当性（レーンB F-B-006）を統合
- **該当箇所**: 「E2E シナリオの洗い出し」E2E-1・E2E-2／「非ゴール」の「読み取り側の差し替え」
- **問題**: (1) **事実誤認**：「既存の `apps/web/e2e/daily-report.test.ts` に日報保存の E2E がすでに存在するため、その拡張として実装する」と書かれているが、実ファイル（全29行）に含まれるのは「日報入力画面が表示され、フォーム要素が存在する」「工数管理ページが読み込める」「勤怠管理ページが読み込める」の3件のみで、**保存ボタンを押すテストは1件も存在しない**。「拡張」ではなく新規作成であり、入力・保存・検証の全ステップを書き起こす必要がある。（司令塔が実ファイルを確認し、この指摘が正しいことを検証済み）(2) **検証手段の未定義**：E2E-1 の期待結果「DB に日報と `time_entries` が作成されている」、E2E-2 の「`time_entries` が二重に増えていない」は、いずれも画面上に現れない事実である（`time_entries` を表示する画面は存在せず、`MonthlyTimesheetTable` は `raw_data` の名前一致集計、`AttendanceTable` は日単位カラムのみ）。Playwright からどう検証するか（`apps/web/src/test/db-helpers.ts` 経由の直接 SQL か、別手段か）が規定されていないため、AC として機械的に判定できない。(3) **フェーズの矛盾（レーンA の主張）**：「Phase 10 ではその拡張として実装する」と書かれているが、判定結果表は E2E の確定方式を Phase 5→6→7 としており、Phase 10 で E2E を書けば人間承認ゲートを迂回しテスト先行にも反する。
- **影響**: Phase 5 の `test-builder` が「既存テストを拡張すればよい」と誤認して着手し、実在しない前提の上で作業する。検証手段が未定義のため E2E の期待値が「保存しました ✓ が出る」だけに退化し、本機能の中核（`time_entries` の正しい積み上げ・二重計上の不在）が E2E で一切保証されない。本機能は実装承認ゲートがスキップされており、Phase 10 の実行検証が最後の砦であるため、ここが空洞化する影響は大きい。
- **推奨対応**: 「既存 E2E がある」という記述を「保存フローの E2E は未整備であり新規に作成する」に訂正する。E2E から DB を検証する手段（`db-helpers` の利用可否・接続先 DB・後始末方針）を規定するか、期待結果を画面で観測可能な範囲に限定して `time_entries` の検証は結合(DB込み)に委ねると明記する。
- **仕様決定の要否**: 要（レーンA）／不要（レーンB）→ **司令塔の裁定: (1)(2) は不要**（事実の訂正と検証手段の選択で解消できる）。**(3) のフェーズ矛盾は指摘自体が誤り**：`docs/開発プロセス/テストレベル選定トリガー表.md` 6章は「結合DB込みは Phase 5 で test-builder がコード化。**E2E は Phase 5 で仕様凍結のみ行い、Phase 10 でコード化・実行**」と規定しており、設計書の記述はこれに従っている。両論を残すため本項は削除せず記録するが、(3) を根拠とする修正は行わない。

#### F-013: MC-4 — 未決事項 No.2 / No.3 が本文では既決値として書かれ、否決時の代替案が存在しない
- **観点 / レーン**: MC-4 未決事項の温存（レーンA）
- **該当箇所**: 未決事項表／T-1「削除範囲は「日付」のみで絞る」／R-2 判定3／AC-21・AC-22／エラー処理／E2E 候補 C-1
- **問題**: 要件メモの未決6件については5件が本文の規定として完結し、残り1件（既存データ衝突）が設計書未決 No.1 に引き継がれていることを確認した。問題は設計書側で新たに立てた未決3件のうち2件である。**No.2（日付単位削除の受容可否）**：未決の問いは「本設計はこのリスクを受け入れる前提でよいか」であるにもかかわらず、T-1・T-2 は日付単位の全削除を確定仕様として記述し、AC-06／AC-07／AC-08／AC-09 の4本がこの方式に全面依存している。人間が Phase 3 で「受け入れない」と判断した場合の代替案（`time_entries` に由来を示す列を持たせる、`daily_reports` との FK を張る等）が**1つも提示されていない**ため、否決＝設計書の全面再作成となる。**No.3（24 上限の妥当性）**：`24` は R-2 判定3、AC-21、AC-22、エラー処理節のメッセージ例、E2E 候補 C-1 の5箇所に確定値として現れるが、**そのいずれにも未決 No.3 への参照がない**。設計書だけを読む Phase 5 の `test-builder` は 24 を確定仕様として受け取り、テストに固定値として埋め込む。
- **影響**: Phase 3 の承認者が「本文が確定的に書かれている＝もう決まっている」と誤読し、未決を素通しで承認するリスクがある。逆に否決された場合、No.2 は Phase 1 全面差し戻し、No.3 は Phase 5 のテスト複数本の書き直しになる。lean 運用でも Phase 2 は Critical/Major ゼロまでの収束が必須であり、未決の温存は収束判定を歪める。
- **推奨対応**: T-1 の該当段落と R-2 判定3・AC-21・AC-22 に「（未決事項 No.2／No.3。Phase 3 の判断により変更されうる）」の注記を入れる。No.2 については、否決された場合の代替案と切替コストを1〜2案、判断材料として未決事項表に併記する。
- **仕様決定の要否**: 不要

#### F-014: 設計本文で決定した仕様のうち 6 件が受け入れ条件に写像されていない
- **観点 / レーン**: 要件の網羅性（レーンB）
- **該当箇所**: 「R-2 / R-4」「M-3」「T-2」「未決事項の写像表」／受け入れ条件 AC-01〜AC-27
- **問題**: 設計書自身が「Phase 5 のテスト観点は受け入れ条件を唯一の根拠として起案される」と定めているのに、本文で明確に決定した以下の仕様に対応する AC がない。(a) スキップした行は**マスタも作らない**（R-2 末尾／AC-19 は `time_entries` が作られないことのみ規定）。(b) 取引先名が空・プロジェクト名が入力済みの行では**プロジェクトマスタを作成しない**（R-4／AC-16/17/18 は 4 通りのうち 3 通りのみでこの 1 分岐だけ抜けている）。(c) 既存タスクは `status` が `completed` / `archived` でも再利用し**一切更新しない**（M-3）。(d) 日報削除時に**マスタは削除しない**（T-2／AC-09 は `time_entries` のみ）。(e) 同一日・同一タスクの複数行は**合算せず行ごとに** `time_entries` を作成する（写像表）。(f) 全角半角は別物として扱う／ただし全角スペースは畳む（R-1 補足／AC-14 は大文字小文字のみ）。
- **影響**: a〜f はいずれも「AC を全件満たすが設計に違反する実装」が成立する。(a) 全行のマスタを先に一括解決する実装は AC 全件 PASS だが未入力行の取引先を `clients` に量産する（マスタ管理画面は非ゴールなので手動削除もできない）。(c) 既存タスク再利用時に `status='active'` へ戻す実装が AC 全件 PASS で通る。(d) 孤立マスタを掃除する実装が AC 全件 PASS で通り、他日付の実績から参照されている `tasks` を消せば FK 違反または実績消失に至る。(f) `toLowerCase()` を挟む実装が AC-13 を PASS し AC-14 のみで落ちる／全角の同一視は誰も検出しない。テスト承認ゲート（Phase 7＝実施）で人間が確認する対象も AC 由来のテスト観点であるため、人間レビューでも拾えない。
- **推奨対応**: a〜f それぞれに AC を追加する。
- **仕様決定の要否**: 不要

#### F-015: タスク名寄せキーが要件メモ「決定事項 #9」から逸脱しているが、逸脱として明示されていない
- **観点 / レーン**: 要件の網羅性・決定事項の無断変更（レーンB）
- **該当箇所**: 「M-3」検索キー／「S-3」／要件メモ `docs/要件/工数実績の正規化保存_要件メモ.md` 決定事項 #9
- **問題**: 要件メモの決定事項 #9 は「同一プロジェクト内で同名なら同一タスクとして再利用する（**PJ が無い場合は `title` と `type` をキーにする**）」と確定している。設計書 M-3・S-3 はキーを `(project_id, client_id, title)` としており、PJ が無い場合のキーに **`client_id` が入り `type` が入らない**。両者は挙動が異なる：決定事項 #9 では「取引先A の adhoc タスク『定例会議』」と「取引先なしの adhoc タスク『定例会議』」が同一タスク（1件）になるのに対し、本設計 M-3 では `client_id` が異なるため別タスク（2件）になる。にもかかわらず、設計書の「未決事項の写像表」は要件メモの**未決事項**6 件の決定のみを列挙しており、**決定事項からの逸脱は 1 件も記載していない**。読み手は決定事項が全件そのまま反映されていると誤読する。
- **影響**: 要件メモを承認根拠として Phase 3 の人間レビューに臨む人間が、確定済みと思っている名寄せ方針が変わっていることに気づけない。取引先ごとに adhoc タスクが分かれると、「取引先横断の共通作業（定例会議・社内作業）」の累計工数が取引先数だけ分割され、本機能の目的である横断集計が意図と異なる粒度になる。
- **推奨対応**: 写像表を「要件メモの決定事項からの変更点」も含む形に拡張し、決定事項 #9 の変更と理由を明記する。Phase 3 の人間承認で明示的に確認する項目として未決事項表に上げる。対応する AC を追加する。
- **仕様決定の要否**: 要

#### F-016: 名前が 255 文字を超える入力に対する規定がなく、保存全体が失敗する（従来は保存できていた）
- **観点 / レーン**: データ量・文字列長／DB制約との衝突（レーンC F-C-003）＋ 要件の網羅性（レーンB F-B-004）＋ レーンA F-A-013 を統合
- **該当箇所**: 「データ項目マッピング表」No.1-3／「R-1 名前の正規化」／「エラー処理」。実体は `apps/web/src/server/schema/master.ts:16,38` と `tasks.ts:23`（いずれも `varchar(255)`）
- **問題**: 取引先名・プロジェクト名・タスク名は正規化後そのまま varchar(255) の列へ書き込まれるが、長さの検証・切り詰め規定が設計書のどこにもない。入力欄（`TaskRow.tsx` / `ProjectBlock.tsx`）には `maxLength` が無く、貼り付けで容易に 255 文字超が入る。R-1 の正規化は空白圧縮のみで長さを縮めない。マッピング表にも桁数欄がない。エラー処理表も「検証エラー＝実績h 24 超」しか列挙していない。
- **影響**: 255 文字超の名前が 1 つでもあると `value too long for type character varying(255)`（22001）が発生し、トランザクション全体がロールバックして**日報が保存できなくなる**。本変更前は `raw_data`（JSONB）に長さ無制限で保存できていたため、これは既存機能のデグレードであり、クリティカルパス業務（日報保存）の停止に直結する。しかも利用者に出るのは PostgreSQL の英語メッセージであり、「どの欄を削ればよいか」が分からない（設計書のエラー処理方針「利用者が入力を修正できる情報を含める」に反する）。
- **失敗シナリオ**: メールから会社名込みの長い件名をタスク名欄に貼り付け（300文字）→ 保存 → `保存エラー: value too long for type character varying(255)` → 当日の日報が一切保存されない。利用者はどの行が原因か分からない。
- **推奨対応**: R-1 に桁数ルールを追加する。(a) 256 文字以上を検証エラーとして実績h 24 超と同様にトランザクション前で落とし、対象欄と入力値を含む日本語メッセージを返す、または (b) 正規化後 255 文字で切り詰めて保存する（切り詰めると別名寄せになる副作用を明記）。あわせて対応する AC（境界 255／256）とデータ項目マッピング表への桁数欄追加を行う。
- **仕様決定の要否**: 要

#### F-017: 並行保存の扱いが未定義（デッドロック・分離レベル・再SELECT 空振り・同一日付の二重計上）
- **観点 / レーン**: 並行（レーンC F-C-006）＋ 非ゴールの明確性（レーンB F-B-005）＋ レーンA F-A-016 を統合
- **該当箇所**: 「マスタ解決ロジック仕様 > 解決の共通方針」／「T-1」／「T-3 原子性の担保」／「例外系 E-3」／「非ゴール」／「自己レビューチェック」
- **問題（レーンC）**: (1) **デッドロック**：異なる日付の保存が同時に走り、双方が同じ新規マスタ群を**異なる順序で**作ろうとすると（TX1 が 取引先A→取引先B、TX2 が B→A）、追加する UNIQUE 制約（S-1〜S-3）の未コミットタプル同士で相互待機となりデッドロック（40P01）になる。設計に処理順序の固定も再試行もなく、E-3 は「ロールバックして例外を伝播」とだけ書くため、利用者には原因不明の `保存エラー: deadlock detected` が出て日報が失われる。(2) **分離レベル依存の明記なし**：「挿入を無視して再検索し既存行の ID を採用する」は、文ごとに新しいスナップショットを取る READ COMMITTED でのみ成立する。REPEATABLE READ 以上では再SELECT が競合相手のコミット行を見られず 0 件を返す。T-3 は `db.transaction()` を使うとだけ述べ、分離レベルを既定から変えてはならないという前提を記していない。(3) **再SELECT が 0 件だった場合の挙動が未定義**：M-1〜M-3 は「見つかればその ID」「無ければ作成」しか定義しておらず、再SELECT 空振り時に何を返すかが無い。NULL のまま進むと `projects.client_id`（NOT NULL）や `time_entries.task_id`（NOT NULL）で NOT NULL 違反になる。
- **問題（レーンB）**: 設計書内で並行性に触れているのはマスタ解決の 1 文のみで、これは**マスタ行の重複作成**しか防がない。本設計の中核である「日付単位で `time_entries` を DELETE してから INSERT する」方式そのものの並行性は一切扱われていない。READ COMMITTED では、同一日付に対する 2 つのトランザクションが互いの未コミット行を DELETE 対象として認識しないため、両者がコミットすると当該日付の `time_entries` が二重に残る（AC-06「二重計上しない」が並行時に破れる）。それにもかかわらず**自己レビューチェックは「並行保存を扱った」と主張**し、非ゴール節にも限定がない。加えて「単一ユーザーだから同時保存は起きない」という前提も、`AUTH_ALLOWED_EMAILS` が複数メール・ドメイン指定を受け付け未設定時は無制限であるため担保されていない。
- **影響**: 実装者・レビュワー・Phase 3 の承認者が「並行性は検討済み」と誤認し、誰も設計判断を求めない。受け入れる場合であれ対処する場合であれ方針が文書化されていないため、Phase 8 の実装者が場当たりに決める。本アプリは複数タブでの操作が容易（オートセーブ付きの単一画面）であり、二重送信で発現しうる。
- **失敗シナリオ**: 8/12 の日報と 8/13 の日報を別タブでほぼ同時に保存。どちらも新規取引先 A・B を含み、ブロックの並び順が逆 → デッドロック検出で片方が中断 → その日の日報が保存されない。
- **推奨対応**: (a) T-1 に「マスタは正規化後の名前でソートした順に解決する」等、ロック取得順序を固定する規定を追加する。(b) T-3 に分離レベル（READ COMMITTED）の前提を明記する。(c) M-1〜M-3 に再SELECT 空振り時の終端条件を定義する。(d) 同一日付の同時保存について、非ゴールとして受け入れる（二重計上の可能性を明記）か、`SELECT ... FOR UPDATE` 等で直列化するかを決める。(e) 自己レビューチェックの「並行保存を扱った」を実際の記述内容と一致するよう修正する。
- **仕様決定の要否**: 要

#### F-018: E2E 候補カタログ C-5「保存済みの日報を削除する」は UI から実行不能
- **観点 / レーン**: テストレベル判定の妥当性（レーンB）
- **該当箇所**: 「E2E 候補カタログ」C-5／「業務フロー > 例外系」E-5／AC-09
- **問題**: `deleteReportFn` および `reportStorage.delete` を呼び出している UI コードは**リポジトリ内に一切存在しない**（参照は `server/functions/reports.ts:103`（定義）と `lib/storage.ts:6,69`（ラッパ）のみ。`routes/` からの呼び出しは 0 件で、画面に削除ボタンが無い）。E2E 候補カタログはトリガー表 5 章の定義上「画面操作シナリオを人間が手動実施する」ための一覧であり、C-5 は誰も実施できない。同様に例外系 E-5 も、実在しない画面操作を前提にした記述になっている。（司令塔が `grep` で確認し、この指摘が正しいことを検証済み）
- **影響**: 人間が C-5 を手動実施しようとして削除導線を探し、時間を失う／「未実装の機能がある」と誤解する。より本質的には、設計書が `deleteReportFn` の到達経路を確認せずに影響範囲を記述していることを示す。`deleteReportFn` は `createServerFn` で公開された HTTP エンドポイントなので直接呼び出しは可能であり AC-09 自体は有効だが、その事実が設計書のどこにも書かれておらず、Phase 5 のテスト作成者が「E2E で削除を操作する」と誤った前提を置きうる。
- **推奨対応**: C-5 をカタログから削除するか、「現状 UI に削除導線が無いため手動実施不可。導線追加時に有効化」と注記する。「影響を受ける実装済み機能」表の `deleteReportFn` 行に「現在 UI から呼ばれていないサーバー関数（エンドポイント直叩きのみ）」であることを明記し、E-5 の記述を画面前提から endpoint 前提に改める。AC-09 の検証は結合(DB込み)で行う旨も明示する。
- **仕様決定の要否**: 不要

---

### Minor（1行のみ）

- F-019: 参照行番号が実ファイルと系統的にずれている（`reports.ts:78`→63、`tasks.ts:80`→67、`tasks.ts:130`→109、`master.ts:26`→27、`TaskRow.tsx:22`→18、`ProjectBlock.tsx:41`→37、`storage.ts:53-60`→57、`MonthlyTimesheetTable.tsx:42`→38。司令塔が実ファイルで全件検証済み）（背景／スキーマ変更一覧／データ項目マッピング表／エラー処理）［レーンA F-A-020 ＋ レーンB F-B-008 を統合］
- F-020: Unicode 正規化（結合文字・NFC/NFD、異体字）の扱いが未記載で、「記号の正規化はしない」が結合文字にも及ぶのか不明（R-1）
- F-021: M-4 のキャッシュキーの生成規則（親 ID が NULL のときの表現、区切り文字と名前中の同一文字の衝突）が未定義（M-4）
- F-022: 利用者が実際に `(名称未設定)` というタスク名を入力した場合、タスク名未入力行と同一タスクに名寄せされる（R-5）
- F-023: 24 上限が行単位のみで1日合計に上限がなく、AC-03 の合計一致は日の実働時間と乖離した値でも成立する（R-2 判定3／AC-03）
- F-024: 要件メモの未決6件と、設計書の「本設計で決定した」5件＋設計書未決 No.1 との対応関係が明示されておらず、6件目の行き先が読み取れない（未決事項）
- F-025（**担当外・発見**）: `saveReportFn` の `inputValidator` が素通しのため、`projects` が配列でない入力に対して新設の走査処理が例外を投げ、従来は保存できていた入力が失敗に転じる可能性があるが、設計書に入力構造の検証規定がない（T-1／エラー処理）
- F-026: `Project.plannedHours`（`lib/types.ts:15`）がデータ項目マッピング表にも非ゴールにも一切現れず、扱いが不明（データ項目マッピング表）
- F-027: 「認可境界 非該当」の理由「単一ユーザー前提」は事実と不一致（`AUTH_ALLOWED_EMAILS` は複数メール・ドメイン指定可、未設定時は無制限：`server/auth/allowlist.ts:24-38`）。トリガー判定の結論自体は妥当だが理由の書き直しが必要（テストレベル判定 > 影響度トリガー）
- F-028: 実績h がちょうど `24` の行が正常に保存されることを示す AC がなく、境界が AC 上は未確定（AC-21／R-2 判定 3）
- F-029: 孤立マスタ（参照されなくなった `clients`/`projects`/`tasks`）のクリーンアップを行わない旨が本文 T-2 にのみあり、非ゴール節に列挙されていない（非ゴール／T-2）
- F-030: R-5 の既定値 `(名称未設定)` が AC-20 では「後述の既定値」としか書かれず、値そのものが AC で固定されていない（AC-20／R-5）
- F-031: AC-14 の例示「`"api改修"` と `"API改修"`」は取引先名の例として不自然（プロジェクト名の例に見える）で、読み手が対象テーブルを取り違えうる（AC-14）
- F-032: DB 制約違反・長さ超過時に PostgreSQL の生メッセージがそのまま `保存エラー: …` として利用者に表示され、原因が伝わらない／内部構造が露出する（エラー処理表 DB エラー行／E-3）
- F-033: 未決事項 No.3 で実績h の上限 24 を見直す判断材料として、`time_entries.hours` が `numeric(4,2)`＝実質上限 99.99 であり 100 以上は数値オーバーフロー（22003）で保存全体が失敗する事実が書かれていない（未決事項 No.3／R-2 判定3）
- F-034: タスク行数の上限規定・ステートメントタイムアウトへの言及がなく、性能記述が「10〜30 行程度」の想定のみで 200 行規模の挙動が未定義（非機能要件 > 性能）
- F-035: 既存の非正規化な名前（前後空白付き等）が手動投入されていた場合、検索キーが `normalizeName` 済みのため既存行に一致せず別レコードを作る（未決事項 No.1 はユニーク制約違反のみを想定し、この取りこぼしに触れていない）（マイグレーション手順／M-1）
- F-036: `clients.is_archived` / `projects.is_archived` が true のマスタも名前一致で再利用され、アーカイブ済みマスタに新規実績が積まれる挙動が未規定（M-1／M-2）
- F-037: S-3 は `parent_task_id` で親の異なるサブタスクにも適用され、同一プロジェクト内で同名サブタスクを作れなくなるが、副作用の記述がこのケースに触れていない（S-3 の副作用）
- F-038: R-2 判定1が空文字・空白のみを「変換できない」と分類する一方、直後の補足が「`Number('')` は `0` を返すため判定2でスキップ」と書いており分類が食い違う（R-2 判定表と補足）［F-006 の一部として対応］

---

## レビュー観点ごとの判定

| 観点 | レーン | 判定 | 裏付け |
| ---- | ---- | ---- | ---- |
| 仕様の曖昧性 | A | **FAIL** | R-1 の空白定義が本文内で矛盾、R-2 が `Number()` の実挙動と乖離、R-3 の丸め手段未定義、M-3 の NULL 一致判定手段未定義。いずれも複数の実装が成立し結果が割れる |
| 受け入れ条件の検証可能性 | A | **FAIL** | AC-01 と AC-21 が同一入力（25h 行）で相反、AC-16〜18 が実績h の前提を欠き AC-19 と衝突、AC-22 は規範的なメッセージ仕様を持たない |
| MC-4（未決事項の温存） | A | **FAIL** | 未決 No.2・No.3 が T-1・R-2・AC-21/22・エラーメッセージ・E2E C-1 で既決値として本文に埋め込まれている。要件メモ未決6件のうち5件の決定完結性は個別に検証済み |
| 要件の網羅性 | B | **FAIL** | 要件メモ「受け入れ条件の種」13 件は AC に全件写像されているが、設計本文で決定した仕様6件と、設計自身が最大リスクと明記した削除範囲に対応する AC が存在しない |
| 非ゴールの明確性 | B | **FAIL** | 要件メモ「スコープ外」7 項目は全件反映済み。ただし並行保存の扱いが本文・非ゴールのどちらにも無いまま「自己レビューチェック」で扱ったと記載されている |
| 設計書フォーマット準拠 | B | PASS | 【必須】9 項目がすべて実体を伴って存在。背景の「マスタ系テーブルはアプリから未参照」も `grep` で裏付け（DB テーブル参照は 0 件）。ER 図・例外系表・影響表も空疎な記述ではない |
| テストレベル判定の妥当性 | B | **FAIL** | 認可境界／PII／認証・セッションの「非該当」は結論として妥当と確認。一方 E2E シナリオ節に UI から実行・検証できないシナリオが含まれている |
| 境界値 | C | **FAIL** | R-3（丸め）と `chk_hours_positive` の組み合わせを 0.001 で反証。24 境界の判定タイミング（丸め前/後）も未定義 |
| 空状態 | C | **FAIL** | 「1 回の複数行 INSERT にまとめる」と、対象行 0 件になる入力（E2E候補 C-2・`defaultDailyReport()` の初期状態）の突き合わせで破綻 |
| 重複・衝突 | C | **FAIL** | 同一日報内の同名行は「行ごとに `time_entries`」＋ M-4 キャッシュで整合するが、取引先空＋プロジェクト有の行が S-3 で別プロジェクトのタスクと衝突する |
| 並行 | C | **FAIL** | 同一日付の二重保存は T-1 の upsert 順序で行ロックにより直列化され安全。一方、異なる日付の同時保存におけるデッドロックと再試行方針、再SELECT が依存する分離レベルが未定義 |
| DB制約との衝突 | C | **FAIL** | `chk_task_type_project` / `chk_task_type` / `chk_task_status` / `chk_task_priority` は R-4・M-3 の書き込み値と整合。`clients_code_idx` / `projects_code_idx` は NULL 同士を distinct 扱いするため `code=NULL` の複数作成と衝突しない。ただし `chk_hours_positive` と varchar(255) で破綻 |
| 削除の巻き込み | C | PASS | T-2 の削除順序は `daily_reports`↔`time_entries` 間に FK が無く、`tasks` を削除しないため `time_entries.task_id` の FK 違反も起きない。日付のみで絞る危険性は未決事項 No.2 として明示済み |
| データ量・文字列長 | C | **FAIL** | `clients.name` / `projects.name` / `tasks.title` が varchar(255) だが、入力欄に長さ制限が無く設計にも検証規定が無い。行数の上限規定も無い |

## 司令塔による裁定の記録

`flow-review-policy` 3.3 に基づき、レーン間で判定が割れた項目を記録する（統合と体裁整理のみ。指摘内容の要約・軟化・削除は行っていない）。

| 項目 | レーン間の相違 | 裁定 | 根拠 |
|---|---|---|---|
| F-007（丸め仕様） | 仕様決定の要否が レーンA=要 / レーンC=不要 | **不要** | 丸めの実行系は業務判断ではなく設計者が決めてよい技術的選択。決めて書けば解消する。決定内容は Phase 3 の人間承認の対象に含める |
| F-012(3)（E2E のフェーズ矛盾） | レーンA のみが「Phase 10 でのコード化はフローに反する」と主張 | **指摘を採用しない**（記録は残す） | `docs/開発プロセス/テストレベル選定トリガー表.md` 6章に「E2E は Phase 5 で仕様凍結のみ行い、Phase 10 でコード化・実行」と明記されており、設計書の記述はフロー正典に従っている |
| F-016（文字列長）／F-009（タスク融合） | レーンB・C が独立に同一問題を指摘、severity は双方 Major | Major のまま1件に統合 | 重複統合により件数の水増しを避ける |

---

# Round 2（差分レビュー）

- レビュー日: 2026-08-13
- 方式: 敵対的較正・3レーン並列・差分レビュー（改訂が広範囲のため全文読解のうえ、解消確認＋新規欠陥＋must-catch に限定）
- 総合判定: **FAIL**

## サマリー

| Severity | 件数 |
| -------- | ---- |
| Critical | 1 |
| Major | 12 |
| Minor | 21 |

レーン別の生件数は A: Critical 1 / Major 5 / Minor 9、B: Major 4 / Minor 9、C: Critical 1 / Major 5 / Minor 9。重複統合後が上表。

## Round 1 findings の解消状況

| FIND-ID | 判定 | 備考 |
|---|---|---|
| F-001（Critical） | **解消** | ただし修正（丸め後判定への変更）が F2-001 を新たに生んだ |
| F-002（Critical） | **解消** | T-1 手順4 の 0 件時 INSERT 非発行・AC-06・E2E-3 |
| F-003（Critical） | **解消** | AC-11・AC-12 新設。全件削除実装は AC-11 で必ず落ちる |
| F-004 | **解消** | R-1 が「1 文字以上の連続する空白列」と明記。規定式と本文が一致 |
| F-005 | **解消** | 空白文字集合を JS の `\s` 基準で一意に規定 |
| F-006 | **解消** | `parseFloat` を正と明記。判定表 12 入力を実挙動と照合し全件一致 |
| F-007 | **部分的** | 24 判定タイミングは解消。規定式そのものに新規欠陥（F2-001）、`toFixed` の不採用根拠が事実誤り（Minor） |
| F-008 | **解消** | `IS NOT DISTINCT FROM` を実現手段まで規定。ただし検証する AC がない（F2-007） |
| F-009 | **解消** | R-4 の連結。残る区切り文字衝突は F2-006 |
| F-010 | **部分的** | RPC 境界は AC 対象外に落として解消。複数件組み立て等が未規定（F2-003） |
| F-011 | **解消** | 共通前提の定義により AC-01×AC-36、AC-23〜26×AC-27 の衝突が解消。AC-01〜45 総当たりで矛盾なし |
| F-012 | **解消** | 事実誤認を訂正。検証手段を画面観測範囲に限定 |
| F-013 | **部分的** | No.2 は注記＋代替案2案で解消。**No.3 は注記ゼロのまま**（F2-004） |
| F-014 | **解消** | a〜f すべて AC に写像 |
| F-015 | **解消** | 変更点表を新設し #9 の変更を明記。ただし表の全件照合に不備（F2-004・F2-005） |
| F-016 | **部分的** | R-6 新設で主要部は解消。検証範囲（F2-002）と検証値の食い違い（F2-010）が残存 |
| F-017 | **部分的** | save↔save は成立を確認（下記）。**T-2 がロック順序規定から漏れている**（F2-009） |
| F-018 | **解消** | C-5 注記・影響表・例外系 E-6 |
| F-019（Minor） | **部分的** | 23 箇所照合し 5 箇所が誤り。**うち 4 件は改訂で新たに混入**（司令塔が実ファイルで再確認済み: `chk_task_type`=50 / `chk_task_status`=54 / `chk_task_priority`=58 / `chk_task_complexity`=62、`inputValidator`=65） |
| F-020〜F-024, F-026〜F-031, F-033, F-035〜F-037 | **解消** | 各 Minor は該当節への追記で解消 |
| F-025 | **部分的** | R-7 新設。配列要素の型が未検証（F2-011） |
| F-032, F-034 | **未解消（文書化のみ）** | lean 運用で Minor は文書化通過可 |

**レーンC が確認した肯定的証拠（save↔save の直列化）**：`daily_reports.date` は `.unique()`（`reports.ts:15`）で一意インデックスが存在するため `ON CONFLICT DO UPDATE` のアービタ推論が働き、行が**無い**初回保存でも後発は投機的挿入トークン→`XactLockTableWait` で先行のコミットまでブロックする。行が**有る**場合も DO UPDATE の行ロックでブロックする。マスタ解決の昇順固定も、共有資源が全 TX で同一値になるため相対順序が一致し循環しない。`time_entries` の FK ロックは KEY SHARE で非排他、`tasks` は UPDATE/DELETE しないため交差しない。**T-1 の主張は save 同士では成立している。**

## Findings

### Critical

#### F2-001: R-3 の丸め規定式が NaN を返す入力で R-2 の判定2・判定3 を同時にすり抜け、`hours = NaN` が保存される
- **観点 / レーン**: 境界値・DB制約との衝突（レーンA F2-A-001 ＋ レーンC F2-C-001。**2レーンが独立に同一の Critical を検出**）
- **該当箇所**: 「R-3」規定式／「R-2」判定表（判定2・判定3 がいずれも丸め後の値に対する判定であること）／例外系末尾「`chk_hours_positive` に違反する経路は存在しない」／AC-01・AC-02・AC-03。制約実体は `apps/web/src/server/schema/tasks.ts:96,109`
- **問題**: 規定式 `Number(\`${Math.round(Number(\`${n}e+2\`))}e-2\`)` は `String(n)` が最短往復表現を返す性質に依存するが、**`String(n)` が指数表記を返す領域で壊れる**。JavaScript は絶対値が 1e21 以上、または 1e-6 未満の数値を指数表記で文字列化する。
  - `n = 1e-7`（入力 `"0.0000001"`）→ `"1e-7e+2"` → **NaN**
  - `n = 1.2345678901234567e19`（入力 `"12345678901234567890"`＝20 桁の貼り付け）→ `"12345678901234567000e+2"` = 1.2345678901234568e21 → `"1.2345678901234568e+21e-2"` → **NaN**
  - `n = 1e23`（入力 `"99999999999999999999999"`）→ `"1e+23e+2"` → **NaN**

  R-2 の判定は「1: `parseFloat` が有限か（`1e19` も `1e-7` も有限なので通過）→ 2: 丸め後が 0 以下か（`NaN <= 0` は **false**）→ 3: 丸め後が 24 超か（`NaN > 24` も **false**）→ 4: 対象行」であるため、**NaN は判定2 と判定3 の両方を素通りして「対象行」に分類される**。これは Round 1 の F-001 修正（判定を丸め前から丸め後へ移した）が直接生んだ経路であり、丸め前判定であれば `1e19` は判定3 でエラーになっていた。
- **影響**: `time_entries.hours` に `NaN` が書き込まれる。PostgreSQL の `numeric` は特殊値 `NaN` を受理し、かつ**比較において NaN を全ての非 NaN 値より大きい**として扱うため、`chk_hours_positive`（`hours > 0`）は**通過する**。`numeric(4,2)` の精度・位取りも NaN には適用されないため 22003 も出ない。すなわち設計書が「到達しない」と断言した唯一の防波堤が機能せず、**無警告で NaN が永続化される**。以後 `SUM(hours)` を含む全ての集計（本機能のゴールそのもの）が期間全体で NaN になり、どの行が原因かは SQL を直接叩くまで分からない。読み取り側の画面は非ゴールで存在しないため発覚が遅れ、バックフィルも非ゴールのため復旧は手動 SQL のみ。仮に実装が数値 NaN をドライバに渡して型エラーになる経路を通った場合でも、結果は「トランザクション全体ロールバック＝日報が保存できない」という新規の保存不能パスであり、いずれの分岐でも設計目標に反する。AC-29〜AC-36 のいずれもこの入力を扱っていないため、Phase 5 のテストは全件 PASS する。
- **失敗シナリオ**: 実績h 欄に別セルからコピーした 20 桁以上の数字列を誤って貼り付けて保存 → 判定1 通過（有限）→ 丸めが NaN → 判定2・3 を通過 → `time_entries` に `NaN` を INSERT → `chk_hours_positive` 通過 → コミット成功、画面には「保存しました ✓」。翌月、取引先別集計の SUM が全て NaN になる。
- **推奨対応**: (a) R-2 に「丸め結果が有限の数値でない場合」の判定を追加する、(b) R-3 を指数表記に依存しない実装で規定し直す、(c) 判定1 を安全域（`Math.abs(n) < 1e15` 等）に強化する、のいずれか。**PostgreSQL の numeric が NaN を受理し `NaN > 0` が真になる事実**も設計書に明記し、「DB 制約が最後の砦にならない」ことを前提として書くこと。`"0.0000001"` と `"12345678901234567890"` を AC に追加して境界を固定する。
- **仕様決定の要否**: 不要（レーンA）／要（レーンC）→ **司令塔の裁定: 不要**。丸めの実行系は設計者が決めてよい技術的選択（Round 1 F-007 の裁定と同じ扱い）。ただし決定内容は Phase 3 の承認対象に含める

### Major

#### F2-002: R-6 の長さ検証が「全行」か「対象行のみ」か未定義で、DB に一切書かれない値のせいで保存全体が失敗する
- **観点 / レーン**: 仕様の曖昧性（レーンA F2-A-002）＋ データ量・文字列長（レーンC F2-C-003）
- **該当箇所**: 「R-6」／「T-1」トランザクション外の手順3／AC-38／AC-27／目的節「これまで保存できていた日報が保存できなくなることがない」
- **問題**: R-6 は検証対象を「取引先名・プロジェクト名・タスク名」とだけ書き、**R-2 でスキップされた行を含むかを規定していない**。T-1 手順3 は R-2 の抽出と R-6 の検証を同じ手順にまとめ、適用順序も書かれていない。AC-38 は「…が正規化後 255 文字を超える**行が 1 件でもある場合**」と無条件で書かれているため、字義どおりなら実績h が空でマスタも作られない行の名前でも保存全体が失敗する。R-6 の根拠は「超過すると 22001 になる」ことだが、スキップ行の名前は**そもそも一度も DB に送られない**ため、根拠が成立しない範囲まで検証が広がっている。
- **影響**: 実績h を入力していない行（予定だけ入れた行・書きかけの行・テンプレートから復元された行）に長い文字列が入っているだけで日報全体が保存できなくなる。従来は `raw_data` に保存できていた入力であり、本変更が持ち込む新規の保存不能パスである。逆に「対象行のみ」と解釈した実装でも AC-38 のテスト（実績h 有効な行で書かれるのが自然）は PASS するため、**この分岐はテストで検出されない**。
- **失敗シナリオ**: 前日のテンプレートを復元するとタスク行が復元され、その 1 行のタスク名にメールから貼り付けた 300 文字が入っている（実績h は未入力）→ 当日分の実績を別の行に入力して保存 → 保存全体が失敗 → 工数を入力した行は正しいのに日報が 1 行も保存できない。
- **推奨対応**: R-6 に「検証対象は R-2 で対象行と判定された行に限る」と明記し、R-2 → R-6 の適用順序を規定する。AC-38 の冒頭に「実績h が有効な行のうち」を追加し、「実績h が無効な行に 300 文字の名前があっても保存は成功する」を AC に追加する。
- **仕様決定の要否**: 要（レーンA）／不要（レーンC）→ **司令塔の裁定: 不要**。R-6 の根拠（22001 回避）と目的節の非デグレード方針の双方と整合する側（対象行のみ）に決めれば足りる

#### F2-003: エラーメッセージ規範が「複数件の組み立て」「2 種の検証エラーの同時発生」「R-4 連結時の項目名・タスク名」を規定しておらず、exact-match を要求する AC-37・AC-38 の期待値が一意に決まらない
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA F2-A-003）＋ レーンC F2-C-010・レーンB F2-B-008 を統合
- **該当箇所**: 「エラー処理 > エラーメッセージの規範」／AC-37／AC-38／「検証の範囲」節／AC-41
- **問題**: 4 点ある。(1) **複数件時の組み立てが未規定**：テンプレートは「見出し行＋該当行 1 行」の 2 行構成で示され、規則は「該当行を改行区切りで全件列挙する」とだけ書かれている。見出し行を 1 回だけ出して該当行を N 行続ける形と、2 行のテンプレート全体を N 回繰り返す形の**両方が規範に適合する**。(2) **2 種の検証エラーの同時発生が未規定**：R-2 判定3（24 超）と R-6（255 文字超）は同じタイミングで検証されるが、両方に該当する日報でどちらを投げるのか、両方を結合するのかが本文にもテンプレートにもない。(3) **R-4 連結が該当する行の表記が未規定**：`<タスク名>` に連結後を出すのか連結前を出すのか、長さ超過側の `<項目名>` が `タスク名` か `プロジェクト名` かが決まらない。(4) エラーメッセージ規範のうち「入力構造の不正」の文言だけ対応する AC がない（AC-41 は文言一致を要求しない）。
- **影響**: 本機能はフロー設定上 Phase 7 で人間がテストを凍結する。凍結された期待文字列と Phase 8 の実装が食い違った場合、Phase 8 はテスト修正禁止であるため収束ループが空転し、承認済み成果物の変更＝人間エスカレーションになる。これは Round 1 の F-010 が指摘した障害モードそのものであり、テンプレートを規範化しただけでは解消していない。
- **失敗シナリオ**: `test-builder` が AC-37 から `"実績工数が上限（24時間）を超えています。\n「A」= 25\n「B」= 30"` を期待値に固定 → Phase 7 で凍結 → `impl-builder` が行ごとに見出しを繰り返す実装を書く → Green にできず、テスト修正も不可 → エスカレーション。
- **推奨対応**: 見出し行と該当行の関係を確定させ、N=2 の具体例でテンプレートを示す。24 超と 255 文字超の同時該当時の優先順位（または結合方法）を規定し AC を追加する。`<タスク名>`・`<項目名>` に R-4 連結の適用有無を明記する。AC-41 に文言一致を含める。
- **仕様決定の要否**: 不要

#### F2-004: MC-4 — 未決事項 No.3（24 時間上限）が本文 8 箇所に確定値として残り注記ゼロ。かつ 24 上限は要件メモ決定事項 #5 に反するのに「要件メモからの変更点」表に記載がない
- **観点 / レーン**: MC-4 未決事項の温存（レーンA F2-A-004）＋ 要件の網羅性・決定事項の無断変更（レーンB F2-B-004(1)）
- **該当箇所**: 未決事項 No.3／「全 AC 共通の前提」／R-2 判定3・判定表／データ項目マッピング表 No.4／AC-32・AC-33・AC-34・AC-36・AC-37／「エラーメッセージの規範」／E2E 候補 C-1／非ゴール「1 日の合計工数の上限チェック」／「要件メモからの変更点」表／要件メモ決定事項 #5
- **問題**: (1) Round 1 の F-013 は No.2 と No.3 の双方への注記を推奨したが、**No.2 には注記と代替案 2 案が入った一方、No.3 にはどの箇所にも注記が入っていない**。`24` は上記 8 箇所に確定値として現れ、いずれからも未決 No.3 への参照がない。設計書だけを根拠にテストを起案する Phase 5 の `test-builder` は 24 を確定仕様として AC-32/33/34/36/37 の 5 本に埋め込む。(2) より重い問題として、**24 上限は要件メモに存在しない新規制約であり、要件メモの決定事項 #5「実績h が 0・空・不正な値の行はスキップ。`h > 0` の行は必ず記録し…」と正面から衝突する**（24 超の行は `h > 0` だが記録されないどころか保存全体を失敗させる）。R-6 の 255 文字超も同じ構造である。設計書は「要件メモの決定事項からの変更点（Phase 3 で明示的な承認が必要）」表を新設しながら、この変更を 1 行も記載していない。さらに同表の #5 の引用は「取引先・PJ が空なら adhoc でマスタ紐づけなし」と要約され、**衝突している「h > 0 の行は必ず記録し」の句が引用から落ちている**ため、表だけを読む承認者は衝突に気づけない。
- **影響**: Phase 3 の人間承認は未決事項表と変更点表を判断材料とする。表に載っていない逸脱は承認されないまま確定する。特に (2) は「従来 `raw_data` に保存できていた入力で日報が保存できなくなる」という**既存機能のデグレード**であり、設計書自身が目的節に掲げた「これまで保存できていた日報が保存できなくなることがない」に直接抵触する。同ブレットは「（保存を失敗させるのは、本設計で明示的に検証エラーと定めた場合に限る）」という括弧書きによって**自己免責され反証不能になっており**、対応する AC も存在しない。No.3 が否決されれば AC-32〜37 の 5 本と R-2 判定表・エラーメッセージ規範の書き直しになり、Phase 5 のテスト複数本が作り直し（凍結後なら人間エスカレーション）になる。
- **失敗シナリオ**: Phase 3 で承認者が変更点表（2 行）を確認し「決定事項からの逸脱はこの 2 件だけ」と理解して承認 → 運用開始後、利用者が実績h に `25`（打ち間違い／複数人分の合算入力）と入力 → 日報全体が保存できない → 「そんな仕様は合意していない」となるが、承認済み設計書に AC-34・AC-36 として明記されているため戻り先は Phase 1 ＝人間エスカレーション。
- **推奨対応**: R-2 判定3・共通前提・AC-32〜37・エラーメッセージ規範・E2E 候補 C-1・非ゴールに「（未決事項 No.3。Phase 3 の判断により変更されうる）」の注記を入れる（No.2 と同じ扱いに揃える）。変更点表に決定事項 #5 の「`h > 0` の行は必ず記録し」に対する変更（24 超・255 文字超は記録せず保存全体を失敗させる）を、デグレードであることを明示して追記する。#5 の引用は該当句を落とさず全文を載せる。目的節のブレットから自己免責の括弧書きを外し、「新たに保存不能になる入力は 24 超・255 文字超・入力構造不正の 3 つに限る」と列挙形に改めて反証可能にする。
- **仕様決定の要否**: 不要（注記と追記で解消。No.3 自体の決定は Phase 3 の人間判断）／レーンB は「24 超をエラーとするかスキップとするかは要件メモ #5 の再確認が必要な業務判断」として**要**と判定 → **司令塔の裁定: 要**（人間に確認する）

#### F2-005: MC-4 — R-1 の「単独の全角スペースを半角スペースに畳む」は要件メモ決定事項 #4 からの逸脱だが、「要件メモからの変更点」表に記載がない
- **観点 / レーン**: MC-4（レーンA F2-A-005）＋ 要件の網羅性（レーンB F2-B-004(2)）
- **該当箇所**: 「R-1」の補足／「要件メモからの変更点」表／要件メモ決定事項 #4
- **問題**: 要件メモ決定事項 #4 は「`trim` ＋ **連続空白の圧縮のみ**。…**大文字小文字・全角半角は別物として扱う**」と確定している。改訂後の R-1 は F-004 の指摘を受けて「1 文字以上の連続する空白列を半角スペース 1 つに置換する（単独の全角スペース・タブも半角スペース 1 つになる）」を採用した。これは (a)「連続空白の圧縮」ではない単独文字の置換であり、(b) 全角文字 U+3000 を半角文字 U+0020 に変換する処理であって「全角半角は別物として扱う」に反する。設計書は R-1 の補足で理由を述べているが、**変更点表には #9 と #5 の 2 件しか載っておらず #4 の変更は載っていない**。同表は Round 1 の F-015（決定事項 #9 の無断変更）を受けて新設されたものであり、同じ種類の逸脱が新たに 1 件、表の外に置かれている。
- **影響**: 名前正規化はマスタ同一性の唯一の判定基準であり、#4 は要件メモで明示的に合意された名寄せの根幹方針である。Phase 3 の承認者は「変更点表に載っていない＝要件メモどおり」と読むため、`"A　社"` と `"A 社"` が同一マスタに統合される方針変更に気づけない。一度統合された `time_entries` は後から分離できず、バックフィルは非ゴールのため復旧手段がない。
- **推奨対応**: 変更点表に決定事項 #4 の行を追加し、「単独の全角スペースも半角スペースに畳む（＝全角半角を別物として扱う原則の例外）」ことと理由・影響を記載して Phase 3 の明示的承認の対象に含める。
- **仕様決定の要否**: 要（要件メモで合意済みの名寄せ方針の変更）→ **司令塔の裁定: 記述追加で足りる**。R-1 の実質（空白の打ち間違いを吸収する）は Round 1 で人間が承認した AC-16 の内容そのものであり、新たな決定ではなく**変更点表への記載漏れ**である。ただし Phase 3 の承認対象として明示する

#### F2-006: R-4 の連結区切り `" / "` が名前中に現れた場合のキー衝突が未規定で、異なる入力が同一タスクに融合する
- **観点 / レーン**: 仕様の曖昧性（レーンA F2-A-006）＋ 重複・衝突（レーンC F2-C-007）
- **該当箇所**: 「R-4」の連結書式／「M-4」／AC-23・AC-25・AC-28／S-3
- **問題**: R-4 は連結書式を「`<プロジェクト名>` + 半角スペース + `/` + 半角スペース + `<タスク名>`」と規定するが、**プロジェクト名やタスク名に `" / "` が含まれる場合の扱いがない**。名寄せキーは `(NULL, NULL, title)` の文字列一致であるため、プロジェクト名 `"設計 / 実装"` × タスク名 `"調査"` と、プロジェクト名 `"設計"` × タスク名 `"実装 / 調査"` が同一タスクに融合する。さらに AC-23 の経路（取引先・プロジェクトともに空、タスク名 `"設計 / 実装 / 調査"`）とも融合する。設計書は M-4 のキャッシュキーについては「区切り文字が名前中に現れた場合に別のキーと衝突する」と**同じ危険を明示的に認識して `JSON.stringify` を採用している**にもかかわらず、DB に永続化される本来のキー（`title`）については同じ検討をしていない。R-5 が `(名称未設定)` の実入力衝突を「既知の帰結」として明記したのとも扱いが不揃いである。
- **影響**: R-4 の連結は Round 1 の F-009 を解消するために導入されたものであり、その目的は「プロジェクト単位で分離できる」ことである。区切り文字の衝突はこの目的を局所的に破る。「`time_entries` の総計は正しいまま内訳だけが静かに誤る」ため、後続の集計機能を作るまで誤りに気づけず、融合後の分離は不能。`"設計/実装"` のようなプロジェクト名は実務で珍しくない。
- **推奨対応**: (a) 名前中に現れ得ない区切り（制御文字など）を規定する、(b) R-5 と同様に「既知の帰結」として衝突を明記し許容する、(c) 連結前に区切り文字をエスケープする、のいずれかを R-4 に明記する。(b) を採る場合も AC に書き、Phase 5 のテストが融合を「正しい挙動」として固定してよいことを明示する。
- **仕様決定の要否**: 不要

#### F2-007: adhoc タスク（`project_id` / `client_id` が NULL）の名寄せ再利用を検証する AC が 1 件もなく、Round 1 F-008 の障害が全 45 AC を通過する
- **観点 / レーン**: 要件の網羅性（レーンB F2-B-001）
- **該当箇所**: 「M-3」の `IS NOT DISTINCT FROM` 規定／AC-15・AC-20・AC-23〜AC-25・AC-28／受け入れ条件冒頭の「Phase 5 のテスト観点はこの条件を唯一の根拠として起案される」
- **問題**: M-3 は Round 1 F-008 への対応として `IS NOT DISTINCT FROM` を障害モードまで明記したが、この規定を検証する AC が存在しない。既存 AC を 1 件ずつ当てると：AC-15 は「同一**プロジェクト内**の同名タスク」＝ `project_id` / `client_id` がともに非 NULL のケースのみ。AC-20 は取引先 A・B の同名 adhoc タスクが**別レコードになる**ことの確認であり、1 回の保存で 2 件新規作成されれば成立するため `eq()` 実装でも PASS する。AC-23・AC-24・AC-25・AC-28 はいずれも 1 回の保存で作られたタスクの属性を見るだけで、**2 回目の解決（既存行の再利用）を要求していない**。AC-07（同じ日報を 2 回保存）は唯一この経路を踏みうるが、テストデータに adhoc 行を含めることが AC 文面に要求されていないため `type='project'` の行だけで書けば PASS する。
- **影響**: AC にない性質はテスト化されない。`eq(tasks.projectId, null)` を書いた実装が **AC-01〜AC-45 を全件 PASS したまま Phase 8・Phase 10 を通過する**。本機能は実装承認ゲートがスキップされており、Phase 10 の機械ゲート（全テスト Green）が最後の砦であるため、ここを通過すると誰も止められない。障害は「取引先欄を空にした日報の 2 回目の保存以降、日報そのものが保存できなくなる」というクリティカルパスの完全停止であり、要件メモ決定事項 #5 が主要ケースとして明示した入力パターンで発生する。
- **失敗シナリオ**: 8/13 に取引先欄を空・タスク名「社内MTG」・実績h 1 で保存（1 回目は成功）→ 8/14 に同じ行を含む日報を保存 → M-3 の検索が `= NULL` で 0 件 → `ON CONFLICT DO NOTHING` が S-3 に弾かれて 0 行 → 再 SELECT も 0 件 → 1 回だけの再試行も同じ結果 → エラーで中断 → 8/14 の日報が保存できない。結合(DB込み)テストは AC-15 で名寄せを確認済みのため、レビュワーも「名寄せはテスト済み」と誤認する。
- **推奨対応**: ①取引先名・プロジェクト名がともに空で同名のタスク行を含む日報を 2 日分保存すると `tasks` は 1 件・`time_entries` は 2 件になり 2 回目の保存も成功する、②取引先名のみあり・プロジェクト名が空の同名 adhoc タスク行で同様、の 2 件を AC に追加する。AC-07 の前提に「日報には `type='project'` の行と adhoc 行の両方を含む」ことを明記する。
- **仕様決定の要否**: 不要

#### F2-008: 本文で新設・変更された規定 4 件に対応する AC がなく、「AC を全件満たすが設計に違反する実装」が通る
- **観点 / レーン**: 要件の網羅性（レーンB F2-B-002）＋ レーンC F2-C-014 を統合
- **該当箇所**: 「R-1」冒頭（適用範囲）／「R-7」末尾（非文字列フィールドの扱い）／「解決の順序」／「T-3」および「解決の共通方針 > 分離レベル」／AC-01〜AC-45 全体
- **問題**: (a) **R-1 の適用範囲**：「すべての名前（取引先名・**プロジェクト名・タスク名**）に共通で適用する」と規定しているが、正規化を検証する AC-16・AC-17・AC-18 はいずれも**取引先名のみ**を対象としている。`clients` の解決だけ正規化する実装は全 45 AC を PASS する。AC-14・AC-15 も完全一致の名前を渡す限り正規化の有無で結果が変わらない。(b) **R-7 の非文字列フィールドの扱い**：「各フィールドが文字列でない場合は空文字として扱う」と規定しているが、これを固定する AC がない。加えてこの規定自体が「`actualHours: 7.5`（数値型）で送られた行は空文字扱い＝R-2 判定 1 でスキップ」という**サイレントな工数欠落**を仕様として確定させており、R-2 の `parseFloat` 採用理由（「画面表示と `time_entries` の合計が常に一致することを優先」）と方向が食い違う。(c) **解決の順序固定**：デッドロックを構造的に防ぐ規定だが対応する AC がない。AC-45 は**同一日付**の同時保存しか対象にしておらず、デッドロックが起きるのは**異なる日付**の同時保存である。逐次解決する実装は全 45 AC を PASS する。(d) **分離レベルの固定**：対応する AC がない。
- **影響**: (a) はマスタ二重化を静かに起こす（「調査 」と「調査」が別タスクになり、総計は正しいまま内訳だけが誤る）。(c) は本番でのみ再現する間欠的な保存失敗で、利用者には `保存エラー: deadlock detected` としか出ない。いずれも実装承認ゲートがスキップされている本機能では AC 由来のテストが唯一の検出手段である。
- **推奨対応**: (a)「空白のみが異なるプロジェクト名／タスク名は同一の `projects` / `tasks` レコードに解決される」、(b)「`Task.actualHours` が文字列でない値で送られても保存は成功し、その行の `time_entries` は作成されない」、(c)「新規の取引先 A・B を逆順に含む別日付の日報を 2 つ同時に保存しても双方が成功しデッドロックにならない」（`db.ts` は `postgres({ max: 10 })` で複数接続が取れることを確認済み）、(d) は (c) と AC-45 の前提として分離レベルを明記する。
- **仕様決定の要否**: (a)(c)(d) 不要／**(b) 要**（非文字列の実績h を無言で捨てる仕様の是非は業務判断）

#### F2-009: T-2（`deleteReportFn`）が T-1 と逆順にロックを取るため、同一日付の保存と削除が並行するとデッドロック／孤児 `time_entries` が発生する
- **観点 / レーン**: 並行・削除の巻き込み（レーンC F2-C-002）
- **該当箇所**: 「T-1」トランザクション内の順序（1. `daily_reports` upsert → 2. `DELETE time_entries`）／「T-2」の順序（1. `DELETE time_entries` → 2. `DELETE daily_reports`）／「解決の順序」／AC-10・AC-12・AC-45／例外系 E-6
- **問題**: 設計書は「順序を固定すればデッドロックが構造的に発生しない」と述べるが、その規定は**マスタ解決の 3 段階にしか及んでいない**。T-1 と T-2 は `daily_reports` と `time_entries` という同じ 2 つの資源を**互いに逆の順序**で取得する（T-1: `daily_reports` → `time_entries`／T-2: `time_entries` → `daily_reports`）。典型的な ABBA であり、同一日付に対して保存と削除がほぼ同時に走ると相互待機になる。さらに T-2 は `daily_reports` のロックを先に取らないため、T-1 が主張する「同一日付の直列化」の枠外にある。デッドロックにならない interleaving でも整合性が壊れる：対象日に既存 `time_entries` が無い状態で T-2 が先に走ると、T-2 の 1 文目は 0 行しかロックせず 2 文目で T-1 の `daily_reports` 行の解放を待つ。その間に T-1 が `time_entries` を INSERT してコミットし、そのあと T-2 が `daily_reports` だけを削除してコミットする。
- **影響**: (1) 利用者に `保存エラー: deadlock detected`（生の英語メッセージ）が出て日報が失われる。E-6 は「ロールバックし、日報も `time_entries` も残る」としか書かずこの経路を想定していない。(2) より深刻なのは孤児化で、**`daily_reports` が存在しない日付に `time_entries` だけが残る**。両者に外部キーはなく日付の一致だけで対応づける設計であるため、この状態を検出・修復する手段がない。当該日付を再保存するまで、削除したはずの日の工数が集計に残る（AC-10 の性質が並行時に破れる）。`deleteReportFn` に UI 導線がないことは緩和材料だが、設計書自身が「エンドポイントを直接叩く経路でのみ到達する」と認めており到達不能ではない。
- **推奨対応**: T-2 のトランザクション内順序を **T-1 と同じ「`daily_reports` を先に触る」順序に揃える**。「解決の順序」節の適用範囲を「本機能が触る全テーブル（`daily_reports` → `time_entries` → `clients` → `projects` → `tasks`）」へ拡張し、T-1 の「手順1 を最初に置くことは整合性の要件」という規定が T-2 にも及ぶことを明記する。「同一日付の保存と削除を同時に実行しても、`daily_reports` が存在しない日付に `time_entries` が残らない」を AC に追加する。
- **仕様決定の要否**: 不要

#### F2-010: R-6 が検証する値と実際に INSERT される `tasks.title` が食い違う経路があり、22001 が残存する
- **観点 / レーン**: データ量・文字列長・DB制約との衝突（レーンC F2-C-004）
- **該当箇所**: 「R-6」（「`normalizeName` 適用後（R-4 の連結が該当する場合は連結後）」）／「R-4」／「R-5」／AC-28・AC-38。列定義は `apps/web/src/server/schema/tasks.ts:23`
- **問題**: `tasks.title` に実際に書かれる値は「R-1 正規化 → R-5 の既定値適用（空なら `(名称未設定)`）→ R-4 の連結」を経た文字列である。ところが R-6 は「`normalizeName` 適用後（R-4 の連結が該当する場合は連結後）」としか書いておらず、**R-5 の既定値適用が検証の前か後かを規定していない**。実装者が「正規化後のタスク名（空文字）＋プロジェクト名を連結した値」を検証すると検証対象は `<PJ名> + " / "`（＝PJ名+3）になるが、実際に INSERT されるのは AC-28 により `<PJ名> / (名称未設定)`（＝PJ名+10）である。両者は 7 文字ずれる。
- **影響**: PJ名が 249〜255 文字でタスク名が空の行を保存すると、検証を通過したうえで 256 文字以上が渡り **22001 でトランザクション全体がロールバック**する。R-6 がそもそも防ごうとした状態が、R-6 を実装したうえでなお残る。境界がずれているだけなので AC-38 の「255 文字ちょうどは成功する」テストは通常のケースで PASS し、この経路は検出されない。
- **推奨対応**: R-6 に「検証対象は、R-5 の既定値適用および R-4 の連結をすべて適用したうえで、実際に各列へ書き込む最終的な文字列とする」と規定する（列ごとに最終値を明示）。AC-38 に「取引先が空・プロジェクト名 250 文字・タスク名未入力の行は連結後 260 文字となり保存全体が失敗する」を境界ケースとして追加する。
- **仕様決定の要否**: 不要

#### F2-011: R-7 が配列要素の型を検証しないため、`projects: [null]` 等で走査が TypeError を投げ、AC-41 の規範メッセージに到達しない
- **観点 / レーン**: 空状態・異常系（レーンC F2-C-005）＋ レーンA F2-A-011 を統合
- **該当箇所**: 「R-7」（検証項目は 2 点のみ／「走査で例外を出さないことのみを保証する」）／AC-41／例外系 E-2／「エラー処理 > 入力構造の不正」テンプレート。実体は `apps/web/src/server/functions/reports.ts:65`（素通しの `inputValidator`）
- **問題**: `data.projects[].tasks` が配列であることを確かめるには各要素の `.tasks` を参照する必要があるが、要素が `null` / `undefined` / プリミティブの場合、その参照自体が TypeError（`Cannot read properties of null (reading 'tasks')`）を投げる。`tasks` 配列の要素も同様で `tasks: [null]` は `.actualHours` の参照で落ちる。R-7 は要素の型に関する規定を持たないため、設計書だけを根拠に実装すると **R-7 自身が宣言した「走査で例外を出さないことのみを保証する」に反する**。加えて `data.date` が文字列であることも無検証のまま `daily_reports.date` と `time_entries.date` の両方に渡る。
- **影響**: AC-41 は「DB に触れる前に検証エラーとなり」、エラー処理節は日本語テンプレートを**規範**と定めているが、実際に返るのは V8 の英語 TypeError であり、AC-41 と規範テンプレートの両方を満たさない。Phase 5 のテストが規範どおりの期待値で書かれれば Phase 8 で Green にならず、凍結済みテストと衝突して人間エスカレーションになる。`saveReportFn` は公開エンドポイントで `inputValidator` は素通しなので任意の JSON が到達する。従来はどんな JSON でも `raw_data` に保存できていたため、保存可能だった入力が失敗に転じる回帰でもある。
- **推奨対応**: R-7 の検証項目に「`data.projects` の各要素が null でないオブジェクトであること」「`tasks` の各要素が null でないオブジェクトであること」を追加する（あるいは「オブジェクトでない要素は無視する」と規定して例外を出さない側に倒す）。`data.date` の検証も含めるか、含めない理由を明記する。AC-41 に `projects: [null]` / `tasks: [null]` を検証対象として明示する。
- **仕様決定の要否**: 不要

#### F2-012: 規範化したエラーメッセージが利用者に届くこと・読めることを設計のどこも担保しておらず、本文内で矛盾し、どのテストレベルでも検証されない
- **観点 / レーン**: 非ゴールの明確性・テストレベル判定の妥当性（レーンB F2-B-003）＋ レーンA F2-A-015 を統合
- **該当箇所**: 「エラー処理 > エラーメッセージの規範」（全件列挙・改行区切り）／「検証の範囲」節／例外系 E-3・E-4／「R-6 > 切り詰めではなくエラーとする理由」／非ゴール「UI の変更」／E2E-1〜E2E-3／E2E 候補 C-1。実装側の実体は `apps/web/src/routes/index.tsx:114-121`（`setTimeout(…, 2000)`）と `:217`（`<span className="text-xs text-success">{savedMsg}</span>`）
- **問題**: (1) **本文内の矛盾**：例外系 E-3・E-4 は「画面に `保存エラー: <メッセージ>` が表示される」と断定する一方、「検証の範囲」節は「RPC 境界を越えて到達するかは**未検証**」と述べ、E2E の期待値からメッセージ本文を外している。同一文書が同じ挙動を「表示される（断定）」と「未検証」の両方で記述している。(2) **表示機構が規範を成立させない**：非ゴール「UI の変更 — 保存失敗時のメッセージ表示は既存機構をそのまま使う」により、エラーは `text-xs` の `text-success`（**成功色の緑**）で 1 行に表示され、**2 秒で消える**。エラーメッセージ規範は「改行区切りで全件列挙」と定めるが、`white-space` 指定のない `<span>` に入るため改行は潰れて 1 行に連結される。R-6 が「切り詰めではなくエラー」を選んだ根拠は「**エラーであれば利用者が入力を修正できる**」だが、緑色・極小・1 行連結・2 秒で消えるメッセージでは 300 文字のタスク名を含む行を特定して修正することはできない。(3) **検証の帰属先がない**：AC-37・AC-38 は `Error.message` しか検証しない（結合(内部)）。E2E-1〜E2E-3 は成功パスのみでエラー表示を見る E2E は 1 件もない。エラー表示を扱う C-1 は候補カタログ送り＝自動化されない。結果、本改訂が新規に持ち込んだ**3 つの保存全体失敗パス**（24 超・255 文字超・入力構造不正）について、「利用者が原因を認識して修正できる」という設計の前提を検証するテストがどのレベルにも存在しない。
- **影響**: 本機能は「これまで保存できていた日報が保存できなくなることがない」をゴールに掲げながら 3 つの保存不能パスを導入している。その代償として約束されているのが「原因が分かるエラーメッセージ」であるにもかかわらず、それが利用者に届く保証も読める保証も検証もない。RPC 境界でメッセージが失われていた場合、利用者に出るのは `保存エラー` の 4 文字だけで、日報が保存できない状態から自力で復帰できない。実装承認ゲートがスキップされているため、この欠落は Phase 10 完了まで誰にも指摘されない。
- **推奨対応**: (1) 例外系 E-3・E-4 の断定表現を「検証の範囲」節と整合させる。(2) RPC 境界でのメッセージ保持を Phase 3 までに実機確認するか、E2E に 1 件追加する（実績h に `25` を入力 → `保存エラー` を含む文言が表示され、再読み込み後も当該日報が保存されていない）。C-1 を E2E 自動化対象へ格上げする判断が妥当かを Phase 3 で決める。(3) 「全件列挙・改行区切り」を規範に残すのであれば非ゴール「UI の変更」に例外を設ける（エラー表示のみ `white-space: pre-line`・エラー色・自動消去なしに変更する）か、規範を「1 行に連結しても読める形式」に改める。**現状の「非ゴールで UI を凍結したまま多行メッセージを規範化する」組み合わせは成立しない。**
- **仕様決定の要否**: **要**（エラー表示 UI を非ゴールから外すか、メッセージ規範を現行 UI に合わせて縮退させるかは業務判断）

#### F2-013: E2E-1〜E2E-3 が開発用 DB の「当日」の日報と `time_entries` を実際に破壊し、並列実行で相互干渉する
- **観点 / レーン**: 削除の巻き込み・並行（レーンC F2-C-006）
- **該当箇所**: 「E2E シナリオの洗い出し > 自動化対象」E2E-1〜E2E-3／「検証手段の規定」／「T-1」の `DELETE FROM time_entries WHERE date = <対象日>`。実体は `apps/web/playwright.config.ts:16,20,29-32`（`fullyParallel: true`、`webServer.command: 'npm run dev'`）、`apps/web/src/lib/defaults.ts:26`（`date: new Date().toISOString().slice(0,10)`＝**今日**）、`apps/web/src/test/db-helpers.ts:3-11`、`AGENTS.md`「DB込み結合テストの注意（重要）」
- **問題**: Round 1 の F-012 は「E2E から DB を検証する手段、接続先 DB、**後始末方針**を規定するか、期待結果を画面で観測可能な範囲に限定する」ことを求めたが、改訂後は後者だけが採用され、**対象日付・接続先 DB・後始末は未規定のまま**残った。E2E-1〜3 は既存 E2E と異なり実際に「日報保存」ボタンを押す。日報入力画面は DB から既存の日報を読み込まず（`routes/index.tsx:54-92` の初期化は sessionStorage / localStorage のみ）、日付の初期値は今日である。したがって E2E は**開発用 DB の今日の `daily_reports` 行を上書きし、T-1 手順2 により今日の `time_entries` を全削除する**。とくに E2E-3（実績h を入力せず初期状態のまま保存）は対象行 0 件のため、**削除だけを実行して何も入れ直さない**。加えて `fullyParallel: true` により E2E-1/2/3 は同一日付・同一行に対して並列に走る。
- **影響**: 開発者が当日入力済みの日報とその実績が、E2E を 1 回流すだけで消える。`raw_data` ごと初期状態で上書きされるため復旧手段がなく（バックフィルは非ゴール、`time_entries` は「実績の唯一のマスター」）、**AGENTS.md が名指しで警告している「テストが開発中の日報データを消す」事故そのもの**である。本変更以前は E2E が保存を行わなかったので、これは本設計が新たに持ち込む破壊である。並列実行によりテストが互いのデータを消し合う状態を「正常」として凍結することにもなる。
- **失敗シナリオ**: 開発者が午前中に当日の日報（実績 7.5h）を保存 → 午後に `npm run test:e2e` を実行 → E2E-3 が初期状態のまま当日日付で保存 → `DELETE FROM time_entries WHERE date = 今日` が走り `raw_data` も初期状態で上書き → 当日の日報と実績が消失。画面には「保存しました ✓」が出るのでテストは PASS する。
- **推奨対応**: E2E シナリオ節に (a) **E2E が使用する日付を実運用と衝突しない固定値にする**（センチネル日付を明示的に入力してから保存する）、(b) 各シナリオの**後始末**（当該日付の `daily_reports` / `time_entries` を削除する。マスタは AC-22 に従い残す）、(c) 並列実行時にシナリオ間で日付を分ける、(d) 開発用 DB と分ける場合の `DATABASE_URL` 上書き方針、を規定として追記する。E2E-3 の期待結果に「他日付・他シナリオの日報に影響しないこと」を加える。
- **仕様決定の要否**: 不要

### Minor（1行のみ）

- F2-014: 参照行番号の誤りが 5 箇所残り、**うち 4 件は改訂で新たに混入**（`chk_task_type`=50 / `chk_task_status`=54 / `chk_task_priority`=58 / `chk_task_complexity`=62、`inputValidator`=65。司令塔が実ファイルで再確認済み）（既存 check 制約との整合／R-7）
- F2-015: `toFixed` 不採用の根拠に書かれた `(2.675).toFixed(2) === '1.00'` は事実誤り（実際は `'2.67'`。`'1.00'` になるのは `(1.005).toFixed(2)`）（R-3）
- F2-016: 「10進 half-up」の規定と規定式が負値で不一致（`Math.round(-267.5) = -267` → away-from-zero にならない。判定2 で除外されるため実害なし）（R-3）
- F2-017: ER 図が `daily_reports.date` を PK と表記しているが実際の PK は `id`（`reports.ts:14-15`）で `date` は unique 制約（ER 図）
- F2-018: 非ゴール「1 日の合計工数の上限チェック」に 24 が確定値で現れるが未決 No.3 の注記がない（非ゴール）
- F2-019: M-1・M-2 の `is_archived` 再利用規定に対応する AC がない（M-1／M-2）
- F2-020: 「解決の共通方針」の終端条件「1 回だけやり直す」に対応する AC がなく、無限リトライ実装も 1 回で諦める実装も全 AC を PASS する（解決の共通方針）
- F2-021: 新規タスク作成時に `status` / `priority` が DB 既定値になることを固定する AC がない（マッピング表 No.10／M-3）
- F2-022: 「Phase 5 で `docs/開発プロセス/E2E候補カタログ.md` に**追記**する」とあるが同ファイルは未作成で新規作成が必要（E2E 候補カタログ）
- F2-023: 「同一日・同一タスクの複数行を合算しない」規定の所在が変更点表のみで R-2 本文には走査手順としてしか書かれていない（R-2）
- F2-024: トリガー表 5 章が明記するトレードオフ（候補カタログは自動テストで守られない）への言及がなく、C-1〜C-4 が無保護であることが読み取れない（E2E 候補カタログ）
- F2-025: AC-04・AC-05 の「本変更の前後で同一」は単一のテスト実行では機械判定できない表現（AC-04／AC-05）
- F2-026: AC-13〜AC-18 に「実績h が有効な行」の前提が明記されておらず、実績h 空でテストを書くと AC-27 によりマスタが 1 件も作られず空振りする（AC-13〜AC-18）
- F2-027: 255 文字の計数単位（UTF-16 コード単位／コードポイント）が未規定で、サロゲートペアを含む名前では AC-38 の境界が実装ごとに割れる（R-6／AC-38）
- F2-028: `\s` に含まれない ZWSP（U+200B）・異体字セレクタ等は正規化されず、見た目が同一の取引先名で別マスタが作られる（R-1／S-1）
- F2-029: READ COMMITTED 前提は `db.transaction()` が素の `BEGIN` を発行することに依存し（`node_modules/drizzle-orm/postgres-js/session.js:108-121`）、サーバ／ロールの `default_transaction_isolation` 次第で静かに破綻する（T-3／解決の共通方針）
- F2-030: エラーメッセージが実績h の「入力された文字列そのまま」を長さ無制限で埋め込むため、貼り付けによる巨大文字列がそのまま返る（エラーメッセージの規範）
- F2-031: 待機側のステートメントタイムアウト規定がなく、同一日付の直列化待ちが `max: 10` の接続プール（`db.ts:10-14`）を占有しうる（非機能要件 > 性能／T-1）
- F2-032: 「対象行を走査して必要なマスタを収集したあと、まとめて解決する」は、タスクの解決順序キーが取引先・プロジェクト解決後にしか確定しないため、収集を段階的に行う必要がある点が明示されていない（解決の順序／M-4）
- F2-033: 24 超と 255 文字超が同時該当した場合の扱いが未規定（F2-003 の一部として対応）（エラー処理／AC-37・AC-38）
- F2-034: DB 生メッセージの露出（Round 1 F-032）は文書化のみで未対処。lean 運用の Minor として通過（エラー処理表 DB エラー行）

## 司令塔による裁定の記録（Round 2）

| 項目 | レーン間の相違 | 裁定 | 根拠 |
|---|---|---|---|
| F2-001（丸め式の NaN） | 仕様決定の要否が A=不要 / C=要 | **不要** | 丸めの実行系は技術的選択（Round 1 F-007 の裁定を踏襲）。ただし決定内容は Phase 3 の承認対象 |
| F2-002（R-6 の検証範囲） | A=要 / C=不要 | **不要** | R-6 の根拠（22001 回避）と目的節の非デグレード方針の双方と整合する「対象行のみ」に決めれば足りる |
| F2-004（24 上限と決定事項#5） | A=不要（注記のみ）/ B=要（業務判断） | **要** | 要件メモで合意済みの「h > 0 の行は必ず記録し」に対する実質的な変更であり、人間の確認が必要 |
| F2-005（決定事項#4 の逸脱） | A=要 / B=（記載漏れとして指摘） | **記述追加で足りる** | R-1 の実質は Round 1 で人間が承認した AC-16 の内容そのもの。新たな決定ではなく変更点表への記載漏れ。ただし Phase 3 の承認対象として明示する |

---

# Round 3（差分レビュー）

- レビュー日: 2026-08-13
- 方式: 敵対的較正・3レーン並列・差分レビュー
- 総合判定: **FAIL**（ただし Critical はゼロ）

## サマリー

| Severity | 件数 |
| -------- | ---- |
| Critical | 0 |
| Major | 9 |
| Minor | 26 |

レーン別の生件数は A: Major 3 / Minor 14、B: Major 4 / Minor 10、C: Major 6 / Minor 10。重複統合後が上表。

## Round 2 findings の解消状況

| FIND-ID | 判定 | 備考 |
|---|---|---|
| F2-001（Critical・丸め式の NaN） | **解消** | 3レーンが独立に確認。レーンA は Node で境界を実測（`String(0.005)="0.005"` 等）、レーンC は ECMA-262 Number::toString の分岐条件（10進指数 n が `−6 < n ≤ 21` なら固定小数表記）から、判定4 の値域 `[0.005, 24.005)` で指数表記が発生しないことを証明。丸め演算自体が消えたため NaN 生成経路が消滅。`chk_hours_positive` が防波堤にならない事実も明記された |
| F2-002（R-6 の検証範囲） | **解消** | R-6 冒頭で対象行に限定・AC-48 新設・T-1 手順3→4 で順序確定 |
| F2-003（メッセージ規範） | **部分的** | 見出し1回・2種同時の連結順・連結後の値・AC-41 の完全一致は規定済み。長さ超過ブロックの列挙単位が未規定（F3-001） |
| F2-004（未決 No.3 / 決定事項#5） | **解消** | grep で本文に No.3 参照 0 件。変更点表 #5 が原文「`h > 0` の行は必ず記録し」を落とさず引用し、デグレードであることを明示。目的節も列挙形に |
| F2-005（決定事項#4） | **解消** | 変更点表に #4 の行を追加 |
| F2-006（`" / "` 衝突） | **解消** | R-4 に「既知の帰結」として3例を明記し許容を宣言 |
| F2-007（adhoc 再利用の AC） | **解消** | AC-49・AC-50 が 2 日目の再解決を要求するため `eq()` 実装は必ず落ちる |
| F2-008(a)（R-1 適用範囲） | **解消** | AC-51・AC-52 |
| F2-008(b)（R-7 非文字列） | **解消** | 人間決定により `String()` 化。AC-53。ただし名前系フィールドには AC なし（Minor） |
| F2-008(c)（解決の順序） | **部分的** | AC-54 は追加されたが非決定的で違反実装を落とせない（F3-009） |
| F2-008(d)（分離レベル） | **未解消** | 散文規定のみで AC が 1 件もない（F3-009） |
| F2-009（T-2 の逆順ロック） | **部分的** | ABBA デッドロックは解消（両者とも `daily_reports` 先行）。ただし対象日の行が存在しない場合に `FOR UPDATE` が 0 行しかロックせず孤児経路が残存（F3-004） |
| F2-010（R-6 の検証値の食い違い） | **解消** | 列ごとの最終値表・AC-56（250+3+7=260 の算術も確認） |
| F2-011（R-7 の配列要素型） | **解消** | 要素のオブジェクト検証・`date` の文字列検証・AC-60 |
| F2-012（メッセージの到達性） | **部分的** | 「読める」は UI 変更節・AC-61・E2E-4 で担保。「届く」の未確認前提が未決事項表に未登録（F3-003） |
| F2-013（E2E の DB 破壊） | **部分的** | センチネル日付・後始末・`DATABASE_URL` 上書きの 3 点が規定されたが、後始末の手段と失敗時の扱いが未規定、`reuseExistingServer` により上書きが実効しない（F3-007）、E2E-2 が E2E-1 依存（F3-006） |
| F2-014（行番号） | **部分的** | Round 2 指摘の 5 件はすべて正。**新規追加分に 2 件の誤り**（F3-014） |
| F2-017〜F2-019, F2-021〜F2-024, F2-029, F2-032 | **解消** | 各 Minor は該当節への追記で解消 |
| F2-020（終端条件の AC） | **未解消** | AC なし（F3-009 に統合） |
| F2-031, F2-034 | **未解消（文書化のみ）** | lean 運用の Minor として通過 |

## Findings

### Major

#### F3-001: 長さ超過エラーの列挙単位・重複・`projects.name` の扱いが未規定で、完全一致を要求する AC-38・AC-59 の期待値が一意に決まらない
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA F3-A-001）＋ データ量・文字列長（レーンC F3-C-005）
- **該当箇所**: 「エラー処理 > エラーメッセージの規範」構成規則および「名前の長さ超過」書式行／「R-6」検証対象表／AC-38・AC-56・AC-59／R-4 の 3 行目・M-2
- **問題**: (a) **列挙単位の不一致**：24 超ブロックの列挙単位は「該当行」（1 行 1 タスク）だが、長さ超過ブロックの単位は「項目」である。にもかかわらず構成規則は「該当行の順序は、日報のプロジェクトブロック順・その中のタスク行順とする」と行単位でしか順序を定義していない。1 つの対象行で取引先名とタスク名が同時に 255 文字超になった場合、出力が 2 行になるのか、どちらを先に置くのかが決まらない。(b) **重複の扱い**：`clients.name` はプロジェクトブロック単位の値であるのに対し R-6 の検証は「対象行」単位である。同一ブロックに対象行が 3 行あって取引先名が 300 文字なら、同一内容の行が 3 回出るのか 1 回に統合されるのかが未規定。(c) **DB に書かれない値の検証**：R-6 本文は「検証する値は、各列へ実際に書き込む最終的な文字列とする」と宣言する一方、直後の表は `projects.name` の検証対象を `normalizeName(Task.label)` と無条件に挙げている。取引先名が空の行では `projects` レコードは作られず（R-4 3 行目・M-2）、この値は DB へ一度も送られない。したがって「取引先名が空・プロジェクト名 300 文字」の行で `プロジェクト名:` 行を出すのか、連結後の `タスク名:` 行だけを出すのかが、宣言文と表のどちらを採るかで割れる。
- **影響**: AC-38 は `Error.message` の完全一致、AC-59 は両ブロックの全件包含を要求する。Phase 7 で人間がテストを凍結し Phase 8 はテスト修正禁止であるため、凍結した期待文字列と実装の列挙方法が食い違うと収束ループが空転し人間エスカレーションになる。これは Round 1 F-010・Round 2 F2-003 が指摘した障害モードが長さ超過ブロックについて解消していないことを示す。AC-56（PJ名 250 文字）は 250 ≤ 255 のため `projects.name` の検証有無で結果が変わらず、**この分岐を素通りする**。
- **失敗シナリオ**: 取引先名 300 文字のブロックに対象行が 2 行ある日報を保存 → `test-builder` は「見出し 1 行＋`取引先名:` 1 行」を期待値に固定 → Phase 7 で凍結 → `impl-builder` が対象行ごとに素直に実行し `取引先名:` を 2 行出力 → 完全一致にならず Green にできず、テストも直せない。
- **推奨対応**: 長さ超過ブロックの列挙単位を「（項目名, 最終値）の一意な組の集合」と規定して重複を排除する。同一行内の順序を `取引先名` → `プロジェクト名` → `タスク名` に固定する。`projects.name` の検証を「`projects` レコードを実際に作成する行に限る」と明記して本文と表を一致させる。該当 2 件・同一項目重複ありの完全な例をテンプレートに追加する。「取引先が空・プロジェクト名 300 文字」を AC に追加してこの分岐を機械的に固定する。
- **仕様決定の要否**: 不要

#### F3-002: 「規範」と明示した完全な例に置換記号が混入しており、省略記号を出力するか否か・30 文字の計数単位が決まらない
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA F3-A-002）
- **該当箇所**: 「エラー処理 > エラーメッセージの規範」冒頭（「以下を**規範**とする（例示ではない）」）／「名前の長さ超過（該当 1 件の場合の完全な例）」ブロック／直後の書式行／AC-38
- **問題**: 24 超ブロックは実値のみからなる完全な例だが、長さ超過ブロックの「完全な例」は `タスク名: 「あああ…（先頭30文字）」（312文字）` であり、`…（先頭30文字）` が**出力文字列の一部なのか読み手向けの置換記号なのかが判別できない**。直後の書式行 `<項目名>: 「<最終値の先頭30文字>」（<最終値の文字数>文字）` は省略記号を含まないため、例と書式行が食い違っている。あわせて「先頭 30 文字」の計数単位が未規定である（R-6 は 255 について `String.prototype.length` を明記したが 30 については無規定）ため、サロゲートペアが 30 文字目で分割される場合の扱いも決まらない。
- **影響**: AC-38 は完全一致を要求するため、`test-builder` が期待値を独自に決めることになる。凍結後に実装と食い違えば F3-001 と同じ経路で人間エスカレーションに至る。規範節が「例示ではない」と宣言していることが、この曖昧さを見えにくくしている。
- **推奨対応**: 長さ超過の「完全な例」を、実際に 30 文字並べた文字列のみからなる例に差し替える（読み手向けの注記は例ブロックの外に置く）。省略記号を出す設計にするなら書式行にも含める。「先頭 30 文字」の計数単位を R-6 と同じ `String.prototype.length` に揃え、サロゲートペア分割時の扱いを明記する。
- **仕様決定の要否**: 不要

#### F3-003: AC-61 の観測手段が未定義。かつ「RPC 境界でのメッセージ保持」という未確認前提が未決事項表に登録されていない
- **観点 / レーン**: 受け入れ条件の検証可能性（レーンA F3-A-003）＋ 要件の網羅性（レーンB F3-B-004）
- **該当箇所**: AC-61／「UI の変更（エラー表示）」変更内容表／「検証の範囲」節／エラー処理表の「画面表示」欄／例外系 E-3・E-4／未決事項表／E2E-4。実体は `apps/web/src/routes/index.tsx:104,110-111,117-120,217`、`apps/web/src/styles/app.css:14-17`
- **問題**: (1) **観測手段が未定義**。「エラー色」がどのトークン／クラスを指すのか特定されていない。本リポジトリの CSS 変数は `--color-danger` / `--color-danger-dim` / `--color-success` / `--color-warn` の 4 種で、`error` という名前のトークンは存在しない。E2E がクラス名を見るのか computed color を見るのかも書かれていない。「自動的に消えない」も何秒待って残存を確認すれば判定成立なのかが決まらない。「改行が保持され」も DOM テキストに `\n` が現れることを見るのか `white-space` の計算値を見るのかが決まらない。(2) **未確認前提が未決事項表にない**。設計書は「保持されないことが判明した場合、…設計の見直しが発生する」と自ら書きながら、この論点を未決事項表に 1 行も載せていない。承認者は未決事項表と変更点表を判断材料にするため、表に載らない未確認前提は「本文が確定的＝もう決まっている」という誤読を再生産する（Round 1 F-013・Round 2 F2-004 と同じ構造）。(3) エラー処理表の「画面表示」欄は依然「`保存エラー: <メッセージ>` をエラー色・改行保持・自動消去なしで表示」と**断定形**であり、「検証の範囲」節の「未検証」と食い違う。(4) AC-61・E2E-4 の「改行保持」は、メッセージ本文が到達することを前提にしている。到達しなければ `保存エラー` の 1 行になり、`white-space: pre-line` を実装してもしなくても観測結果が同一になって判定能力を失う。
- **影響**: R-6 が「切り詰めではなくエラー」を選んだ根拠は「エラーであれば利用者が入力を修正できる」ことであり、規範メッセージが利用者に届くことに全面的に依存している。届かなければ、新規に導入した 3 つの保存全体失敗パスに対して利用者に出るのは `保存エラー` の 4 文字だけになる。E2E-4 は AC-61 を検証する唯一のテストであり、実装承認ゲートがスキップされているため Phase 10 の実行検証が最後の砦である。
- **推奨対応**: (a) AC-61 のエラー色を既存トークンで名指しし、E2E の観測手段と自動消去の判定条件を AC に書く。(b) RPC 境界の到達性を未決事項表に登録し、否決時の代替案（戻り値に `errorMessage` を含める形。`storage.ts:57-65` の 1 箇所の変更で済む）と切替コストを併記する。(c) エラー処理表の断定表現を整合させる。
- **仕様決定の要否**: 要（レーンA・レーンB とも）→ **司令塔の裁定: 不要（実装から確定済み）**。司令塔が TanStack Start の実装を追跡して検証した：サーバー側 `node_modules/@tanstack/start-server-core/dist/esm/server-functions-handler.js` の catch 節が `toCrossJSONAsync(error, { plugins: serovalPlugins })` でエラーを seroval シリアライズし `x-tss-serialized` ヘッダ付きで返す。クライアント側 `node_modules/@tanstack/start-client-core/dist/esm/client-rpc/serverFnFetcher.js:129-131` が `fromCrossJSON` で復元し `if (result instanceof Error) throw result` を実行する。seroval の実挙動も Node で実測し、**改行と日本語を含む `Error.message` が完全一致で復元される**ことを確認した（`revived.message === err.message` が true）。したがってメッセージは改行ごとクライアントに到達する。未決事項への登録ではなく、根拠を明記して留保を解消する

#### F3-004: T-2 の `SELECT ... FOR UPDATE` は対象日の `daily_reports` 行が存在しない場合 0 行しかロックせず、孤児 `time_entries` の経路が残存する
- **観点 / レーン**: 並行・削除の巻き込み（レーンC F3-C-001。F2-009 の未解消部分）
- **該当箇所**: 「T-2」手順1〜3 および「手順 1 を置く理由」／「解決の順序」／AC-55
- **問題**: 設計書は「`SELECT ... FROM daily_reports WHERE date = <対象日> FOR UPDATE`（対象日の行ロックを獲得し、T-1 と直列化する）」と断定するが、**行ロックは行が存在する場合にのみ獲得できる**。対象日に `daily_reports` 行が無い状態（初回保存と削除が競合するケース）では `FOR UPDATE` は 0 行を返しロックを 1 つも取らない。ロックを取らない以上 T-1 との直列化は成立せず、T-1 の `ON CONFLICT DO UPDATE` が獲得する投機的挿入トークンとも待ち合わせない（`FOR UPDATE` は既存タプルのみを対象とし投機的挿入トークンを待たない）。設計書には「行が存在しない場合」への言及が一切ない。
- **影響**: READ COMMITTED では文ごとに新しいスナップショットを取るため、`daily_reports` が存在しない日付に `time_entries` だけが残る。両者に外部キーは無く検出も修復もできない。削除したはずの日の工数が集計に残り続ける。T-2 手順1 を追加した唯一の目的（AC-55 の孤児化防止）が、初回保存と競合する場合に達成されない。AC-55 は「同一日付に対する保存と削除を同時に実行しても」としか書いておらず、**対象日に既存行がある状態で書かれるのが自然**であるため、この経路はテストで検出されない。
- **失敗シナリオ**: 対象日 X に `daily_reports` 行も `time_entries` 行も無い状態。①T-2: `SELECT ... FOR UPDATE` → 0 行・ロック無し。②T-1: `INSERT daily_reports ... ON CONFLICT DO UPDATE`（未コミット）→ `DELETE time_entries`（0 行）→ マスタ解決 → `INSERT time_entries`（3 行・未コミット）。③T-2: `DELETE FROM time_entries WHERE date = X` → 新しいスナップショットからは T-1 の未コミット行が見えないため 0 行削除（不可視タプルなのでロック競合も起きず待たされない）。④T-1: COMMIT。⑤T-2: `DELETE FROM daily_reports WHERE date = X` → コミット済み行が見えるため 1 行削除 → COMMIT。最終状態：`daily_reports` に X の行は無く `time_entries` に X の 3 行が残る＝孤児。
- **推奨対応**: T-2 手順1 を「行が存在しない場合でも T-1 と直列化される手段」に置き換える。(a) T-2 も `daily_reports` に対して `INSERT ... ON CONFLICT` を先に発行して投機的挿入トークンまで含めた待ち合わせを起こす、(b) `pg_advisory_xact_lock(hashtext(<対象日>))` のように行の有無に依存しないロックを T-1・T-2 の双方の先頭に置く、のいずれか。AC-55 の前提に「対象日に `daily_reports` 行が存在しない状態から開始する」ケースを明示的に追加する。
- **仕様決定の要否**: 不要

#### F3-005: E2E-4 の期待結果「再読み込み後も当該日報が保存されていない」は画面で観測できず、設計自身の「画面で観測できる事実に限定」規定に違反する
- **観点 / レーン**: E2E の検証可能性（レーンC F3-C-002 ＋ レーンB F3-B-001）
- **該当箇所**: 「E2E シナリオの洗い出し > 自動化対象」E2E-4／同節「検証手段の規定」／AC-61。実体は `apps/web/src/routes/index.tsx:54-92`（初期化）と `:95-100`（オートセーブ）、`apps/web/src/routes/timesheet.tsx:22-32`
- **問題**: 日報入力画面は **DB を一切読まない**。`index.tsx:54-92` の初期化は `sessionStorage`（autosave）→ `localStorage`（テンプレート）の順にしか参照せず、`reportStorage.getAll()` / `getByMonth()` は呼ばれていない。さらに `:95-100` が 500ms デバウンスで入力内容を sessionStorage に書き続けるため、`page.reload()` 後に画面へ復元されるのは**保存に失敗した入力内容そのもの**であり、DB に行が作られたか否かとまったく無関係に同じ状態になる。すなわちこの期待結果は判定能力がゼロである。DB を読む画面は `/timesheet` のみだが、初期表示は今日を含む当月であり、センチネル日付（過去日）は別月のため月送り操作が必要で、E2E-4 はその手順を規定していない。
- **影響**: E2E-4 は AC-61 を検証する唯一のテストであり、新規に導入した 3 つの保存全体失敗パスを画面側で押さえる唯一の E2E である。E2E は Phase 5 で仕様凍結され Phase 10 でコード化されるため、この矛盾は Phase 10 で初めて露見する。そのとき戻り先は Phase 5 ないし Phase 1 となり、`_flow-config.md` の代償措置により人間エスカレーションが確定する。
- **失敗シナリオ**: Phase 5 で E2E-4 を「保存 → エラー文言確認 → `page.reload()` → 保存されていないことを確認」として凍結 → Phase 10 でコード化 → reload 後の画面には実績h `25` がそのまま復元されており観測点が存在しないことが判明 → 凍結済み仕様を変更できず収束ループが空転。
- **推奨対応**: E2E-4 の後段の期待結果を、(a) 画面で観測できる事実に置き換える（例：エラー文言が表示されたのち「保存しました ✓」が一度も表示されないこと）、(b) `/timesheet` へ遷移して当該月まで月送りする手順まで規定する、(c) 「保存されていないこと」の検証を結合(DB込み)（AC-36）に委ね E2E-4 の期待結果から外す、のいずれかに確定する。あわせて「日報入力画面は DB を読まないため、画面の再読み込みは DB 状態の観測手段にならない」ことを明記する（E2E-2 の「再読み込みして再度保存」も同じ誤解を招く）。
- **仕様決定の要否**: 不要

#### F3-006: E2E-2 が E2E-1 の実行結果・同一日付に依存しており、`fullyParallel: true`・設計自身の「シナリオごとに異なる日付」規定・AGENTS.md「テスト間の依存を作らない」と三重に矛盾する
- **観点 / レーン**: 並行（テスト間干渉）（レーンC F3-C-003 ＋ レーンB F3-B-002）
- **該当箇所**: 「テストデータの分離（必須）」規定 1／E2E-1／E2E-2。実体は `apps/web/playwright.config.ts:14`（`fullyParallel: true`）・`:17`（`workers`）、`AGENTS.md`「テスト間の依存を作らない（実行順序に依存しない設計）」
- **問題**: 規定 1 は「シナリオごとに異なる日付を割り当て、`fullyParallel: true` での相互干渉を避ける」と定めているが、E2E-2 は「**E2E-1 の直後に**ページを再読み込みし、**同じセンチネル日付で**再度保存する」と定義されており、(a) 同一日付を使うこと、(b) E2E-1 の実行完了後であること、(c) E2E-1 が作った画面状態が引き継がれること、の 3 つに依存している。3 つとも成立しない。Playwright は `test()` ごとに独立した BrowserContext を与えるため sessionStorage は引き継がれず、E2E-2 の初期状態は `defaultDailyReport()`＝**今日の日付・空のタスク行 1 件**になる。`fullyParallel: true` により実行順序も保証されない。
- **影響**: E2E-2 は冪等性を画面側で押さえる唯一のシナリオである。仕様のまま凍結されると Phase 10 で書き換えが必要になり、凍結済み仕様の変更＝人間エスカレーションになる。加えて、後始末が並列に走る 2 テストで同一日付を対象にするため相互破壊が「正常」として凍結される。
- **失敗シナリオ**: ローカルで並列実行 → E2E-2 が先に起動し、`defaultDailyReport()` の**今日の日付**のまま保存（センチネル日付を入力する手順が E2E-2 の定義に無い）→ 開発用 DB の当日の `daily_reports` が初期状態で上書きされ当日の `time_entries` が全削除される → 「保存しました ✓」が出るのでテストは PASS。F2-013 で対処したはずの事故が E2E-2 の経路で再現する。
- **推奨対応**: E2E-2 を自己完結させる（専用のセンチネル日付を入力して保存し、続けてもう一度保存する）。規定 1 に「各シナリオは他シナリオの実行結果・画面状態に依存してはならない（Playwright はテストごとに独立した BrowserContext を与えるため sessionStorage は引き継がれない）」を明記する。
- **仕様決定の要否**: 不要

#### F3-007: E2E の後始末の実行手段・失敗時の扱いが未規定で、かつ `reuseExistingServer` により「`DATABASE_URL` の上書き」という唯一の分離手段が実効しない
- **観点 / レーン**: 削除の巻き込み・異常系（レーンC F3-C-004 ＋ レーンB F3-B-012）
- **該当箇所**: 「テストデータの分離（必須）」2・3。実体は `apps/web/playwright.config.ts:31-35`（`webServer.command: 'npm run dev'` / `reuseExistingServer: !process.env.CI`）、`apps/web/src/test/db-helpers.ts`、`apps/web/e2e/auth.setup.ts:20`
- **問題**: (1) **後始末の実行手段が未規定**：Playwright のテストプロセスからどうやって DB に到達するか（`src/test/db-helpers.ts` の `testDb` を import するのか、別接続を張るのか）が書かれていない。`e2e/auth.setup.ts:20` に `src/server/db` の `db` を E2E プロセスから直接使う前例があるため実現は可能だが、設計書はどちらの手段を採るか示していない。(2) **後始末の失敗時・テストが途中で失敗した場合の規定がない**：`test.afterEach` で必ず実行するのかテスト本文末尾に置くのかで挙動が変わる。本文末尾ではアサーション失敗時に後始末へ到達せずデータが残る。残骸があると次回実行時に E2E-1 が既存行の上書きになり、E2E-4 は前回の残骸を検出して誤判定する。CI は `retries: 2` のためリトライ時にこの状態が確実に発生する。(3) **`DATABASE_URL` 上書きが実効しない**：DB へ接続するのは Playwright のテストプロセスではなく `webServer` が起動する**アプリサーバープロセス**である。`reuseExistingServer: !process.env.CI` により、ローカルで開発サーバーが起動済みの場合（開発中の常態）Playwright は新しいプロセスを起動せず既存サーバーを再利用する。既存サーバーの環境変数は変わらないため、保存先は開発用 DB のままである。
- **影響**: 本節は F2-013 への対処として新設されたものであり、その対処が (2)(3) の穴によって実効性を失う。とくに (3) は「分離したつもり」という誤った安心を与えるため、無規定より危険である。
- **失敗シナリオ**: 開発者が `npm run dev` を起動したまま `DATABASE_URL=... npm run test:e2e` を実行 → `reuseExistingServer` が真なので既存の開発サーバー（開発用 DB 接続）が使われる → E2E がセンチネル日付に書き込み、その日付の `time_entries` を全削除 → 後始末は Playwright プロセス側の `DATABASE_URL`（テスト用 DB）に対して実行されるため**開発用 DB の残骸は消えない**。
- **推奨対応**: (1) 後始末の実行手段を明記する（`truncateTables()` は使わず日付を絞った DELETE にすることを含める）。(2) 後始末は `test.afterEach`（またはワーカースコープの fixture）で成否によらず必ず実行することを規定する。(3) `DATABASE_URL` を上書きする場合は `reuseExistingServer` を無効にするか別ポートで明示起動する必要があることを規定に書き添える。書かないなら規定3 自体を「開発用 DB で実行することを前提とし、センチネル日付と後始末で保護する」に一本化する。
- **仕様決定の要否**: 不要

#### F3-008: 成功保存の直後に失敗が起きると、キャンセルされていない `setTimeout` がエラーメッセージを消すため AC-61 の「自動的に消えない」が成立しない
- **観点 / レーン**: 状態管理の異常系（レーンC F3-C-006）
- **該当箇所**: 「UI の変更（エラー表示）」変更内容表／AC-61／E2E-4。実体は `apps/web/src/routes/index.tsx:48`（`savedMsg` state）、`:102-106`（`saveTemplate`）、`:108-121`（`saveReport`）
- **問題**: 現行実装は `savedMsg` という単一の state を `saveTemplate` と `saveReport`（さらに日付未入力パス）が共有し、いずれも `setTimeout(() => setSavedMsg(''), 2000)` を**タイマー ID を保持せずに**発火する。設計書は「失敗時は自動消去しない」とだけ規定し、**先行して発火済みのタイマーをキャンセルする規定を置いていない**。したがって、成功保存の 2 秒以内に次の保存が失敗した場合、先行タイマーが満了して `setSavedMsg('')` を実行し、表示したばかりのエラーメッセージが消える。「テンプレ保存 → 2 秒以内に日報保存が失敗」でも同じ。
- **影響**: 3 つの保存不能パスに対する唯一の代償措置が「利用者が原因を認識して入力を修正できるエラー表示」であり、エラーが 2 秒で消える経路が残るとこれが成立しない。しかも発現するのは「保存に成功した直後、値を直して再保存したら失敗した」という**もっとも起こりやすい操作列**である。AC-61 も E2E-4 も単発の失敗保存しか想定していないため、この経路はテストで検出されない。
- **推奨対応**: 「メッセージを更新する際は、先行して予約された自動消去を必ず取り消す（タイマー ID を保持して `clearTimeout` するか `useEffect` の cleanup で解除する）」ことを規定として追加する。`savedMsg` を `saveTemplate` / 日付未入力パスと共有していることから生じる副作用を、変更範囲に含めるか対象外とするかを明示する。AC-61 に「成功保存の直後に失敗した場合も、エラー表示は自動的に消えない」を追加する。
- **仕様決定の要否**: 不要

#### F3-009: 並行系 AC（AC-45・AC-54・AC-55）が非決定的で違反実装を落とせず、「解決の順序固定」「テーブル間ロック順序」「分離レベル」「終端条件」には AC が 1 件もない
- **観点 / レーン**: 要件の網羅性・テストレベル判定の妥当性（レーンB F3-B-003 ＋ F3-B-013）
- **該当箇所**: 「解決の順序（デッドロック回避のため固定する）」／「解決の共通方針 > 分離レベル・終端条件」／「T-3」／AC-45・AC-54・AC-55／受け入れ条件冒頭の「Phase 5 のテスト観点はこの条件を唯一の根拠として起案される」
- **問題**: 追加された 3 つの並行系 AC はいずれも「特定の interleaving が起きたときにだけ違反が顕在化する」性質を期待値にしており、テストからその interleaving を強制する手段が設計書のどこにも規定されていない。AC-54（新規取引先 A・B を逆順に含む別日付の日報を同時保存 → デッドロックしない）は、順序を固定していない実装でも、2 つのトランザクションが実際に重なり双方が相手のマスタ挿入を未コミットで待つ瞬間が生じなければデッドロックしない。`Promise.all` では重なりを制御できず、順序を守らない実装が常に PASS しうる。AC-45・AC-55 も同様で、T-1 手順1 や T-2 手順1 を省いた実装が高確率で PASS する。さらに「テーブル間のロック順序を固定する」「マスタは名前の昇順に解決する（行ごとに逐次解決してはならない）」「分離レベルを変更してはならない」「再 SELECT が 0 件なら 1 回だけやり直す」の 4 規定には**対応する AC が 1 件も存在しない**（AC-01〜AC-61 を総当たりで確認）。
- **影響**: 行ごとに逐次解決する実装・テーブル順序を守らない実装・`db.transaction({ isolationLevel: 'repeatable read' })` を書いた実装・無限リトライ実装のいずれも、AC を全件 PASS したまま Phase 8・Phase 10 を通過する。実装承認ゲートがスキップされており Phase 10 の機械ゲートが最後の砦であるため、通過すると止める手段がない。現れる障害は (1) 本番でのみ間欠的に起きる `保存エラー: deadlock detected`、(2) REPEATABLE READ 環境での再 SELECT 空振りによる保存失敗、(3) 保存と削除が交差したときの孤児 `time_entries`。加えて非決定的な AC を結合(DB込み)テストとして凍結すること自体が CI に恒常的な flaky test を持ち込む。結合(DB込み)は人間本記載であり、人間が「どう書けば違反実装を落とせるか」の手掛かりを設計書から得られない。
- **失敗シナリオ**: `impl-builder` が対象行を走査しながら行ごとにマスタを解決する素直な実装（順序固定なし）を書く → AC-54 のテストは `Promise.all` で 2 件の保存を投げるが、実行時間の都合で 2 つのトランザクションが重ならず双方成功 → PASS → 運用開始 → 月初に別タブで 2 日分をまとめて保存した際にデッドロックし片方の日報が失われる。
- **推奨対応**: 非決定的な interleaving に依存しない、観測可能な事実に対する AC を追加する。①**順序の観測**：正規化後の名前が `"Z社"` → `"A社"` の順に並んだ日報を 1 回保存し、`clients` の作成順（`created_at` 昇順または挿入順）が名前の昇順と一致することを検証する AC。単一トランザクションで決定的に判定でき、行ごとの逐次解決実装を確実に落とす。②**テーブル順序の観測**：結合(内部・モック)で発行される文の順序が規定順であることを検証する AC。③**分離レベルの観測**：トランザクション内で `SHOW transaction_isolation` が `read committed` を返すことを検証する AC。④AC-45・AC-55 は、テスト側が別接続で対象日をロックしたうえで関数を呼び、ロック解放まで進行しないことを確認する形に期待値を書き換える。⑤終端条件（1 回だけ再試行）を検証する AC。あわせてこれらの検証手段を設計書側で規定し、人間本記載の記入例に含める。
- **仕様決定の要否**: 不要

### Minor（1行のみ）

- F3-010: R-2 の判定4 が「上記以外」という受動的定義のため、判定1（NaN）を省いた実装では NaN が判定2・判定3 を素通りする（判定4 を能動条件で定義すべき）（R-2 判定表）
- F3-011: ロック順序 `daily_reports → time_entries → clients → projects → tasks` を T-1 自身の手順（1. daily_reports → 2. time_entries DELETE → 3. マスタ → 4. time_entries INSERT）が破っており、規範として実行不能（実害は無いことをレーンC が確認済み）（解決の順序／T-1）
- F3-012: 「昇順（コードポイント順）」の規定は JS の `Array.prototype.sort()` 既定（UTF-16 コード単位順）と補助面文字で不一致（全 TX が同一実装を使う限り実害なし）（解決の順序）
- F3-013: T-2 の `DELETE FROM time_entries` にも未決事項 No.2 の注記が必要（T-1 側にしかない）（T-2）
- F3-014: 参照行番号の誤りが新規追加分に 2 件（`playwright.config.ts:16` → 実際は 14 行が `fullyParallel`、`lib/defaults.ts:26` → 実際は 28 行が `date:`。2レーンが独立に検出、司令塔も確認済み）（E2E テストデータの分離）
- F3-015: 目的節の「実績h が**有効な行**のうち、丸めると 24 時間を超えるもの」は共通前提の定義（有効な行＝判定4）と矛盾し、字義どおりでは空集合になる（目的／共通前提）
- F3-016: 目的節「それ以外の入力は、本変更の前後で保存の成否が変わらない」は、直列化待ち・デッドロック・接続プール占有により同一入力でも成否が変わりうる点を除外していない（目的／E-5）
- F3-017: AC-02「PostgreSQL が丸めた値と一致」・AC-03「格納値（丸め後）の合計と一致」は DB の値を読んで自分自身と比較するトートロジーになりうる（具体値の固定は AC-31/32/33 のみ）（AC-02／AC-03）
- F3-018: AC-05 の `total_work_hours` は実装の `String(Math.max(0, workHours))`（`reports.ts:76,89`）のクランプを反映しておらず、終業<始業では一致しない（AC-05）
- F3-019: R-3 の `toFixed` 不採用理由「いずれも `2.675 * 100` が…になるため」は `toFixed` の機序ではない（乗算を経ずに double の厳密値を丸める）（R-3）
- F3-020: 変更点表 #4 の引用が要件メモ原文の逐語ではない／前文「9 件を照合した結果 4 件が変更」は行数 4・対象 3 件で単位が一致しない（要件メモからの変更点）
- F3-021: R-1 の全角スペース畳み込みが「Round 1 で人間が承認した AC-16 の内容そのもの」と主張されているが、人間決定表に対応行がない（要件メモからの変更点／人間決定表）
- F3-022: エラー表示 UI の変更が要件メモ決定事項 #1「画面・帳票の変更は含めない」の射程に入るか未整理で、変更点表に行がない（要件メモからの変更点／UI の変更）
- F3-023: 24 超メッセージの `<入力された文字列そのまま>` は非文字列入力（AC-53）での表示が未規定、かつ長さ無制限（長さ超過ブロックが先頭 30 文字に切るのと不揃い）（エラーメッセージの規範）
- F3-024: AC-51／AC-52 は「同一取引先内」「同一プロジェクト内」という比較の前提が明記されておらず、異なる親で書くと AC-19 と衝突する読みが残る（AC-51／AC-52）
- F3-025: AC-59 だけが「含まれる」判定で、規範が定めるブロック順序を固定していない（AC-37・AC-38 は完全一致）（AC-59）
- F3-026: `savedMsg` を共有する他 2 経路（`index.tsx:110` の日付未入力、`:104` のテンプレ保存失敗 — 後者は E2E-4 が keyword とする `保存エラー` と同一文字列）の扱いが未規定（UI の変更）
- F3-027: R-7 の `String()` 化により名前系フィールドにオブジェクトが届くと `"[object Object]"` がマスタ名として作成され、マスタ管理画面が非ゴールのため手動削除もできない（R-7）
- F3-028: R-7 の名前系フィールドの `String()` 化・`null`/`undefined`→空文字を固定する AC がなく、`actualHours` だけ文字列化する実装が全 AC を PASS する（R-7／AC-53）
- F3-029: R-2 判定 2 の下限境界（`0.005` ちょうどは対象行で `0.01` として格納）を固定する AC がなく、上限が AC-32〜34 で三重に固定されているのと非対称（R-2／AC-30）
- F3-030: `time_entries` の「1 回の複数行 INSERT」はバインドパラメータ上限 65535 に対し 1 行 3 パラメータのため約 21,845 行で失敗するが、行数上限を設けない規定と併存（非機能要件／T-1 手順4）
- F3-031: センチネル日付が具体値で規定されておらず、CI の `retries: 2` でのリトライ時に前回実行の残骸をどう扱うかも未規定（E2E テストデータの分離）
- F3-032: 判定結果表が R-6・R-7 を単体テスト対象の「純粋関数」として挙げる一方、T-1 はこれらをトランザクション外手順としてのみ規定し、独立モジュールとして切り出す規定がない（判定結果／T-1）
- F3-033: `schema/reports.ts` と `functions/reports.ts` を同じ `reports.ts` として参照しており紛らわしい（背景／ER 図ほか）
- F3-034: 「解決の共通方針 > 終端条件」に対応する AC がない（F3-009 に統合。無限リトライ実装も再試行ゼロ実装も全 AC を PASS する）（解決の共通方針）

## 司令塔による裁定の記録（Round 3）

| 項目 | レーン間の相違 | 裁定 | 根拠 |
|---|---|---|---|
| F3-003（RPC 境界の到達性） | レーンA・B とも「仕様決定＝要」（人間の判断が必要）と判定 | **不要（実装から確定）** | 司令塔が TanStack Start の実装を追跡し、サーバー側の seroval シリアライズ（`server-functions-handler.js` の catch 節）とクライアント側の復元・再 throw（`serverFnFetcher.js:129-131`）を確認。さらに seroval の実挙動を Node で実測し、改行と日本語を含む `Error.message` が完全一致で復元されることを検証した。未確認前提ではなくなったため未決事項への登録は行わず、根拠を明記して留保を解消する |
| F3-005（E2E-4 の観測点） | レーンC は「(a)(b)(c) のいずれかに確定」、レーンB も同様の3案 | **(c) を採用** | 「保存されていないこと」の検証は結合(DB込み)（AC-36）に委ね、E2E-4 の期待結果は画面で観測できる事実に限定する。設計書自身の「検証手段の規定」と整合する |
