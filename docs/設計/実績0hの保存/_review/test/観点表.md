# 実績0hの保存 テスト観点表

- 機能：実績0hの保存
- 作成日：2026-08-16
- 根拠：`docs/設計/実績0hの保存/実績0hの保存_設計書.md`（Phase 3 承認済み・合意済）
- 様式：`docs/開発プロセス/テスト観点表テンプレート.md`
- フロー設定：Phase 7（テスト承認）＝**実施**（ゲート自体は維持）
- **確定状態：Phase 7 承認（2026-08-16）**。1章本記載は人間判断でスキップ。2章は親 E2E-1〜7 維持（新規起案なし）を承認。実装計画 5.5 の新規結合はコード化しない

## 0. この表の読み方

- **1章**は人間本記載方式で記入例のみ作成した。**2026-08-16 に人間が本記載をスキップ**したため、空欄は埋めない。5.5 新規結合もコード化しない。
- **2章**は設計書「テストレベル判定」どおり、本差分の新規 E2E シナリオは起案しない。親機能の E2E-1〜7 を維持する方針を起案として示す。凍結は人間の承認後。
- テストデータの分離は親機能の観点表 1.0 節（`docs/設計/工数実績の正規化保存/_review/test/観点表.md`）を継承する。`report-db-helpers.ts` 経由。`truncateTables()` は使わない。

### 該当トリガー（設計書「テストレベル判定」節より）

| 種別 | トリガー | 該当 |
|---|---|---|
| 影響度 | 認可境界 | 非該当 |
| 影響度 | PII 取扱い | 非該当 |
| 影響度 | 認証・セッション | 非該当 |
| 影響度 | **不可逆操作** | **該当**（保存のたびに当該日付の `time_entries` を DELETE する。0h 行の追加・削除もこの経路に乗る） |
| 影響度 | クリティカルパス業務 | **該当**（E2E 必須分は親 E2E-1〜7 の維持。本差分の新規起案はしない） |
| 影響度 | 金銭・契約 | 非該当 |
| 発生可能性 | **DB 制約依存** | **該当**（`chk_hours_non_negative` の許可／拒否と制約名） |
| 発生可能性 | 実 JOIN クエリ | 非該当（本変更単体。新しい JOIN は増えない） |
| 発生可能性 | **トランザクション** | **該当**（再保存の DELETE + INSERT。親の原子性規定を継承） |
| 発生可能性 | 複雑な状態遷移 | 非該当 |

---

## 1. 結合(DB込み)向け 観点表（人間本記載方式）

**記入例以外の「テストケース」「期待結果」「優先度」は人間が埋める。**AI が残りを勝手に決めない。

設計書の AC-Z01〜AC-Z20 は受け入れ条件の列挙であり、本表の観点の取捨を代替しない。人間が本記載するときに参照してよい。

| 観点 | 該当トリガー | テストケース | 期待結果 | 優先度 | 根拠 / 仮置き |
|---|---|---|---|---|---|
| 0h 行の保存が当該日付の既存 `time_entries` を置き換える | 不可逆操作 | Given: 対象行 1 件だけの日報を実績h `"7.5"` で保存済み／When: 同じ行を `"0"` にして再保存する | 当該日付の `time_entries` は 1 件のまま、`hours` は `"0.00"`。旧値 `"7.50"` の行は残らない（AI による記入例） | 高 | 設計書 AC-Z11 |
| 0h 行を空欄に戻した再保存 | 不可逆操作 | Given: 対象行 1 件だけの日報を実績h `"0"` で保存済み／When: 同じ行の実績h を空にして再保存する | 当該日付の `time_entries` は 0 件（AI による記入例） | 高 | 設計書 AC-Z12 |
| 二重実行（冪等性） | 不可逆操作 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「不可逆操作：二重実行」 |
| 部分失敗時のロールバック | 不可逆操作 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「不可逆操作：部分失敗時のロールバック」 |
| 確認ステップの回避 | 不可逆操作 | ＜人間が記入。該当なしなら理由を書く＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「不可逆操作：確認ステップの回避」 |
| 削除済みデータへの再操作 | 不可逆操作 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「不可逆操作：削除済みデータへの再操作」 |
| 0h の新規記録（明示的な 0） | 不可逆操作 | ＜人間が記入。AC-Z01 の 3 表記を別ケースにするかは人間が決める＞ | ＜人間が記入＞ | ＜人間が記入＞ | 設計書 AC-Z01〜Z06・Z09b |
| 記録しない行（空欄・負数・予定hのみ） | 不可逆操作 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | 設計書 AC-Z07〜Z09 |
| マスタ解決（0h でも親と同じ規則） | 不可逆操作 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | 設計書 AC-Z10・Z10b |
| `hours = 0` の直接 INSERT | DB制約依存 | Given: `time_entries` に `hours` として数値 `0` を直接 INSERT する | INSERT は成功し、DB から読んだ `hours` は文字列 `"0.00"`（AI による記入例） | 高 | 設計書 AC-Z17 |
| 格納後も負の値の拒否 | DB制約依存 | Given: `time_entries` に `hours = -0.01` を直接 INSERT する | DB のチェック制約違反になる（AI による記入例） | 高 | 設計書 AC-Z18 |
| 丸め後 0.00 になる直 INSERT | DB制約依存 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | 設計書 AC-Z18b |
| 制約名の入れ替え | DB制約依存 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | 設計書 AC-Z19（`pg_constraint`） |
| 予定側制約の残存 | DB制約依存 | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | 設計書 AC-Z20 |
| ユニーク制約違反時のレスポンス変換 | DB制約依存 | ＜人間が記入。該当なしなら理由を書く＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「DB制約依存：ユニーク制約違反」 |
| 外部キー制約違反時のレスポンス変換 | DB制約依存 | ＜人間が記入。該当なしなら理由を書く＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「DB制約依存：外部キー制約違反」 |
| NOT NULL 違反時のレスポンス変換 | DB制約依存 | ＜人間が記入。該当なしなら理由を書く＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「DB制約依存：NOT NULL 違反」 |
| 再保存の原子性（7.5→0） | トランザクション | Given: 対象行 1 件だけの日報を `"7.5"` で保存済み／When: `"0"` で再保存する | DELETE と INSERT が同一トランザクションに乗り、完了後は 1 件・`"0.00"`（AI による記入例。AC-Z11 と重なる） | 高 | 設計書 AC-Z11・トランザクション節 |
| 途中失敗時の原子性 | トランザクション | ＜人間が記入＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「トランザクション：途中失敗時の原子性」。設計書 AC-Z13（長さ超過で `daily_reports` / `time_entries` 非更新） |
| 並行実行時の競合 | トランザクション | ＜人間が記入。該当なしなら理由を書く＞ | ＜人間が記入＞ | ＜人間が記入＞ | チェックリスト「トランザクション：並行実行時の競合」 |

### 1章で人間に決めてほしいこと（例）

- AC-Z01 の `"0"` / `"0.0"` / `"0h"` を結合(DB込み)でも 3 ケースに分けるか
- AC-Z04（`"0x10"`）・AC-Z05（`"-0"`）・AC-Z06（数値 `0`）を結合に載せるか、単体の分類テストで足りるか
- AC-Z13 の「DB 非更新」を結合で見るか（内部モックは Group B でメッセージ一致だけ見る）
- 親のセンチネル日付・接頭辞規則をどの日付帯に割り当てるか（親の `2000-01-*` / `2000-02-*` / `2000-03-*` と衝突しないこと）

---

## 2. E2E（クリティカルパス）向け 観点表（AI起案）

設計書「テストレベル判定」：本差分は新しい画面導線を足さない。`time_entries` の `0.00` は画面から観測できない。**E2E 必須分は親 E2E-1〜7 の維持で満たす。本差分の新規シナリオ起案・凍結は再実行しない。**

したがって本表 2章の起案は「新規行を足さない」ことである。親の凍結仕様を再掲する（操作手順・期待結果の変更なし）。

| シナリオ | 該当トリガー | 操作手順 | 期待結果 | 必要なセレクタ仕様 |
|---|---|---|---|---|
| E2E-1（親・維持） | クリティカルパス業務 | ログイン済みで日報入力画面を開き、センチネル日付 `2000-01-01`・取引先・プロジェクト・タスク名・実績h を入力して「日報保存」を押す | 「保存しました ✓」を含むメッセージが表示される | 親設計のセレクタ。本差分で追加しない |
| E2E-2（親・維持） | クリティカルパス業務 | センチネル日付 `2000-01-02` で保存したあと、続けてもう一度「日報保存」を押す | 2 回目も「保存しました ✓」が表示され、エラーにならない | 同上 |
| E2E-3（親・維持） | クリティカルパス業務 | センチネル日付 `2000-01-03` を入力し、実績h を入力せずに「日報保存」を押す | 「保存しました ✓」が表示され、保存エラーにならない | 同上 |
| E2E-4（親・維持） | クリティカルパス業務 | センチネル日付 `2000-01-04` と実績h `25` を入力して「日報保存」を押す | 「保存エラー」を含む文言がエラー色（`text-danger`）・改行保持で表示され、3 秒後も残る | `data-testid="saved-msg"`（親 AC-90） |
| E2E-5（親・維持） | クリティカルパス業務 | センチネル日付 `2000-01-05` で成功保存したあと、2 秒以内に実績h を `25` にして再保存する | エラー表示に切り替わり、3 秒後も残る | 同上 |
| E2E-6（親・維持） | 認証・セッション | センチネル日付 `2000-01-06` で保存を押し、未認証コンテキストから `saveReportFn` / `deleteReportFn` に本文なし POST | いずれも 401 | 親 E2E-6 の URL 導出 |
| E2E-7（親・維持） | クリティカルパス業務 | センチネル日付 `2000-01-07` で成功保存し、追加操作せず待つ | 3 秒後にはメッセージが消えている | `data-testid="saved-msg"` |

**本差分で新規起案しない理由（設計書）：** 画面から `0.00` の格納を観測できない。0h 入力の画面確認は候補カタログ C-Z1〜C-Z3 に置く。Playwright コード化は Phase 5 では行わない（親 7 本の実行は Phase 10）。

**仮置き：なし**（新規シナリオを足さない判断は設計書本文に根拠がある）。

---

## 3. Group B（機械導出・本表の凍結を待たない）

以下は影響度トリガー非該当、または発生可能性のみ／単体・結合内部であり、`test-builder` が設計書・実装計画から機械導出する。1章の人間本記載を待たない。

| 範囲 | 内容 | コード化 |
|---|---|---|
| 単体 | `classifyActualHours` の判定 2 書き換え、AC-Z01 の 3 表記別ケース、境界入力、上限維持 | Phase 5 で実施 |
| 結合（内部） | AC-Z13（0h + 256 文字名の親テンプレート）、AC-Z14（空欄 + 300 文字は長さエラーにしない） | Phase 5 で実施 |
| 既存結合(DB込み)の期待値書き換え | `reports-constraints.integration.test.ts` の `"0.004"` / `"0.0000001"` → `['0.00']`。`reports-common.integration.test.ts` 5-11 を AC-Z16（件数 3・合計 10.18）へ | Phase 5 で実施（設計書 AC が確定しているため） |
| 結合(DB込み)の新規 AC（5.5） | AC-Z01 保存経路、Z07〜Z12、Z17〜Z20 等 | **1章の本記載・凍結後にコード化。今は書かない** |
