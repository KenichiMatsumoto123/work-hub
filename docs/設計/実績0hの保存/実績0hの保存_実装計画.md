# 実績0hの保存 実装計画

## 本書の位置づけ

10フェーズフロー（`docs/開発プロセス/設計から実装までのフロー.md`）に沿う。対象は承認済み差分設計書の**機能全体**である。スキーマ変更と判定変更は同じ保存経路に乗るため、ブランチ分割はしない（`ブランチ運用方針.md` 2.3：既存挙動・DB スキーマ変更は同一スコープ）。

- 運用モード: lean（`_flow-config.md`）
- Phase 4 自体は実施する（本書）。Phase 9 は省略可
- Phase 7（テスト承認）: **実施**（結合(DB込み)は人間本記載）
- 実装承認（Phase 10 完了時）: **スキップ**（代償措置適用）
- 省略不可: Phase 5 / 6 / 8 / 10 と収束ループ

## 作業ステータス凡例

| ステータス | 意味 |
|---|---|
| 完了 | 当該タスクは終わっている |
| 未着手 | これから行う |
| 省略可 | lean により実施しなくてよい（実施してもよい） |
| 実施（人間） | 人間が行うゲート |

## 参照資料一覧

| 資料 | パス |
|---|---|
| 設計書（合意済） | `docs/設計/実績0hの保存/実績0hの保存_設計書.md` |
| 要件メモ | `docs/要件/実績0hの保存_要件メモ.md` |
| フロー設定 | `docs/設計/実績0hの保存/_flow-config.md` |
| Phase 2 findings | `docs/設計/実績0hの保存/_review/spec/findings.md` |
| 親設計 | `docs/設計/工数実績の正規化保存/工数実績の正規化保存_設計書.md` |
| フロー正典 | `docs/開発プロセス/設計から実装までのフロー.md` |
| 判定関数 | `apps/web/src/server/report-normalize.ts` |
| スキーマ | `apps/web/src/server/schema/tasks.ts` |
| 保存経路 | `apps/web/src/server/functions/reports.ts` |
| 結合ヘルパー | `apps/web/src/test/report-db-helpers.ts` |

## レビュー成果物の格納先

| フェーズ | パス |
|---|---|
| Phase 2（設計） | `docs/設計/実績0hの保存/_review/spec/` |
| Phase 5〜7（テスト） | `docs/設計/実績0hの保存/_review/test/` |
| Phase 10（実装） | `docs/設計/実績0hの保存/_review/impl/` |

## 0. 前提タスク（他機能で対応・本機能の着手前提）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 0.1 | 親機能「工数実績の正規化保存」が develop に入っている | `classifyActualHours`・`saveReportFn`・`time_entries` が存在する。本差分はその上書き | 完了 |
| 0.2 | 設計書が合意済 | Phase 3（2026-08-16） | 完了 |

ブロック中の前提は無い。Phase 5 に進んでよい。

## Phase 1. 設計書作成

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 1.1 | 差分設計書を作成する | `実績0hの保存_設計書.md` | 完了 |

## Phase 2. 設計書 敵対的レビュー

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 2.1 | 3レーン並列レビューと収束 | Round 3 で Critical 0・Major 0 | 完了 |

**ゲート:** Critical 0 かつ Major 0（達成済み）

## Phase 3. 人間レビュー（設計書承認ゲート）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 3.1 | 設計書を承認する | ステータス合意済。未決 No.1〜3 は決定済み | 完了 |

## Phase 4. 実装計画書作成

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 4.1 | 本書を作成する | 本ファイル | 完了（本書の確定をもって） |

## Phase 5. テスト作成（Red Phase）

実施者: `test-builder`（fresh context）。司令塔は起動・Red 証拠の独立検証・カタログ追記。

**ゲート:** 新規および書き換えたテストが現行実装で **FAIL**（期待値不一致）。矛盾しない既存テストは **PASS**。import エラーだけで落ちないこと。

設計書「Phase 5 への申し送り」を守る。Phase 8 はテスト変更禁止のため、旧 AC-30 / AC-03 / AC-46 を固定した既存テストは**このフェーズで**新期待値へ書き換える。

### 5.1 環境・観点表（記入例）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.1.1 | 結合(DB込み)観点表の記入例を作る | Phase 7 = 実施のため **人間本記載**。AI は各該当トリガー（不可逆操作・DB制約・トランザクション）につき 1〜2 件の記入例のみ。本体は `docs/設計/実績0hの保存/_review/test/観点表.md`。**2026-08-16 人間判断で本記載スキップ**。5.5 新規結合はコード化しない | スキップ（人間判断） |
| 5.1.2 | クリティカルパス E2E の新規起案はしない | 設計書どおり親 E2E-1〜7 を維持。本差分では起案・凍結を再実行しない | 完了（方針確定） |

### 5.2 単体テスト（`classifyActualHours`）

対象: `apps/web/src/server/report-normalize.test.ts`。`beforeEach` で `vi.clearAllMocks()`。既存の「判定 2（0.005 未満）」ブロックを本設計の分類表に合わせて書き換える。

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.2.1 | 判定 2 を `value < 0` に合わせて書き換える | `"-3"` / `"-0.004"` / `"-Infinity"` は skip。`"0"` / `"0.004"` / `"0x10"` は target（旧テストの反転） | 完了 |
| 5.2.2 | 0 表記の対象行 | AC-Z01 の `"0"` / `"0.0"` / `"0h"` を**別ケース**で target・value `0`（申し送り 7） | 完了 |
| 5.2.3 | 境界・周辺入力 | `" 0"` / `"0.00"` / `"0,5"` / `"-0"` / 数値 `0`（AC-Z05・AC-Z06）。`"0.0000001"` / `"0.000001"` は target | 完了 |
| 5.2.4 | 上限は変えない | `"24.005"` / `"Infinity"` は over のまま（AC-Z15）。空欄・`"abc"` は skip のまま（AC-Z07） | 完了 |

### 5.3 結合（内部・モック）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.3.1 | 0h 行の名前長さ超過 | AC-Z13。`saveReportFn` を直接呼び、`Error.message` が親テンプレートに完全一致。実績h `"0"` と `"0.004"` の両方 | 完了 |
| 5.3.2 | 空欄＋長い名前は成功 | AC-Z14。現行どおり throw しない | 完了（現行でも PASS。AC-48 の再確認） |

### 5.4 既存結合(DB込み)の期待値書き換え

`report-db-helpers.ts` 経由。センチネル日付・接頭辞分離は親と同じ。

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.4.1 | 制約境界ケースを書き換える | `reports-constraints.integration.test.ts` の `"0.004"` / `"0.0000001"` を空配列から `['0.00']` へ（AC-Z02・AC-Z03）。`"0.005"` → `0.01`、`"24.005"` 失敗は維持 | 完了 |
| 5.4.2 | AC-03 相当を AC-Z16 へ | 4 行（`7.5` / `2.675` / `0.004` / 空）で件数 **3**・合計 `10.18` | 完了 |

### 5.5 結合(DB込み)の新規 AC（人間本記載後にコード化）

AI は観点表の記入例のみ。本記載と Adversary 収束のあと、test-builder がコード化する。対応関係:

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.5.1 | 0h の記録 | AC-Z01（3 表記別ケース）・Z02・Z03・Z04・Z05・Z06・Z09b | スキップ（人間判断・観点表本記載スキップに連動） |
| 5.5.2 | 記録しない行 | AC-Z07・Z08・Z09。マスタが作られないこと | スキップ（人間判断） |
| 5.5.3 | マスタ・再保存 | AC-Z10（`type='project'`・`project_id`）・Z10b（三者空）・Z11・Z12 | スキップ（人間判断） |
| 5.5.4 | 検証・混合 | AC-Z13（DB 非更新）・Z14・Z15・Z16 | スキップ（人間判断。Z13 メッセージ・Z14・Z16 は Group B で部分カバー） |
| 5.5.5 | スキーマ | AC-Z17・Z18・Z18b・Z19（`pg_constraint`）・Z20 | スキップ（人間判断） |

**注意:** 現行 DB はまだ `chk_hours_positive` のため、AC-Z17 / Z18b は制約差し替え前は FAIL する（Red として正しい）。AC-Z19 も現行は旧名が残るので FAIL。

### 5.6 E2E 候補カタログ

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.6.1 | C-Z1〜C-Z3 を追記する | `docs/開発プロセス/E2E候補カタログ.md`。コード化しない。表示モジュールのテストは新設しない | 完了（#6〜#8） |

### 5.7 Red Phase 証拠

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 5.7.1 | 司令塔が独立実行して記録する | `docs/設計/実績0hの保存/_review/test/red-phase-evidence.md`。new-feature / 書き換えたテスト: FAIL。regression（上限・空欄・認証・親 E2E 以外の既存）: PASS | 完了（Group B。5.5 は人間判断でスキップ） |

## Phase 6. テスト 敵対的レビュー（収束ループ）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 6.1 | 3レーン並列レビュー | A: トートロジー / B: AC 対応マトリクス / C: セキュリティ・Red 証拠。must-catch MC-1・MC-4・MC-5・MC-6・MC-9 | 完了 |
| 6.2 | Critical/Major が 0 まで収束 | 修正は `test-builder`。上限 5 周。Major の文書化通過は、人間が明示した場合のみ（ゲート実施のため可だが、AI 判断では不可） | 完了（Round 2） |

**ゲート:** Critical 0 かつ Major 0

## Phase 7. 人間レビュー（テスト承認ゲート）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 7.1 | 観点表の本記載 | 不可逆操作・DB制約・トランザクション。AI 記入例の残りを人間が埋める | 実施（人間） |
| 7.2 | テストと findings を承認する | 「テストを承認します」 | 実施（人間） |

**ゲート:** 人間の承認。差戻しなら Phase 5 へ。

## Phase 8. 実装（Green Phase）

実施者: `impl-builder`（fresh context）。**テストは変更しない。** 画面・`report-generator.ts`・`MonthlyTimesheetTable` は触らない。

**適用順（設計書マイグレーション必須）:** 制約の `db:push` が有効になってから判定下限を変える。同一 PR に載せてよいが、0.00 を INSERT するコードが動く環境ではその前に制約が付いていること。

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 8.1 | S-Z1 スキーマ | `tasks.ts` の `chk_hours_positive` を DROP し `chk_hours_non_negative`（`hours >= 0`）を ADD。`chk_planned_hours_positive` は触らない | 未着手 |
| 8.2 | `db:push` | 対象 DB に制約を反映。情報スキーマで AC-Z19 を満たすことを確認してから 8.3 | 未着手 |
| 8.3 | R-2 判定 2 | `classifyActualHours` の `value < 0.005` を `value < 0` にする。判定 4 は `0` 以上 `24.005` 未満。`-0` を `Object.is` で除外しない。アプリ側で丸めない（R-3） | 未着手 |
| 8.4 | `saveReportFn` | 分岐は増やさない。`classifyActualHours` の結果を使う現行のまま対象行が増える | 未着手（変更なし想定） |
| 8.5 | Green Phase 証拠 | 司令塔が独立実行。`_review/test/green-phase-evidence.md`。対象テスト PASS、既存 PASS | 未着手 |

**ゲート:** target-feature-tests PASS / regression-baseline PASS

## Phase 9. リファクタ

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 9.1 | 構造改善 | lean のため **省略可**。関数名・配置をテストが import しており、変更点は閾値と制約名に限られる | 省略可 |

**ゲート（実施する場合）:** 全テスト Green 維持

## Phase 10. 実装検証（テスト・E2E 実行＋実装レビュー・収束ループ）

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 10.1 | 実行検証 | `npm run test` / `test:integration` / `test:e2e`（親 E2E-1〜7 を含む）/ `check-types`。lint は該当なし（読み替えどおり `check-types` で代替）。1 項目も省略しない | 未着手 |
| 10.2 | 実装レビュー 3レーン | A: 仕様忠実性 / B: セキュリティ / C: テストのすり抜け。報告は Critical/Major のみ。must-catch MC-2・MC-3・MC-7・MC-8・MC-9 | 未着手 |
| 10.3 | 収束ループ | Critical 0 かつ Major 0。Major の文書化通過は**使えない**（実装承認スキップの代償）。修正は `impl-builder`。テストは誰も変えない | 未着手 |

**ゲート:** 実行検証すべて PASS ＋ Critical 0・Major 0

## 実装承認ゲート

| No | タスク | 詳細 | ステータス |
|---|---|---|---|
| 11.1 | 人間の実装承認 | `_flow-config.md` により **スキップ**。Phase 10 収束をもって完了。その旨を `_review/impl/findings.md` に記録する | スキップ |

## フィードバックルーティング（未収束・エスカレーション時）

| 失敗・findings の種類 | 戻り先 | ループ内で解消 |
|---|---|---|
| テスト・E2E の実行失敗（実装バグ） | Phase 8 | ✅ |
| 実装の仕様乖離・セキュリティ欠陥 | Phase 8 | ✅ |
| 型 / build の失敗 | Phase 8 | ✅ |
| テストの不備 | Phase 5 | ❌ 人間へ |
| エッジケースの欠落 | Phase 1 + Phase 5 | ❌ 人間へ |
| 仕様の曖昧性・要件不足 | Phase 1 | ❌ 人間へ |
| E2E 凍結仕様と実装挙動の乖離 | 人間判断 | ❌ 人間へ |

戻り先が Phase 1 / 5 の findings は、実装承認スキップでも人間へエスカレーションする。

## 完了条件

- [ ] Phase 5: Red 証拠あり（新規・書き換えテスト FAIL / 非矛盾既存 PASS）
- [x] Phase 6: Critical 0・Major 0
- [ ] Phase 7: 人間がテストを承認
- [ ] Phase 8: Green 証拠あり（適用順: 制約 → 判定）
- [ ] Phase 10: 全テスト・E2E・型が PASS、実装レビュー Critical 0・Major 0
- [ ] 画面・日報テキスト・月次表を変更していない
- [ ] バックフィル用の一括移行を入れてない

## 補足：テスト方針として議論すべき論点（メタ目的）

- Q.1 既存テストの書き換えを Phase 5 で行う理由は、Phase 8 がテスト変更禁止だからか（はい。申し送り 1）
- Q.2 結合(DB込み)の新規 AC を人間本記載前にコード化してよいか（いいえ。書き換え分は設計書 AC が確定しているので可。新規 5.5 は本記載後）
- Q.3 0h の E2E を自動化すべきか（いいえ。画面から `0.00` を観測できない。親 E2E-1〜7 の維持でクリティカルパスを満たす）
- Q.4 表示側不変をテストすべきか（いいえ。非ゴール。カタログ C-Z3 のみ）
- Q.5 AC-Z17 / Z18b は制約変更前に Red でよいか（はい）
- Q.6 `chk_hours_positive` を条件だけ変えて名前を残す実装をどう落とすか（AC-Z19 を `pg_constraint` で見る）
- Q.7 開発 DB を壊さないか（`report-db-helpers.ts`・センチネル日付・接頭辞。TRUNCATE しない）
- Q.8 判定だけ先にマージしてよいか（いいえ。旧 CHECK が 0.00 を拒否し保存全体が失敗する）
- Q.9 認可・PII の新規観点は要るか（非該当。`requireSession` は親テストが維持）
- Q.10 親 AC-23〜25・AC-28 を 0h で再掲するか（しない。新しい「有効な行」定義で継承）

## 将来拡張

- F.1 `getProjectSummary` が 0h 行を出すようにする（本スコープ外）
- F.2 `time_entries` を読む集計画面
- F.3 既存 `raw_data` の 0h 行のバックフィル
- F.4 予定h のみを 0h 実績として記録する
